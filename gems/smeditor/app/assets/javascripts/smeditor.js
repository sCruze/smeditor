/* SMEditor Rails browser bundle. Generated by scripts/build-gem-assets.mjs.
 * Contains SMEditor core + starter/full extensions. No npm/runtime JS dependencies. */
(function(){
"use strict";
var M={
0:[function(require,module,exports){
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.boot = boot;
exports.destroy = destroy;
const core_1 = require("@smeditor/core");
const starter_kit_1 = require("@smeditor/starter-kit");
const full_kit_1 = require("@smeditor/full-kit");
const instances = new Map();
const disposers = new Map();
const ICONS = {
    undo: '<path d="M3.5 8h7a3 3 0 0 1 0 6H6"/><path d="m6 4-3 4 3 4"/>',
    redo: '<path d="M12.5 8h-7a3 3 0 0 0 0 6H10"/><path d="m10 4 3 4-3 4"/>',
    bold: '<path d="M4 3h4.5a2.5 2.5 0 0 1 0 5H4z"/><path d="M4 8h5a2.5 2.5 0 0 1 0 5H4z"/>',
    italic: '<path d="M10 3 6 13"/><path d="M6 3h5"/><path d="M5 13h5"/>',
    strike: '<path d="M3 8h10"/><path d="M5.5 4.5C6 4 6.7 3.5 8 3.5c2 0 3 1 3 2.5"/><path d="M5 11c0 1.4 1.3 2.5 3 2.5 1.8 0 3-1 3-2.2"/>',
    underline: '<path d="M4 3v5a4 4 0 0 0 8 0V3"/><path d="M3.5 13.5h9"/>',
    code: '<path d="m6 5-3 3 3 3"/><path d="m10 5 3 3-3 3"/>',
    eraser: '<path d="m9 3 5 5-6 6H4l-1-1z"/><path d="m6 6 5 5"/><path d="M9 14h5"/>',
    link: '<path d="M7 9a3 3 0 0 0 4 0l2-2a3 3 0 1 0-4-4L8 4"/><path d="M9 7a3 3 0 0 0-4 0l-2 2a3 3 0 1 0 4 4l1-1"/>',
    highlight: '<path d="M3 13l3-1 6-6-2-2-6 6z"/><path d="M2 14h12"/>',
    textColor: '<text x="3" y="11" font-size="9" font-weight="700" fill="currentColor" stroke="none">A</text><rect x="3" y="13" width="10" height="2" fill="currentColor" stroke="none"/>',
    background: '<rect x="2" y="2" width="12" height="12" rx="1.5"/><path d="M2 2l12 12"/>',
    blockquote: '<path d="M3 5v3c0 1.5-.7 2.5-2 3"/><path d="M9 5v3c0 1.5-.7 2.5-2 3"/>',
    codeBlock: '<rect x="1.5" y="3" width="13" height="10" rx="1.5"/><path d="m5 6.5-2 1.5 2 1.5"/><path d="m11 6.5 2 1.5-2 1.5"/>',
    hr: '<path d="M2 8h12"/><path d="M2 4h8"/><path d="M2 12h6"/>',
    bullet: '<circle cx="3.5" cy="4.5" r=".7" fill="currentColor"/><circle cx="3.5" cy="8" r=".7" fill="currentColor"/><circle cx="3.5" cy="11.5" r=".7" fill="currentColor"/><path d="M6 4.5h7M6 8h7M6 11.5h7"/>',
    ordered: '<text x="2" y="6" font-size="4" font-family="monospace" fill="currentColor" stroke="none">1.</text><text x="2" y="13" font-size="4" font-family="monospace" fill="currentColor" stroke="none">2.</text><path d="M6 4.5h7M6 11.5h7"/>',
    task: '<rect x="2" y="3" width="3" height="3" rx=".5"/><path d="m2.5 4.5 1 1 1.5-1.5"/><rect x="2" y="10" width="3" height="3" rx=".5"/><path d="M7 4.5h6M7 11.5h6"/>',
    alignLeft: '<path d="M2.5 4h11M2.5 7h7M2.5 10h11M2.5 13h7"/>',
    alignCenter: '<path d="M2.5 4h11M4.5 7h7M2.5 10h11M4.5 13h7"/>',
    alignRight: '<path d="M2.5 4h11M6.5 7h7M2.5 10h11M6.5 13h7"/>',
    alignJustify: '<path d="M2.5 4h11M2.5 7h11M2.5 10h11M2.5 13h11"/>',
    indent: '<path d="M2 4h12M7 8h7M2 12h12"/><path d="m2 7 2 1.5L2 10" fill="currentColor"/>',
    outdent: '<path d="M2 4h12M7 8h7M2 12h12"/><path d="m6 7-2 1.5L6 10" fill="currentColor"/>',
    image: '<rect x="2" y="3" width="12" height="10" rx="1.5"/><circle cx="5.5" cy="6.5" r="1.2"/><path d="m2.5 11 3.5-3.5 2.5 2.5L11 7.5l2.5 2.5"/>',
    table: '<rect x="2" y="3" width="12" height="10" rx="1"/><path d="M2 6.5h12M2 10h12M6 3v10M10 3v10"/>',
    more: '<circle cx="3.5" cy="8" r="1.2" fill="currentColor"/><circle cx="8" cy="8" r="1.2" fill="currentColor"/><circle cx="12.5" cy="8" r="1.2" fill="currentColor"/>',
    heading: '<text x="2" y="12" font-family="sans-serif" font-size="9" font-weight="700" fill="currentColor" stroke="none">H</text><text x="9.5" y="13" font-family="sans-serif" font-size="6" font-weight="700" fill="currentColor" stroke="none">2</text>',
    paragraph: '<text x="3" y="12" font-family="serif" font-size="11" font-weight="700" fill="currentColor" stroke="none">¶</text>',
    subscript: '<path d="M3 4l4 5"/><path d="M7 4l-4 5"/><text x="9" y="14" font-size="6" font-weight="700" fill="currentColor" stroke="none">x</text>',
    superscript: '<path d="M3 6l4 5"/><path d="M7 6l-4 5"/><text x="9" y="7" font-size="6" font-weight="700" fill="currentColor" stroke="none">x</text>',
};
function svgIcon(name, strokeWidth = 1.5) {
    const svg = document.createElementNS("http://www.w3.org/2000/svg", "svg");
    svg.setAttribute("width", "16");
    svg.setAttribute("height", "16");
    svg.setAttribute("viewBox", "0 0 16 16");
    svg.setAttribute("fill", "none");
    svg.setAttribute("stroke", "currentColor");
    svg.setAttribute("stroke-width", String(strokeWidth));
    svg.setAttribute("stroke-linecap", "round");
    svg.setAttribute("stroke-linejoin", "round");
    svg.setAttribute("aria-hidden", "true");
    svg.innerHTML = ICONS[name] || "";
    return svg;
}
function csrfToken() {
    var _a, _b;
    return (_b = (_a = document.querySelector('meta[name="csrf-token"]')) === null || _a === void 0 ? void 0 : _a.content) !== null && _b !== void 0 ? _b : null;
}
function uploadHandler(uploadUrl) {
    if (!uploadUrl)
        return undefined;
    return async (file) => {
        const body = new FormData();
        body.append("file", file);
        const headers = {};
        const token = csrfToken();
        if (token)
            headers["X-CSRF-Token"] = token;
        const response = await fetch(uploadUrl, {
            method: "POST",
            headers,
            body,
            credentials: "same-origin",
        });
        if (!response.ok)
            throw new Error(`SMEditor upload failed with ${response.status}`);
        return response.json();
    };
}
function kitExtensions(kit, uploadUrl) {
    const base = kit === "full" ? full_kit_1.FullKit.extensions : starter_kit_1.StarterKit.extensions;
    const upload = uploadHandler(uploadUrl);
    if (!upload)
        return [...base];
    return base.map((extension) => extension.name === "image" ? starter_kit_1.ImageExtension.configure({ upload }) : extension);
}
function run(editor, command, ...args) {
    const fn = editor.commands[command];
    if (typeof fn !== "function")
        return false;
    const result = fn(...args);
    editor.focus();
    return Boolean(result);
}
function hasCommand(editor, command) {
    return typeof editor.commands[command] === "function";
}
function divider() {
    const el = document.createElement("span");
    el.className = "smeditor-toolbar__divider";
    el.setAttribute("role", "separator");
    el.setAttribute("aria-hidden", "true");
    return el;
}
function commandButton(editor, iconName, title, command, active, label) {
    const el = document.createElement("button");
    el.type = "button";
    el.className = "smeditor-button";
    el.title = title;
    el.setAttribute("aria-label", title);
    if (label)
        el.textContent = label;
    else
        el.appendChild(svgIcon(iconName, iconName === "bold" ? 2 : 1.5));
    el.addEventListener("mousedown", (event) => event.preventDefault());
    el.addEventListener("click", () => command());
    const refresh = () => {
        if (active) {
            const value = active();
            el.dataset.active = value ? "true" : "false";
            el.classList.toggle("is-active", value);
        }
    };
    el.__smeditorRefresh = refresh;
    refresh();
    return el;
}
function dropdown(ariaLabel, triggerContent, items, options = {}) {
    const wrapper = document.createElement("div");
    wrapper.className = "smeditor-dropdown";
    const trigger = document.createElement("button");
    trigger.type = "button";
    trigger.className = "smeditor-button smeditor-dropdown__trigger";
    trigger.setAttribute("aria-haspopup", "menu");
    trigger.setAttribute("aria-expanded", "false");
    trigger.setAttribute("aria-label", ariaLabel);
    trigger.title = ariaLabel;
    const label = document.createElement("span");
    label.className = "smeditor-dropdown__label";
    if (typeof triggerContent === "string")
        label.textContent = triggerContent;
    else
        label.appendChild(triggerContent);
    trigger.appendChild(label);
    let summary = null;
    if (options.summary) {
        summary = document.createElement("span");
        summary.className = "smeditor-dropdown__summary";
        trigger.appendChild(summary);
    }
    const chevron = document.createElement("span");
    chevron.className = "smeditor-dropdown__chevron";
    chevron.setAttribute("aria-hidden", "true");
    chevron.textContent = "▾";
    trigger.appendChild(chevron);
    wrapper.appendChild(trigger);
    let menu = null;
    const close = () => {
        wrapper.classList.remove("is-open");
        trigger.setAttribute("aria-expanded", "false");
        menu === null || menu === void 0 ? void 0 : menu.remove();
        menu = null;
    };
    const open = () => {
        var _a, _b, _c, _d;
        if (menu)
            return close();
        wrapper.classList.add("is-open");
        trigger.setAttribute("aria-expanded", "true");
        menu = document.createElement("div");
        menu.className = "smeditor-dropdown__menu" + (options.align === "right" ? " is-right" : "");
        menu.setAttribute("role", "menu");
        menu.setAttribute("aria-label", ariaLabel);
        for (const spec of items()) {
            if (spec.separatorBefore) {
                const sep = document.createElement("div");
                sep.className = "smeditor-dropdown__separator";
                sep.setAttribute("role", "separator");
                menu.appendChild(sep);
            }
            if (spec.content) {
                menu.appendChild(spec.content);
                continue;
            }
            const item = document.createElement("button");
            item.type = "button";
            item.className = "smeditor-dropdown__item";
            item.setAttribute("role", spec.active ? "menuitemcheckbox" : "menuitem");
            const isActive = (_b = (_a = spec.active) === null || _a === void 0 ? void 0 : _a.call(spec)) !== null && _b !== void 0 ? _b : false;
            if (spec.active)
                item.setAttribute("aria-checked", String(isActive));
            item.classList.toggle("is-active", isActive);
            item.dataset.active = isActive ? "true" : "false";
            const isDisabled = (_d = (_c = spec.disabled) === null || _c === void 0 ? void 0 : _c.call(spec)) !== null && _d !== void 0 ? _d : false;
            item.disabled = isDisabled;
            if (isDisabled)
                item.setAttribute("aria-disabled", "true");
            if (spec.icon) {
                const icon = document.createElement("span");
                icon.className = "smeditor-dropdown__item-icon";
                icon.appendChild(svgIcon(spec.icon));
                item.appendChild(icon);
            }
            const text = document.createElement("span");
            text.className = "smeditor-dropdown__item-label";
            text.textContent = spec.label;
            item.appendChild(text);
            if (spec.shortcut) {
                const shortcut = document.createElement("span");
                shortcut.className = "smeditor-dropdown__item-shortcut";
                shortcut.textContent = spec.shortcut;
                item.appendChild(shortcut);
            }
            item.addEventListener("mousedown", (event) => event.preventDefault());
            item.addEventListener("click", () => {
                var _a;
                if (item.disabled)
                    return;
                (_a = spec.action) === null || _a === void 0 ? void 0 : _a.call(spec);
                close();
            });
            menu.appendChild(item);
        }
        wrapper.appendChild(menu);
    };
    trigger.addEventListener("mousedown", (event) => event.preventDefault());
    trigger.addEventListener("click", (event) => {
        event.stopPropagation();
        open();
    });
    const outside = (event) => {
        if (!menu)
            return;
        const path = event.composedPath();
        if (!path.includes(wrapper))
            close();
    };
    document.addEventListener("pointerdown", outside, true);
    const refresh = () => {
        if (summary && options.summary)
            summary.textContent = options.summary();
        if (options.active) {
            const value = options.active();
            trigger.dataset.active = value ? "true" : "false";
            trigger.classList.toggle("is-active", value);
        }
    };
    wrapper.__smeditorRefresh = refresh;
    wrapper.__smeditorDispose = () => document.removeEventListener("pointerdown", outside, true);
    refresh();
    return wrapper;
}
function colorDropdown(editor, ariaLabel, iconName, markName, setCommand, unsetCommand, extraAttrs = {}) {
    const colors = [
        "#1a1a1f", "#6b7280", "#ef4444", "#f97316", "#eab308",
        "#22c55e", "#06b6d4", "#3b82f6", "#8b5cf6", "#ec4899",
        "#ffffff", "#d1d5db", "#fecaca", "#fed7aa", "#fef08a",
        "#bbf7d0", "#a5f3fc", "#bfdbfe", "#ddd6fe", "#fbcfe8",
    ];
    return dropdown(ariaLabel, svgIcon(iconName), () => {
        const grid = document.createElement("div");
        grid.className = "smeditor-color-grid";
        grid.setAttribute("role", "group");
        const clear = document.createElement("button");
        clear.type = "button";
        clear.className = "smeditor-color-grid__swatch smeditor-color-grid__swatch--clear";
        clear.title = `Clear ${ariaLabel.toLowerCase()}`;
        clear.setAttribute("aria-label", clear.title);
        clear.addEventListener("mousedown", (e) => e.preventDefault());
        clear.addEventListener("click", () => run(editor, unsetCommand));
        grid.appendChild(clear);
        for (const color of colors) {
            const swatch = document.createElement("button");
            swatch.type = "button";
            swatch.className = "smeditor-color-grid__swatch";
            swatch.style.backgroundColor = color;
            swatch.title = color;
            swatch.setAttribute("aria-label", `${ariaLabel}: ${color}`);
            swatch.addEventListener("mousedown", (e) => e.preventDefault());
            swatch.addEventListener("click", () => run(editor, setCommand, { color, ...extraAttrs }));
            grid.appendChild(swatch);
        }
        return [{ label: "", content: grid }];
    }, { active: () => editor.isActive(markName) });
}
function blockTypeSummary(editor) {
    if ((0, core_1.selectionInsideWrapper)(editor.getJSON(), editor.getSelection(), "blockquote"))
        return "❝";
    if (editor.isActive("code_block"))
        return "</>";
    for (const level of [1, 2, 3, 4, 5, 6]) {
        if (editor.isActive("heading", { level }))
            return `H${level}`;
    }
    return "P";
}
function tablePicker(editor) {
    return dropdown("Insert table", svgIcon("table"), () => {
        const grid = document.createElement("div");
        grid.className = "smeditor-table-picker";
        grid.setAttribute("role", "group");
        grid.setAttribute("aria-label", "Insert table size");
        const cells = [];
        const refreshHover = (rows, cols) => {
            cells.forEach((cell, idx) => {
                const r = Math.floor(idx / 6) + 1;
                const c = (idx % 6) + 1;
                cell.classList.toggle("is-on", r <= rows && c <= cols);
            });
            label.textContent = rows && cols ? `${rows} × ${cols}` : "Select size";
        };
        for (let r = 1; r <= 6; r++) {
            for (let c = 1; c <= 6; c++) {
                const cell = document.createElement("button");
                cell.type = "button";
                cell.className = "smeditor-table-picker__cell";
                cell.setAttribute("aria-label", `Insert ${r} by ${c} table`);
                cell.addEventListener("mouseenter", () => refreshHover(r, c));
                cell.addEventListener("mousedown", (e) => e.preventDefault());
                cell.addEventListener("click", () => run(editor, "insertTable", { rows: r, cols: c }));
                cells.push(cell);
                grid.appendChild(cell);
            }
        }
        const label = document.createElement("div");
        label.className = "smeditor-table-picker__label";
        label.textContent = "Select size";
        const box = document.createElement("div");
        box.append(grid, label);
        box.addEventListener("mouseleave", () => refreshHover(0, 0));
        return [{ label: "", content: box }];
    });
}
function textDropdown(editor, ariaLabel, label, values, command, unset) {
    return dropdown(ariaLabel, label, () => values.map(([text, value]) => ({
        label: text,
        action: () => value === null && unset ? run(editor, unset) : run(editor, command, { [command === "setFontFamily" ? "family" : command === "setFontSize" ? "size" : "value"]: value }),
    })));
}
function buildToolbar(editor, kit, uploadUrl) {
    const toolbar = document.createElement("div");
    toolbar.className = "smeditor-toolbar";
    toolbar.setAttribute("role", "toolbar");
    toolbar.setAttribute("aria-label", "SMEditor toolbar");
    toolbar.setAttribute("aria-orientation", "horizontal");
    const controls = [];
    const add = (...els) => { toolbar.append(...els); controls.push(...els); };
    const undo = commandButton(editor, "undo", "Undo", () => run(editor, "undo"));
    const redo = commandButton(editor, "redo", "Redo", () => run(editor, "redo"));
    add(undo, redo, divider());
    const block = dropdown("Block type", svgIcon("heading"), () => {
        const items = [{ label: "Paragraph", icon: "paragraph", active: () => editor.isActive("paragraph"), action: () => run(editor, "setParagraph") }];
        for (let level = 1; level <= 6; level++)
            items.push({ label: `Heading ${level}`, icon: "heading", shortcut: `⌘⌥${level}`, active: () => editor.isActive("heading", { level }), action: () => run(editor, "setHeading", { level }), separatorBefore: level === 1 });
        items.push({ label: "Quote", icon: "blockquote", active: () => (0, core_1.selectionInsideWrapper)(editor.getJSON(), editor.getSelection(), "blockquote"), action: () => run(editor, "toggleBlockquote"), separatorBefore: true });
        items.push({ label: "Code block", icon: "codeBlock", active: () => editor.isActive("code_block"), action: () => run(editor, "setCodeBlock") });
        return items;
    }, { summary: () => blockTypeSummary(editor) });
    const lists = dropdown("Lists", svgIcon("bullet"), () => [
        { label: "Bullet List", icon: "bullet", shortcut: "⌘⇧8", active: () => (0, core_1.selectionInsideWrapper)(editor.getJSON(), editor.getSelection(), "bullet_list"), action: () => run(editor, "toggleBulletList") },
        { label: "Ordered List", icon: "ordered", shortcut: "⌘⇧7", active: () => (0, core_1.selectionInsideWrapper)(editor.getJSON(), editor.getSelection(), "ordered_list"), action: () => run(editor, "toggleOrderedList") },
        { label: "Task List", icon: "task", shortcut: "⌘⇧9", active: () => (0, core_1.selectionInsideWrapper)(editor.getJSON(), editor.getSelection(), "task_list"), action: () => run(editor, "toggleTaskList") },
    ], { active: () => ["bullet_list", "ordered_list", "task_list"].some((name) => (0, core_1.selectionInsideWrapper)(editor.getJSON(), editor.getSelection(), name)) });
    add(block, lists, divider());
    add(commandButton(editor, "bold", "Bold", () => run(editor, "toggleBold"), () => editor.isActive("bold")), commandButton(editor, "italic", "Italic", () => run(editor, "toggleItalic"), () => editor.isActive("italic")), commandButton(editor, "strike", "Strike-through", () => run(editor, "toggleStrike"), () => editor.isActive("strike")), commandButton(editor, "underline", "Underline", () => run(editor, "toggleUnderline"), () => editor.isActive("underline")), commandButton(editor, "eraser", "Clear formatting", () => run(editor, "clearFormatting")), commandButton(editor, "link", "Link", () => {
        if (editor.isActive("link"))
            return run(editor, "unsetLink");
        const href = window.prompt("Link URL", "https://");
        return href ? run(editor, "setLink", { href }) : false;
    }, () => editor.isActive("link")), divider());
    const align = dropdown("Alignment", svgIcon("alignLeft"), () => [
        { label: "Left", icon: "alignLeft", active: () => editor.isActive("text_align", { align: "left" }), action: () => run(editor, "setTextAlign", { align: "left" }) },
        { label: "Center", icon: "alignCenter", active: () => editor.isActive("text_align", { align: "center" }), action: () => run(editor, "setTextAlign", { align: "center" }) },
        { label: "Right", icon: "alignRight", active: () => editor.isActive("text_align", { align: "right" }), action: () => run(editor, "setTextAlign", { align: "right" }) },
        { label: "Justify", icon: "alignJustify", active: () => editor.isActive("text_align", { align: "justify" }), action: () => run(editor, "setTextAlign", { align: "justify" }) },
        { label: "Increase indent", icon: "indent", action: () => run(editor, "indent"), separatorBefore: true },
        { label: "Decrease indent", icon: "outdent", action: () => run(editor, "outdent") },
    ]);
    add(align, divider());
    add(commandButton(editor, "blockquote", "Blockquote", () => run(editor, "toggleBlockquote"), () => (0, core_1.selectionInsideWrapper)(editor.getJSON(), editor.getSelection(), "blockquote")), commandButton(editor, "codeBlock", "Code block", () => run(editor, "setCodeBlock"), () => editor.isActive("code_block")), commandButton(editor, "hr", "Horizontal rule", () => run(editor, "insertHorizontalRule")));
    const image = dropdown("Insert image", svgIcon("image"), () => {
        const items = [{ label: "From URL…", icon: "link", action: () => {
                    var _a;
                    const src = window.prompt("Image URL", "https://");
                    if (!src)
                        return;
                    const alt = (_a = window.prompt("Alt text (optional)", "")) !== null && _a !== void 0 ? _a : "";
                    run(editor, "insertImage", { src, alt: alt || undefined });
                } }];
        if (uploadUrl && hasCommand(editor, "uploadImage"))
            items.push({ label: "Upload image…", icon: "image", action: () => {
                    const input = document.createElement("input");
                    input.type = "file";
                    input.accept = "image/*";
                    input.onchange = () => { var _a; const file = (_a = input.files) === null || _a === void 0 ? void 0 : _a[0]; if (file)
                        run(editor, "uploadImage", file); };
                    input.click();
                } });
        return items;
    });
    add(image, tablePicker(editor));
    if (kit === "full") {
        add(divider());
        add(colorDropdown(editor, "Highlight", "highlight", "highlight", "setHighlight", "unsetHighlight"), colorDropdown(editor, "Text color", "textColor", "text_color", "setTextColor", "unsetTextColor"), colorDropdown(editor, "Text stroke", "textColor", "text_stroke", "setTextStroke", "unsetTextStroke", { width: 1 }), colorDropdown(editor, "Background color", "background", "background_color", "setBackgroundColor", "unsetBackgroundColor"), textDropdown(editor, "Font family", "Font", [["Default", null], ["Sans-serif", "ui-sans-serif, system-ui, sans-serif"], ["Serif", "ui-serif, Georgia, serif"], ["Monospace", "ui-monospace, Menlo, monospace"], ["Georgia", "Georgia, serif"], ["Courier", "'Courier New', monospace"]], "setFontFamily", "unsetFontFamily"), textDropdown(editor, "Font size", "Size", [["Default", null], ["Small · 13px", "13px"], ["Normal · 16px", "16px"], ["Large · 20px", "20px"], ["Huge · 28px", "28px"]], "setFontSize", "unsetFontSize"), textDropdown(editor, "Line height", "Spacing", [["Default", null], ["Tight · 1", "1"], ["Snug · 1.15", "1.15"], ["Normal · 1.5", "1.5"], ["Relaxed · 2", "2"]], "setLineHeight"));
        if (hasCommand(editor, "addComment")) {
            add(commandButton(editor, "", "Comment", () => {
                const body = window.prompt("New comment");
                if (!body)
                    return false;
                return run(editor, "addComment", { threadId: `comment-${Date.now()}` });
            }, undefined, "Comment"));
        }
        const more = dropdown("More tools", svgIcon("more"), () => [
            { label: "Inline code", icon: "code", shortcut: "⌘E", active: () => editor.isActive("code"), action: () => run(editor, "toggleCode") },
            { label: "Subscript", icon: "subscript", active: () => editor.isActive("subscript"), action: () => run(editor, "toggleSubscript"), separatorBefore: true },
            { label: "Superscript", icon: "superscript", active: () => editor.isActive("superscript"), action: () => run(editor, "toggleSuperscript") },
            { label: "Increase indent", icon: "indent", action: () => run(editor, "indent"), separatorBefore: true },
            { label: "Decrease indent", icon: "outdent", action: () => run(editor, "outdent") },
        ], { align: "right" });
        add(more);
    }
    const refresh = () => {
        var _a, _b;
        undo.toggleAttribute("disabled", !editor.canUndo());
        redo.toggleAttribute("disabled", !editor.canRedo());
        for (const control of controls)
            (_b = (_a = control).__smeditorRefresh) === null || _b === void 0 ? void 0 : _b.call(_a);
    };
    const disposeUpdate = editor.on("update", refresh);
    const disposeSelection = editor.on("selectionUpdate", refresh);
    queueMicrotask(refresh);
    toolbar.__smeditorDispose = () => {
        var _a, _b;
        disposeUpdate();
        disposeSelection();
        for (const control of controls)
            (_b = (_a = control).__smeditorDispose) === null || _b === void 0 ? void 0 : _b.call(_a);
    };
    return toolbar;
}
function shadowRootFor(mount) {
    const root = mount.shadowRoot || mount.attachShadow({ mode: "open" });
    root.innerHTML = "";
    const stylesheet = mount.dataset.smeditorStylesheet;
    if (stylesheet) {
        const link = document.createElement("link");
        link.rel = "stylesheet";
        link.href = stylesheet;
        root.appendChild(link);
    }
    return root;
}
function bootMount(mount) {
    var _a, _b;
    if (instances.has(mount))
        return;
    const field = mount.closest(".smeditor-field");
    const input = field === null || field === void 0 ? void 0 : field.querySelector("[data-smeditor-input]");
    if (!field || !input)
        return;
    const kit = mount.dataset.smeditorKit === "full" ? "full" : "starter";
    const uploadUrl = mount.dataset.smeditorUploadUrl || undefined;
    const placeholder = mount.dataset.smeditorPlaceholder || undefined;
    const explicitTheme = ((_b = (_a = mount.parentElement) === null || _a === void 0 ? void 0 : _a.closest("[data-theme]")) === null || _b === void 0 ? void 0 : _b.dataset.theme)
        || document.documentElement.dataset.theme;
    if (explicitTheme === "light" || explicitTheme === "dark")
        mount.dataset.theme = explicitTheme;
    const root = shadowRootFor(mount);
    const shell = document.createElement("div");
    shell.className = "smeditor";
    const responsiveHeights = [
        ["--sme-editor-min-height", mount.dataset.smeditorMinHeight],
        ["--sme-editor-min-height-tablet", mount.dataset.smeditorTabletMinHeight],
        ["--sme-editor-min-height-mobile", mount.dataset.smeditorMobileMinHeight],
    ];
    for (const [property, value] of responsiveHeights) {
        if (value)
            shell.style.setProperty(property, value);
    }
    const surface = document.createElement("div");
    surface.className = "smeditor-editor";
    surface.setAttribute("aria-label", mount.dataset.smeditorLabel || "Rich text editor");
    shell.appendChild(surface);
    root.appendChild(shell);
    const editor = (0, core_1.createEditor)({
        element: surface,
        extensions: kitExtensions(kit, uploadUrl),
        content: input.value || "<p></p>",
        placeholder,
        deepSelection: true,
        onUpdate: ({ editor: current }) => {
            input.value = current.getHTML();
            input.dispatchEvent(new Event("input", { bubbles: true }));
            input.dispatchEvent(new CustomEvent("smeditor:change", { bubbles: true, detail: { editor: current } }));
        },
    });
    const toolbar = buildToolbar(editor, kit, uploadUrl);
    shell.insertBefore(toolbar, surface);
    input.smeditorInstance = editor;
    mount.smeditorInstance = editor;
    mount.dataset.smeditorBooted = "1";
    instances.set(mount, editor);
    disposers.set(mount, [() => { var _a, _b; return (_b = (_a = toolbar).__smeditorDispose) === null || _b === void 0 ? void 0 : _b.call(_a); }]);
}
function boot(root = document) {
    root.querySelectorAll("[data-smeditor]").forEach(bootMount);
}
function destroy(root = document) {
    root.querySelectorAll("[data-smeditor]").forEach((mount) => {
        var _a;
        const editor = instances.get(mount);
        if (!editor)
            return;
        for (const dispose of (_a = disposers.get(mount)) !== null && _a !== void 0 ? _a : [])
            dispose();
        disposers.delete(mount);
        editor.destroy();
        const field = mount.closest(".smeditor-field");
        const input = field === null || field === void 0 ? void 0 : field.querySelector("[data-smeditor-input]");
        if (input)
            delete input.smeditorInstance;
        delete mount.smeditorInstance;
        delete mount.dataset.smeditorBooted;
        instances.delete(mount);
        if (mount.shadowRoot)
            mount.shadowRoot.innerHTML = "";
    });
}
const api = { boot, destroy, createEditor: core_1.createEditor, StarterKit: starter_kit_1.StarterKit, FullKit: full_kit_1.FullKit, ImageExtension: starter_kit_1.ImageExtension };
if (typeof window !== "undefined") {
    window.SMEditor = Object.assign(window.SMEditor || {}, api);
    if (!window.__smeditorRailsEventsBound) {
        window.__smeditorRailsEventsBound = true;
        if (document.readyState === "loading")
            document.addEventListener("DOMContentLoaded", () => { var _a; return (_a = window.SMEditor) === null || _a === void 0 ? void 0 : _a.boot(); });
        else
            boot();
        document.addEventListener("turbo:load", () => { var _a; return (_a = window.SMEditor) === null || _a === void 0 ? void 0 : _a.boot(); });
        document.addEventListener("turbo:frame-load", (event) => { var _a; return (_a = window.SMEditor) === null || _a === void 0 ? void 0 : _a.boot(event.target || document); });
        document.addEventListener("turbo:before-cache", () => { var _a; return (_a = window.SMEditor) === null || _a === void 0 ? void 0 : _a.destroy(); });
    }
    else {
        boot();
    }
}

},{"@smeditor/core":1,"@smeditor/starter-kit":12,"@smeditor/full-kit":39}],
1:[function(require,module,exports){
"use strict";
/**
 * @smeditor/core — public API.
 *
 * This barrel re-exports the framework-agnostic surface of the editor.
 * Higher-level packages (`@smeditor/react`, extensions, …) import from
 * here. We are deliberate about what is re-exported: internal helpers
 * stay internal so we can evolve them without breaking consumers.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.selectionBlockRange = exports.normalizeDeepPoint = exports.resolveSelectionTarget = exports.removeNodeAt = exports.mapNodeAt = exports.descendToLeafPath = exports.parentAt = exports.nodeAt = exports.isContainerBlock = exports.isLeafBlock = exports.splitBlock = exports.removeMarkFromInlineRange = exports.setMarkOnInlineRange = exports.applyMarkToInlineRange = exports.replaceInlineRange = exports.deleteInlineRange = exports.insertInlineAt = exports.splitInlineAt = exports.blockVisibleText = exports.inlineLength = exports.visibleLength = exports.stripBlockPrefix = exports.getBlockText = exports.leafBlocksInSelection = exports.setBlockType = exports.clearMarksAcrossSelection = exports.unsetMarkAcrossSelection = exports.setMarkAcrossSelection = exports.toggleMarkInDoc = exports.selectionHasMark = exports.hasMark = exports.emptyDocument = exports.docsEqual = exports.cloneDoc = exports.EventEmitter = exports.History = exports.hardenLinkAttrs = exports.sanitizeLinkTarget = exports.sanitizeURL = exports.sanitizeCSSTextStrokeWidth = exports.sanitizeCSSLineHeight = exports.sanitizeCSSFontSize = exports.sanitizeCSSFontFamily = exports.sanitizeCSSColor = exports.escapeHTML = exports.parseHTML = exports.serializeToHTML = exports.flattenExtensions = exports.compileSchema = exports.createEditor = void 0;
exports.VERSION = exports.getBlockAttr = exports.setBlockAttrs = exports.selectionInsideWrapper = exports.toggleWrap = exports.unwrapBlock = exports.wrapBlocks = void 0;
var editor_js_1 = require("./editor.js");
Object.defineProperty(exports, "createEditor", { enumerable: true, get: function () { return editor_js_1.createEditor; } });
var schema_js_1 = require("./schema.js");
Object.defineProperty(exports, "compileSchema", { enumerable: true, get: function () { return schema_js_1.compileSchema; } });
Object.defineProperty(exports, "flattenExtensions", { enumerable: true, get: function () { return schema_js_1.flattenExtensions; } });
var serializer_js_1 = require("./serializer.js");
Object.defineProperty(exports, "serializeToHTML", { enumerable: true, get: function () { return serializer_js_1.serializeToHTML; } });
Object.defineProperty(exports, "parseHTML", { enumerable: true, get: function () { return serializer_js_1.parseHTML; } });
Object.defineProperty(exports, "escapeHTML", { enumerable: true, get: function () { return serializer_js_1.escapeHTML; } });
var css_js_1 = require("./css.js");
Object.defineProperty(exports, "sanitizeCSSColor", { enumerable: true, get: function () { return css_js_1.sanitizeCSSColor; } });
Object.defineProperty(exports, "sanitizeCSSFontFamily", { enumerable: true, get: function () { return css_js_1.sanitizeCSSFontFamily; } });
Object.defineProperty(exports, "sanitizeCSSFontSize", { enumerable: true, get: function () { return css_js_1.sanitizeCSSFontSize; } });
Object.defineProperty(exports, "sanitizeCSSLineHeight", { enumerable: true, get: function () { return css_js_1.sanitizeCSSLineHeight; } });
Object.defineProperty(exports, "sanitizeCSSTextStrokeWidth", { enumerable: true, get: function () { return css_js_1.sanitizeCSSTextStrokeWidth; } });
Object.defineProperty(exports, "sanitizeURL", { enumerable: true, get: function () { return css_js_1.sanitizeURL; } });
Object.defineProperty(exports, "sanitizeLinkTarget", { enumerable: true, get: function () { return css_js_1.sanitizeLinkTarget; } });
Object.defineProperty(exports, "hardenLinkAttrs", { enumerable: true, get: function () { return css_js_1.hardenLinkAttrs; } });
var history_js_1 = require("./history.js");
Object.defineProperty(exports, "History", { enumerable: true, get: function () { return history_js_1.History; } });
var events_js_1 = require("./events.js");
Object.defineProperty(exports, "EventEmitter", { enumerable: true, get: function () { return events_js_1.EventEmitter; } });
var doc_utils_js_1 = require("./doc-utils.js");
Object.defineProperty(exports, "cloneDoc", { enumerable: true, get: function () { return doc_utils_js_1.cloneDoc; } });
Object.defineProperty(exports, "docsEqual", { enumerable: true, get: function () { return doc_utils_js_1.docsEqual; } });
Object.defineProperty(exports, "emptyDocument", { enumerable: true, get: function () { return doc_utils_js_1.emptyDocument; } });
Object.defineProperty(exports, "hasMark", { enumerable: true, get: function () { return doc_utils_js_1.hasMark; } });
Object.defineProperty(exports, "selectionHasMark", { enumerable: true, get: function () { return doc_utils_js_1.selectionHasMark; } });
Object.defineProperty(exports, "toggleMarkInDoc", { enumerable: true, get: function () { return doc_utils_js_1.toggleMarkInDoc; } });
Object.defineProperty(exports, "setMarkAcrossSelection", { enumerable: true, get: function () { return doc_utils_js_1.setMarkAcrossSelection; } });
Object.defineProperty(exports, "unsetMarkAcrossSelection", { enumerable: true, get: function () { return doc_utils_js_1.unsetMarkAcrossSelection; } });
Object.defineProperty(exports, "clearMarksAcrossSelection", { enumerable: true, get: function () { return doc_utils_js_1.clearMarksAcrossSelection; } });
Object.defineProperty(exports, "setBlockType", { enumerable: true, get: function () { return doc_utils_js_1.setBlockType; } });
Object.defineProperty(exports, "leafBlocksInSelection", { enumerable: true, get: function () { return doc_utils_js_1.leafBlocksInSelection; } });
Object.defineProperty(exports, "getBlockText", { enumerable: true, get: function () { return doc_utils_js_1.getBlockText; } });
Object.defineProperty(exports, "stripBlockPrefix", { enumerable: true, get: function () { return doc_utils_js_1.stripBlockPrefix; } });
var transform_js_1 = require("./transform.js");
Object.defineProperty(exports, "visibleLength", { enumerable: true, get: function () { return transform_js_1.visibleLength; } });
Object.defineProperty(exports, "inlineLength", { enumerable: true, get: function () { return transform_js_1.inlineLength; } });
Object.defineProperty(exports, "blockVisibleText", { enumerable: true, get: function () { return transform_js_1.blockVisibleText; } });
Object.defineProperty(exports, "splitInlineAt", { enumerable: true, get: function () { return transform_js_1.splitInlineAt; } });
Object.defineProperty(exports, "insertInlineAt", { enumerable: true, get: function () { return transform_js_1.insertInlineAt; } });
Object.defineProperty(exports, "deleteInlineRange", { enumerable: true, get: function () { return transform_js_1.deleteInlineRange; } });
Object.defineProperty(exports, "replaceInlineRange", { enumerable: true, get: function () { return transform_js_1.replaceInlineRange; } });
Object.defineProperty(exports, "applyMarkToInlineRange", { enumerable: true, get: function () { return transform_js_1.applyMarkToInlineRange; } });
Object.defineProperty(exports, "setMarkOnInlineRange", { enumerable: true, get: function () { return transform_js_1.setMarkOnInlineRange; } });
Object.defineProperty(exports, "removeMarkFromInlineRange", { enumerable: true, get: function () { return transform_js_1.removeMarkFromInlineRange; } });
Object.defineProperty(exports, "splitBlock", { enumerable: true, get: function () { return transform_js_1.splitBlock; } });
var path_js_1 = require("./path.js");
Object.defineProperty(exports, "isLeafBlock", { enumerable: true, get: function () { return path_js_1.isLeafBlock; } });
Object.defineProperty(exports, "isContainerBlock", { enumerable: true, get: function () { return path_js_1.isContainerBlock; } });
Object.defineProperty(exports, "nodeAt", { enumerable: true, get: function () { return path_js_1.nodeAt; } });
Object.defineProperty(exports, "parentAt", { enumerable: true, get: function () { return path_js_1.parentAt; } });
Object.defineProperty(exports, "descendToLeafPath", { enumerable: true, get: function () { return path_js_1.descendToLeafPath; } });
Object.defineProperty(exports, "mapNodeAt", { enumerable: true, get: function () { return path_js_1.mapNodeAt; } });
Object.defineProperty(exports, "removeNodeAt", { enumerable: true, get: function () { return path_js_1.removeNodeAt; } });
Object.defineProperty(exports, "resolveSelectionTarget", { enumerable: true, get: function () { return path_js_1.resolveSelectionTarget; } });
Object.defineProperty(exports, "normalizeDeepPoint", { enumerable: true, get: function () { return path_js_1.normalizeDeepPoint; } });
var block_ops_js_1 = require("./block-ops.js");
Object.defineProperty(exports, "selectionBlockRange", { enumerable: true, get: function () { return block_ops_js_1.selectionBlockRange; } });
Object.defineProperty(exports, "wrapBlocks", { enumerable: true, get: function () { return block_ops_js_1.wrapBlocks; } });
Object.defineProperty(exports, "unwrapBlock", { enumerable: true, get: function () { return block_ops_js_1.unwrapBlock; } });
Object.defineProperty(exports, "toggleWrap", { enumerable: true, get: function () { return block_ops_js_1.toggleWrap; } });
Object.defineProperty(exports, "selectionInsideWrapper", { enumerable: true, get: function () { return block_ops_js_1.selectionInsideWrapper; } });
Object.defineProperty(exports, "setBlockAttrs", { enumerable: true, get: function () { return block_ops_js_1.setBlockAttrs; } });
Object.defineProperty(exports, "getBlockAttr", { enumerable: true, get: function () { return block_ops_js_1.getBlockAttr; } });
/** Library version — kept in sync with package.json by build tooling. */
exports.VERSION = "0.0.12";

},{"./editor.js":2,"./schema.js":3,"./serializer.js":4,"./css.js":10,"./history.js":5,"./events.js":6,"./doc-utils.js":7,"./transform.js":9,"./path.js":8,"./block-ops.js":11}],
2:[function(require,module,exports){
"use strict";
/**
 * Editor instance — the heart of @smeditor/core.
 *
 * Responsibilities:
 *   - Compile the schema from extensions
 *   - Hold the canonical document (JSON) and selection state
 *   - Run transactions and update the DOM if mounted
 *   - Wire commands and keyboard shortcuts contributed by extensions
 *   - Sync the DOM-side `contenteditable` selection into our internal model
 *   - Emit lifecycle events
 *
 * The mounted view uses native contenteditable. We treat the DOM as a
 * render target and rebuild it from the JSON document on each update.
 * This is simple and correct; performance optimizations (diff-patching)
 * are a future task.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.createEditor = createEditor;
const schema_js_1 = require("./schema.js");
const serializer_js_1 = require("./serializer.js");
const history_js_1 = require("./history.js");
const events_js_1 = require("./events.js");
const doc_utils_js_1 = require("./doc-utils.js");
const path_js_1 = require("./path.js");
const css_js_1 = require("./css.js");
/** Public entry point used by `@smeditor/react` and standalone users. */
function createEditor(options) {
    return new Editor(options);
}
class Editor {
    constructor(options) {
        var _a, _b, _c;
        this.selection = null;
        this.history = new history_js_1.History();
        this.emitter = new events_js_1.EventEmitter();
        this.destroyed = false;
        this.updatingDOM = false;
        /**
         * True while an IME composition is in progress (CJK input, dead keys,
         * accent menus). The browser fires intermediate `input` events during
         * composition whose DOM we must NOT reparse/rebuild — doing so tears
         * down the composition mid-keystroke. We wait for `compositionend`.
         */
        this.composing = false;
        // Bound listeners (kept so we can remove them on destroy)
        this.onDomInput = () => this.handleDOMInput();
        this.onDomBeforeInput = (e) => this.handleBeforeInput(e);
        this.onDomPaste = (e) => this.handlePaste(e);
        this.onDomKeydown = (e) => this.handleKeydown(e);
        this.onDomFocus = () => this.emit("focus", { editor: this });
        this.onDomBlur = () => this.emit("blur", { editor: this });
        this.onSelectionChange = () => this.syncSelectionFromDOM();
        this.onCompositionStart = () => {
            this.composing = true;
        };
        this.onCompositionEnd = () => {
            this.composing = false;
            // Capture the finished composed text in one shot.
            this.handleDOMInput();
        };
        this.options = options;
        this.element = (_a = options.element) !== null && _a !== void 0 ? _a : null;
        // 1. Resolve extensions
        this.extensions = (0, schema_js_1.flattenExtensions)((_b = options.extensions) !== null && _b !== void 0 ? _b : []);
        // 2. Compile schema
        this.schema = (0, schema_js_1.compileSchema)(this.extensions);
        // 3. Normalize initial content into JSON
        this.doc = this.normalizeContent(options.content);
        // 4. Wire commands
        this.commands = this.buildCommandMap();
        // 5. Lifecycle: onCreate for each extension
        for (const ext of this.extensions) {
            (_c = ext.onCreate) === null || _c === void 0 ? void 0 : _c.call(ext, this);
        }
        // 6. Mount to DOM if an element was given
        if (this.element) {
            this.attachToElement();
        }
        // 7. Fire onCreate
        queueMicrotask(() => {
            var _a, _b;
            if (this.destroyed)
                return;
            this.emit("create", { editor: this });
            (_b = (_a = this.options).onCreate) === null || _b === void 0 ? void 0 : _b.call(_a, { editor: this });
            if (options.autofocus)
                this.focus();
        });
    }
    // ==========================================================================
    // Content
    // ==========================================================================
    getJSON() {
        return (0, doc_utils_js_1.cloneDoc)(this.doc);
    }
    getHTML() {
        return (0, serializer_js_1.serializeToHTML)(this.doc, this.schema);
    }
    setContent(content) {
        const next = this.normalizeContent(content);
        this.dispatch({
            doc: next,
            selection: null,
            addToHistory: true,
        });
    }
    normalizeContent(content) {
        if (!content)
            return (0, doc_utils_js_1.emptyDocument)();
        if (typeof content === "string") {
            return (0, serializer_js_1.parseHTML)(content, this.schema);
        }
        if (content.type === "doc" && Array.isArray(content.content)) {
            return (0, doc_utils_js_1.cloneDoc)(content);
        }
        return (0, doc_utils_js_1.emptyDocument)();
    }
    // ==========================================================================
    // Selection
    // ==========================================================================
    getSelection() {
        return this.selection ? (0, doc_utils_js_1.cloneDoc)(this.selection) : null;
    }
    setSelection(selection) {
        this.dispatch({ selection });
    }
    /**
     * The pixel coordinates of a selection point, relative to the
     * editor element's top-left corner. Used to position overlays such
     * as remote collaborator cursors. Returns null if the point can't
     * be resolved or the editor isn't mounted.
     */
    coordsAtPoint(point) {
        var _a;
        if (!this.element)
            return null;
        const deep = (_a = this.options.deepSelection) !== null && _a !== void 0 ? _a : false;
        const loc = domNodeFromPoint(point, this.element, deep);
        if (!loc)
            return null;
        try {
            const range = this.element.ownerDocument.createRange();
            const offset = Math.min(loc.offset, lengthOf(loc.node));
            range.setStart(loc.node, offset);
            range.collapse(true);
            const rect = range.getBoundingClientRect();
            const host = this.element.getBoundingClientRect();
            return {
                top: rect.top - host.top,
                left: rect.left - host.left,
                height: rect.height || 16,
            };
        }
        catch {
            return null;
        }
    }
    // ==========================================================================
    // Transactions
    // ==========================================================================
    dispatch(tr) {
        var _a, _b, _c, _d, _e, _f, _g, _h, _j;
        if (this.destroyed)
            return;
        const prevDoc = this.doc;
        const prevSelection = this.selection;
        let docChanged = false;
        if (tr.doc && !(0, doc_utils_js_1.docsEqual)(tr.doc, this.doc)) {
            if (tr.addToHistory !== false) {
                const coalesce = typeof ((_a = tr.meta) === null || _a === void 0 ? void 0 : _a.coalesce) === "string" ? tr.meta.coalesce : undefined;
                this.history.record({
                    doc: (0, doc_utils_js_1.cloneDoc)(prevDoc),
                    selection: prevSelection ? (0, doc_utils_js_1.cloneDoc)(prevSelection) : null,
                }, coalesce);
            }
            this.doc = tr.doc;
            docChanged = true;
        }
        let selectionChanged = false;
        if (tr.selection !== undefined) {
            let nextSelection = tr.selection;
            if (nextSelection && ((_b = this.options.deepSelection) !== null && _b !== void 0 ? _b : false)) {
                const selectionDoc = (_c = tr.doc) !== null && _c !== void 0 ? _c : this.doc;
                nextSelection = {
                    anchor: (0, path_js_1.normalizeDeepPoint)(selectionDoc, nextSelection.anchor),
                    head: (0, path_js_1.normalizeDeepPoint)(selectionDoc, nextSelection.head),
                };
            }
            if (!selectionsEqual(nextSelection, this.selection)) {
                this.selection = nextSelection;
                selectionChanged = true;
            }
        }
        this.emit("transaction", { editor: this, transaction: tr });
        (_e = (_d = this.options).onTransaction) === null || _e === void 0 ? void 0 : _e.call(_d, { editor: this });
        if (docChanged) {
            this.renderToDOM();
            const html = this.getHTML();
            const json = this.getJSON();
            this.emit("update", { editor: this, html, json });
            (_g = (_f = this.options).onUpdate) === null || _g === void 0 ? void 0 : _g.call(_f, { editor: this, html, json });
        }
        if (selectionChanged) {
            // A selection-only change must still move the live DOM caret.
            // renderToDOM (which restores the caret via applySelectionToDOM)
            // only runs when the document changed, so without this an
            // `editor.setSelection(...)` updated the model but left the
            // browser caret — and the next typed character — in the old spot.
            if (!docChanged && this.element) {
                this.updatingDOM = true;
                try {
                    this.applySelectionToDOM();
                }
                finally {
                    this.updatingDOM = false;
                }
            }
            this.emit("selectionUpdate", {
                editor: this,
                selection: this.selection ? (0, doc_utils_js_1.cloneDoc)(this.selection) : null,
            });
            (_j = (_h = this.options).onSelectionUpdate) === null || _j === void 0 ? void 0 : _j.call(_h, {
                editor: this,
                selection: this.selection ? (0, doc_utils_js_1.cloneDoc)(this.selection) : null,
            });
        }
    }
    // ==========================================================================
    // Commands
    // ==========================================================================
    buildCommandMap() {
        const map = {};
        // Built-in commands always available
        map.setContent = (content) => {
            this.setContent(content);
            return true;
        };
        map.clearContent = () => {
            this.dispatch({
                doc: (0, doc_utils_js_1.emptyDocument)(),
                selection: null,
                addToHistory: true,
            });
            return true;
        };
        map.focus = () => {
            this.focus();
            return true;
        };
        map.blur = () => {
            this.blur();
            return true;
        };
        map.undo = () => {
            const entry = this.history.undo({
                doc: (0, doc_utils_js_1.cloneDoc)(this.doc),
                selection: this.selection ? (0, doc_utils_js_1.cloneDoc)(this.selection) : null,
            });
            if (!entry)
                return false;
            this.dispatch({
                doc: entry.doc,
                selection: entry.selection,
                addToHistory: false,
            });
            return true;
        };
        map.redo = () => {
            const entry = this.history.redo({
                doc: (0, doc_utils_js_1.cloneDoc)(this.doc),
                selection: this.selection ? (0, doc_utils_js_1.cloneDoc)(this.selection) : null,
            });
            if (!entry)
                return false;
            this.dispatch({
                doc: entry.doc,
                selection: entry.selection,
                addToHistory: false,
            });
            return true;
        };
        // Extension-provided commands
        for (const ext of this.extensions) {
            if (!ext.commands)
                continue;
            for (const [name, factory] of Object.entries(ext.commands)) {
                if (map[name]) {
                    // Earlier registration wins to keep core commands stable.
                    continue;
                }
                map[name] = (...args) => {
                    const cmd = factory(...args);
                    if (typeof cmd !== "function")
                        return false;
                    return cmd(this);
                };
            }
        }
        return map;
    }
    isActive(name, attrs) {
        var _a, _b;
        // Mark active check
        if (this.schema.marks[name]) {
            return (0, doc_utils_js_1.selectionHasMark)(this.doc, this.selection, name, attrs);
        }
        // Block-type check. With the deep selection model the anchor path
        // reaches down into tables / lists / blockquotes, so we walk every
        // node along that path: `isActive("table")` is true when the caret
        // sits in any cell, and `isActive("paragraph")` is true for the
        // leaf the caret is actually in. Indexing `path[0]` only ever saw
        // the top level and reported the wrong type inside containers.
        if (this.schema.nodes[name]) {
            const sel = this.selection;
            if (!sel)
                return false;
            let node = this.doc;
            const chain = [];
            for (const idx of sel.anchor.path) {
                const child = (_a = node.content) === null || _a === void 0 ? void 0 : _a[idx];
                if (!child)
                    break;
                chain.push(child);
                node = child;
            }
            for (const block of chain) {
                if (block.type !== name)
                    continue;
                if (!attrs)
                    return true;
                let allMatch = true;
                for (const [k, v] of Object.entries(attrs)) {
                    if (((_b = block.attrs) === null || _b === void 0 ? void 0 : _b[k]) !== v) {
                        allMatch = false;
                        break;
                    }
                }
                if (allMatch)
                    return true;
            }
            return false;
        }
        return false;
    }
    // ==========================================================================
    // DOM mount
    // ==========================================================================
    /**
     * Public mount. Useful in framework integrations where the host
     * element only becomes available inside an effect / mounted hook.
     * Safe to call only once; subsequent calls are ignored.
     */
    mount(element) {
        if (this.element || this.destroyed)
            return;
        this.element = element;
        this.attachToElement();
    }
    attachToElement() {
        const el = this.element;
        el.setAttribute("contenteditable", String(this.options.editable !== false));
        el.classList.add("smeditor-editor");
        el.addEventListener("input", this.onDomInput);
        el.addEventListener("beforeinput", this.onDomBeforeInput);
        el.addEventListener("paste", this.onDomPaste);
        el.addEventListener("keydown", this.onDomKeydown);
        el.addEventListener("focus", this.onDomFocus);
        el.addEventListener("blur", this.onDomBlur);
        el.addEventListener("compositionstart", this.onCompositionStart);
        el.addEventListener("compositionend", this.onCompositionEnd);
        if (typeof document !== "undefined") {
            document.addEventListener("selectionchange", this.onSelectionChange);
        }
        this.renderToDOM();
    }
    focus() {
        var _a;
        if (this.destroyed)
            return;
        (_a = this.element) === null || _a === void 0 ? void 0 : _a.focus();
    }
    blur() {
        var _a;
        if (this.destroyed)
            return;
        (_a = this.element) === null || _a === void 0 ? void 0 : _a.blur();
    }
    isEditable() {
        var _a;
        const attr = (_a = this.element) === null || _a === void 0 ? void 0 : _a.getAttribute("contenteditable");
        if (attr === "false")
            return false;
        if (attr === "true")
            return true;
        return this.options.editable !== false;
    }
    canUndo() {
        return this.history.canUndo();
    }
    canRedo() {
        return this.history.canRedo();
    }
    /**
     * Render the document to the contenteditable element.
     * Caret position is preserved by snapshotting the DOM selection
     * before the rewrite and restoring it after (best-effort).
     */
    renderToDOM() {
        if (!this.element)
            return;
        this.updatingDOM = true;
        try {
            const html = this.getHTML();
            // Don't clobber if identical — avoids losing caret on no-op updates.
            if (this.element.innerHTML !== html) {
                this.element.innerHTML = html || "<p><br/></p>";
            }
            // Restore caret from internal selection model
            this.applySelectionToDOM();
        }
        finally {
            this.updatingDOM = false;
        }
    }
    /**
     * Read the contenteditable HTML back into our JSON model after user input.
     * This is the round-trip that makes typing work.
     */
    /**
     * Intercept input before it reaches the DOM. Typed text and
     * deletes are offered to extension hooks (track changes uses these
     * to mark edits); if a hook handles the event, the native input is
     * suppressed.
     */
    handleBeforeInput(e) {
        var _a, _b;
        if (this.updatingDOM || !this.element)
            return;
        // Let the browser drive IME composition; we reconcile on compositionend.
        if (this.composing || e.isComposing)
            return;
        const type = e.inputType;
        if (type === "insertText" && typeof e.data === "string") {
            for (const ext of this.extensions) {
                if ((_a = ext.handleTextInput) === null || _a === void 0 ? void 0 : _a.call(ext, this, e.data)) {
                    e.preventDefault();
                    return;
                }
            }
        }
        else if (type === "deleteContentBackward" ||
            type === "deleteContentForward") {
            const direction = type === "deleteContentBackward" ? "backward" : "forward";
            for (const ext of this.extensions) {
                if ((_b = ext.handleDeleteContent) === null || _b === void 0 ? void 0 : _b.call(ext, this, direction)) {
                    e.preventDefault();
                    return;
                }
            }
        }
    }
    handleDOMInput() {
        if (this.updatingDOM || !this.element)
            return;
        // Mid-composition DOM is incomplete; reconcile on compositionend.
        if (this.composing)
            return;
        const html = this.element.innerHTML;
        const nextDoc = (0, serializer_js_1.parseHTML)(html, this.schema);
        if ((0, doc_utils_js_1.docsEqual)(nextDoc, this.doc))
            return;
        // Read the DOM selection while it's still fresh
        const sel = this.readSelectionFromDOM();
        this.dispatch({
            doc: nextDoc,
            selection: sel,
            addToHistory: true,
            // Coalesce a run of typing in one block into a single undo step.
            meta: { coalesce: "input:" + (sel ? sel.anchor.path.join(",") : "") },
        });
        // After the document reflects what the user typed, give input
        // rules a chance to fire (e.g. "## " → heading). Rules run against
        // the freshly-updated doc; a rule that matches dispatches its own
        // follow-up transaction.
        this.applyInputRules();
    }
    /**
     * Test every extension's input rules against the text immediately
     * before the caret in the current block. The first rule whose regex
     * matches gets to run its handler; if the handler returns true we
     * stop (one rule per input event).
     *
     * Input rules are how typed-text shortcuts work — markdown patterns
     * (`# `, `> `, `1. `) and similar. The regex is matched against the
     * block text up to the caret offset, so `match[0]` is the typed
     * prefix the handler will usually want to strip.
     */
    applyInputRules() {
        if (this.destroyed || !this.selection)
            return;
        const block = (0, path_js_1.nodeAt)(this.doc, this.selection.anchor.path);
        if (!block)
            return;
        const offset = this.selection.anchor.offset;
        const fullText = (0, doc_utils_js_1.getBlockText)(block);
        // Guard: offset can momentarily exceed text length during fast
        // typing — clamp so slice() behaves.
        const textBefore = fullText.slice(0, Math.max(0, Math.min(offset, fullText.length)));
        if (!textBefore)
            return;
        for (const ext of this.extensions) {
            if (!ext.inputRules)
                continue;
            for (const rule of ext.inputRules) {
                // Reset lastIndex so a stateful (/g) regex doesn't skip matches.
                if (rule.match.global || rule.match.sticky)
                    rule.match.lastIndex = 0;
                const m = rule.match.exec(textBefore);
                if (!m)
                    continue;
                const handled = rule.handler(this, m);
                if (handled)
                    return;
            }
        }
    }
    /**
     * Intercept paste, sanitize the pasted HTML, and insert the cleaned
     * version. This is the §12 "paste cleanup" — pasting from Word /
     * Google Docs otherwise drags in `mso-*` styles, empty `<span>`s,
     * `<o:p>` tags and other noise.
     *
     * Plain-text pastes (no `text/html` payload) are left to the browser.
     */
    handlePaste(e) {
        if (this.updatingDOM || !this.element)
            return;
        if (this.element.getAttribute("contenteditable") === "false")
            return;
        const data = e.clipboardData;
        if (!data)
            return;
        const html = data.getData("text/html");
        if (!html)
            return; // plain text — browser default is fine
        e.preventDefault();
        let cleaned = sanitizePastedHTML(html);
        // Let extensions apply source-specific cleanup on top of the
        // built-in sanitizer. A faulty transformer is skipped, not fatal.
        for (const ext of this.extensions) {
            if (typeof ext.transformPastedHTML === "function") {
                try {
                    cleaned = ext.transformPastedHTML(cleaned);
                }
                catch {
                    /* ignore — keep the previous HTML */
                }
            }
        }
        if (typeof document === "undefined")
            return;
        // execCommand is deprecated but still the most reliable way to
        // insert HTML at the caret across browsers. The resulting `input`
        // event flows through handleDOMInput like any other edit.
        try {
            document.execCommand("insertHTML", false, cleaned);
        }
        catch {
            // If insertHTML isn't available, fall back to plain text so the
            // paste at least lands somewhere.
            const text = data.getData("text/plain");
            if (text)
                document.execCommand("insertText", false, text);
        }
        // Some browsers don't emit `input` for execCommand — nudge the
        // round-trip so the JSON model stays in sync.
        queueMicrotask(() => {
            if (!this.destroyed)
                this.handleDOMInput();
        });
    }
    handleKeydown(e) {
        var _a, _b, _c, _d;
        // Convert event into a "Mod-key" string
        const combo = comboFromEvent(e);
        for (const ext of this.extensions) {
            if (!ext.keyboardShortcuts)
                continue;
            const handler = ext.keyboardShortcuts[combo];
            if (handler) {
                const handled = handler(this);
                if (handled) {
                    e.preventDefault();
                    return;
                }
            }
        }
        // Built-in shortcuts: Ctrl/Cmd-Z, Ctrl/Cmd-Shift-Z / Ctrl-Y
        if (combo === "Mod-z") {
            if ((_b = (_a = this.commands).undo) === null || _b === void 0 ? void 0 : _b.call(_a))
                e.preventDefault();
        }
        else if (combo === "Mod-Shift-z" || combo === "Mod-y") {
            if ((_d = (_c = this.commands).redo) === null || _d === void 0 ? void 0 : _d.call(_c))
                e.preventDefault();
        }
    }
    // ==========================================================================
    // Selection sync
    // ==========================================================================
    syncSelectionFromDOM() {
        var _a, _b;
        if (this.updatingDOM || !this.element)
            return;
        const domSel = selectionForElement(this.element);
        if (!domSel)
            return;
        if (!this.element.contains(domSel.anchorNode))
            return;
        const next = this.readSelectionFromDOM();
        if (!next)
            return;
        if (selectionsEqual(next, this.selection))
            return;
        this.selection = next;
        this.emit("selectionUpdate", {
            editor: this,
            selection: (0, doc_utils_js_1.cloneDoc)(next),
        });
        (_b = (_a = this.options).onSelectionUpdate) === null || _b === void 0 ? void 0 : _b.call(_a, {
            editor: this,
            selection: (0, doc_utils_js_1.cloneDoc)(next),
        });
    }
    readSelectionFromDOM() {
        var _a;
        if (!this.element)
            return null;
        const sel = selectionForElement(this.element);
        if (!sel || sel.rangeCount === 0)
            return null;
        const deep = (_a = this.options.deepSelection) !== null && _a !== void 0 ? _a : false;
        const anchor = pointFromDOM(sel.anchorNode, sel.anchorOffset, this.element, deep);
        const head = pointFromDOM(sel.focusNode, sel.focusOffset, this.element, deep);
        if (!anchor || !head)
            return null;
        // In deep mode, a point that resolved to a container (a click on
        // table structure rather than text) is pulled down to a leaf.
        if (deep) {
            const doc = this.getJSON();
            return {
                anchor: (0, path_js_1.normalizeDeepPoint)(doc, anchor),
                head: (0, path_js_1.normalizeDeepPoint)(doc, head),
            };
        }
        return { anchor, head };
    }
    applySelectionToDOM() {
        var _a;
        if (!this.selection || !this.element) {
            return;
        }
        const sel = selectionForElement(this.element);
        if (!sel)
            return;
        const deep = (_a = this.options.deepSelection) !== null && _a !== void 0 ? _a : false;
        const anchorNode = domNodeFromPoint(this.selection.anchor, this.element, deep);
        const headNode = domNodeFromPoint(this.selection.head, this.element, deep);
        if (!anchorNode || !headNode)
            return;
        try {
            const range = this.element.ownerDocument.createRange();
            range.setStart(anchorNode.node, Math.min(anchorNode.offset, lengthOf(anchorNode.node)));
            range.setEnd(headNode.node, Math.min(headNode.offset, lengthOf(headNode.node)));
            sel.removeAllRanges();
            sel.addRange(range);
        }
        catch {
            // Selection restore is best-effort.
        }
    }
    // ==========================================================================
    // Events
    // ==========================================================================
    on(event, handler) {
        return this.emitter.on(event, handler);
    }
    emit(event, payload) {
        this.emitter.emit(event, payload);
    }
    // ==========================================================================
    // Destroy
    // ==========================================================================
    destroy() {
        var _a, _b, _c;
        if (this.destroyed)
            return;
        this.destroyed = true;
        if (this.element) {
            this.element.removeEventListener("input", this.onDomInput);
            this.element.removeEventListener("beforeinput", this.onDomBeforeInput);
            this.element.removeEventListener("paste", this.onDomPaste);
            this.element.removeEventListener("keydown", this.onDomKeydown);
            this.element.removeEventListener("focus", this.onDomFocus);
            this.element.removeEventListener("blur", this.onDomBlur);
            this.element.removeEventListener("compositionstart", this.onCompositionStart);
            this.element.removeEventListener("compositionend", this.onCompositionEnd);
        }
        if (typeof document !== "undefined") {
            document.removeEventListener("selectionchange", this.onSelectionChange);
        }
        for (const ext of this.extensions) {
            (_a = ext.onDestroy) === null || _a === void 0 ? void 0 : _a.call(ext, this);
        }
        this.emit("destroy", { editor: this });
        (_c = (_b = this.options).onDestroy) === null || _c === void 0 ? void 0 : _c.call(_b, { editor: this });
        this.emitter.clear();
    }
}
// ============================================================================
// Helpers (module-private)
// ============================================================================
/**
 * Return the Selection object that actually owns the editor caret.
 *
 * `document.getSelection()` is not sufficient for editors mounted inside an
 * open ShadowRoot: Chromium exposes a composed selection on the document whose
 * anchor can be the shadow host instead of the text node inside the editor.
 * Reading that selection produces a null/wrong editor position; the next DOM
 * render then loses the caret (most visibly when typing spaces).
 *
 * Both Document and Chromium's ShadowRoot expose getSelection(). Prefer the
 * editor's root and fall back to its owner document for browsers that do not
 * expose ShadowRoot#getSelection.
 */
function selectionForElement(element) {
    var _a, _b, _c, _d;
    const root = (_a = element.getRootNode) === null || _a === void 0 ? void 0 : _a.call(element);
    const rootedGetSelection = root === null || root === void 0 ? void 0 : root.getSelection;
    if (root && typeof rootedGetSelection === "function") {
        const selection = rootedGetSelection.call(root);
        if (selection)
            return selection;
    }
    return (_d = (_c = (_b = element.ownerDocument) === null || _b === void 0 ? void 0 : _b.getSelection) === null || _c === void 0 ? void 0 : _c.call(_b)) !== null && _d !== void 0 ? _d : null;
}
/**
 * Strip noise and unsafe elements from pasted HTML.
 *
 * Removes script/style/meta/link/head/title and Office namespace tags
 * (`<o:p>`, `<xml>`, …), drops HTML comments, and prunes attributes
 * down to a safe whitelist. Inline `style` is reduced to just the
 * declarations SMEditor understands — text-align, color,
 * background-color — so Word's `mso-*` and font-family clutter is
 * dropped while genuine formatting survives.
 */
function sanitizePastedHTML(html) {
    var _a;
    if (typeof DOMParser === "undefined") {
        return html.replace(/<[^>]*>/g, "");
    }
    const parsed = new DOMParser().parseFromString(html, "text/html");
    const body = parsed.body;
    if (!body)
        return "";
    // Drop dangerous / useless elements outright.
    body
        .querySelectorAll("script,style,meta,link,title,head,o\\:p,v\\:shape,w\\:sdt,xml,iframe,object,embed,svg,math")
        .forEach((n) => n.remove());
    // Drop HTML comments (Word emits conditional comments everywhere).
    const walker = parsed.createTreeWalker(body, NodeFilter.SHOW_COMMENT);
    const comments = [];
    let c;
    while ((c = walker.nextNode()))
        comments.push(c);
    comments.forEach((n) => { var _a; return (_a = n.parentNode) === null || _a === void 0 ? void 0 : _a.removeChild(n); });
    // Word renders list bullets as <span style="mso-list:Ignore">•</span>.
    // Remove them before the style attribute is pruned away, so the
    // bullet glyph doesn't bleed into the text.
    body.querySelectorAll("span").forEach((span) => {
        var _a;
        const style = (_a = span.getAttribute("style")) !== null && _a !== void 0 ? _a : "";
        if (/mso-list\s*:\s*ignore/i.test(style))
            span.remove();
    });
    body.querySelectorAll("*").forEach((el) => sanitizePastedElement(el));
    // Unwrap spans left with no attributes — Word nests these many deep.
    // Repeat until stable, since unwrapping exposes more empty spans.
    for (let pass = 0; pass < 5; pass++) {
        const empties = Array.from(body.querySelectorAll("span")).filter((s) => s.attributes.length === 0);
        if (empties.length === 0)
            break;
        for (const span of empties) {
            while (span.firstChild) {
                (_a = span.parentNode) === null || _a === void 0 ? void 0 : _a.insertBefore(span.firstChild, span);
            }
            span.remove();
        }
    }
    // Remove block elements left visually empty (Word emits stray <p>s).
    body.querySelectorAll("p,div").forEach((el) => {
        var _a;
        const text = ((_a = el.textContent) !== null && _a !== void 0 ? _a : "").replace(/\u00a0/g, " ").trim();
        if (!text && el.children.length === 0)
            el.remove();
    });
    return body.innerHTML;
}
function sanitizePastedElement(el) {
    for (const attr of Array.from(el.attributes)) {
        const name = attr.name.toLowerCase();
        const value = attr.value;
        if (name === "style") {
            const style = sanitizePastedStyle(value);
            if (style)
                el.setAttribute("style", style);
            else
                el.removeAttribute(attr.name);
        }
        else if (name === "href") {
            const href = (0, css_js_1.sanitizeURL)(value, {
                protocols: ["http", "https", "mailto", "tel"],
                allowBareEmail: true,
            });
            if (href)
                el.setAttribute("href", href);
            else
                el.removeAttribute(attr.name);
        }
        else if (name === "src") {
            const src = (0, css_js_1.sanitizeURL)(value, {
                protocols: ["http", "https"],
                allowFragments: false,
                allowDataUrls: true,
                dataUrlPattern: /^data:image\/(?:png|jpe?g|gif|webp|avif|bmp);base64,[a-z0-9+/=\s]+$/i,
            });
            if (src)
                el.setAttribute("src", src);
            else
                el.removeAttribute(attr.name);
        }
        else if (name === "target") {
            const target = (0, css_js_1.sanitizeLinkTarget)(value);
            if (target)
                el.setAttribute("target", target);
            else
                el.removeAttribute(attr.name);
        }
        else if (name === "rel") {
            el.removeAttribute(attr.name);
        }
        else if (name === "alt" || name === "title") {
            el.setAttribute(name, value.replace(/[\u0000-\u001f\u007f]/g, ""));
        }
        else if (name === "colspan" || name === "rowspan") {
            const span = sanitizeTableSpan(value);
            if (span)
                el.setAttribute(name, span);
            else
                el.removeAttribute(attr.name);
        }
        else {
            el.removeAttribute(attr.name);
        }
    }
    if (el.tagName.toLowerCase() === "a") {
        const href = el.getAttribute("href");
        if (!href) {
            el.removeAttribute("target");
        }
        else {
            const attrs = { href };
            const target = (0, css_js_1.sanitizeLinkTarget)(el.getAttribute("target"));
            if (target)
                attrs.target = target;
            const hardened = (0, css_js_1.hardenLinkAttrs)(attrs);
            if (hardened.rel)
                el.setAttribute("rel", hardened.rel);
            else
                el.removeAttribute("rel");
        }
    }
}
function sanitizePastedStyle(style) {
    const kept = [];
    for (const decl of style.split(";")) {
        const match = /^\s*([a-z-]+)\s*:\s*(.+?)\s*$/i.exec(decl);
        if (!match)
            continue;
        const name = match[1].toLowerCase();
        const value = sanitizePastedStyleValue(name, match[2]);
        if (value)
            kept.push(`${name}: ${value}`);
    }
    return kept.length > 0 ? kept.join("; ") : null;
}
function sanitizePastedStyleValue(name, value) {
    if (name === "text-align")
        return sanitizeTextAlign(value);
    if (name === "color")
        return (0, css_js_1.sanitizeCSSColor)(value);
    if (name === "background-color" || name === "background") {
        return (0, css_js_1.sanitizeCSSColor)(value);
    }
    if (name === "font-family")
        return (0, css_js_1.sanitizeCSSFontFamily)(value);
    if (name === "font-size")
        return (0, css_js_1.sanitizeCSSFontSize)(value);
    if (name === "line-height")
        return (0, css_js_1.sanitizeCSSLineHeight)(value);
    if (name === "-webkit-text-stroke" || name === "text-stroke") {
        return sanitizeTextStroke(value);
    }
    if (name === "-webkit-text-stroke-color" ||
        name === "text-stroke-color") {
        return (0, css_js_1.sanitizeCSSColor)(value);
    }
    if (name === "-webkit-text-stroke-width" ||
        name === "text-stroke-width") {
        return (0, css_js_1.sanitizeCSSTextStrokeWidth)(value);
    }
    return null;
}
function sanitizeTextAlign(value) {
    const align = value.trim().toLowerCase();
    return ["left", "center", "right", "justify"].includes(align) ? align : null;
}
function sanitizeTextStroke(value) {
    const match = /^\s*([^\s]+)\s+(.+)\s*$/.exec(value.trim());
    if (!match)
        return null;
    const width = (0, css_js_1.sanitizeCSSTextStrokeWidth)(match[1]);
    const color = (0, css_js_1.sanitizeCSSColor)(match[2]);
    return width && color ? `${width} ${color}` : null;
}
function sanitizeTableSpan(value) {
    const span = Math.round(Number(value));
    return Number.isFinite(span) && span >= 1 && span <= 100 ? String(span) : null;
}
function comboFromEvent(e) {
    const parts = [];
    // "Mod" is Cmd on Mac, Ctrl elsewhere — for shortcuts written as "Mod-b"
    if (e.metaKey || e.ctrlKey)
        parts.push("Mod");
    if (e.shiftKey)
        parts.push("Shift");
    if (e.altKey)
        parts.push("Alt");
    let key = e.key;
    // For the digit row, browsers report the SHIFTED symbol in `e.key`
    // when Shift is held (Shift+7 → "&", Shift+8 → "*", Shift+9 → "("),
    // so a shortcut registered as "Mod-Shift-7" would never match. Derive
    // the digit from the layout-independent physical code instead.
    if (/^Digit[0-9]$/.test(e.code))
        key = e.code.slice(5);
    else if (key.length === 1)
        key = key.toLowerCase();
    parts.push(key);
    return parts.join("-");
}
function selectionsEqual(a, b) {
    if (a === b)
        return true;
    if (!a || !b)
        return false;
    return (pointsEqual(a.anchor, b.anchor) && pointsEqual(a.head, b.head));
}
function pointsEqual(a, b) {
    if (a.offset !== b.offset)
        return false;
    if (a.path.length !== b.path.length)
        return false;
    return a.path.every((v, i) => v === b.path[i]);
}
/**
 * Translate a DOM node + offset into our internal SelectionPoint.
 * The path is computed as the chain of childNode indices from the
 * editor root to the parent block; the offset is the character offset
 * within the text content of that block.
 */
/**
 * Tags that correspond to a block-level node in the document tree.
 * The DOM mirrors the JSON tree for these (table → tr → td → p, with
 * no structural wrappers), so walking block elements reconstructs the
 * node path. Mark wrappers (strong, em, a, …) are deliberately
 * absent — they don't add a level to the path.
 */
const BLOCK_TAGS = new Set([
    "p", "h1", "h2", "h3", "h4", "h5", "h6",
    "blockquote", "pre", "hr",
    "ul", "ol", "li",
    "table", "tr", "td", "th",
    "figure",
]);
function isBlockElement(n) {
    return (n.nodeType === 1 &&
        BLOCK_TAGS.has(n.tagName.toLowerCase()));
}
/**
 * Table section wrappers. The browser auto-inserts a `<tbody>` around
 * `<tr>` rows when innerHTML is assigned, but the JSON model has no
 * such level (table → row → cell). They are transparent: not a path
 * level themselves, but their `<tr>` children are.
 */
const SECTION_TAGS = new Set(["tbody", "thead", "tfoot"]);
/**
 * Block-level element children of `container`, descending through
 * transparent table section wrappers so the DOM table→row→cell→block
 * chain keeps matching the JSON path. Used by both the read
 * (pointFromDOM) and write (domNodeFromPoint) deep-selection paths so
 * they stay symmetric — otherwise the caret restored after a table
 * edit landed on the `<table>` element instead of the cell.
 */
function blockChildrenOf(container) {
    const out = [];
    for (const c of Array.from(container.childNodes)) {
        if (c.nodeType !== 1)
            continue;
        const tag = c.tagName.toLowerCase();
        if (SECTION_TAGS.has(tag))
            out.push(...blockChildrenOf(c));
        else if (isBlockElement(c))
            out.push(c);
    }
    return out;
}
/** Character offset of (node, offset) within `container`. */
function textOffsetWithin(container, node, offset) {
    let charOffset = 0;
    const walk = (n) => {
        var _a, _b;
        if (n === node) {
            charOffset += offset;
            return true;
        }
        if (n.nodeType === 3) {
            charOffset += (_b = (_a = n.textContent) === null || _a === void 0 ? void 0 : _a.length) !== null && _b !== void 0 ? _b : 0;
        }
        else {
            for (const c of Array.from(n.childNodes)) {
                if (walk(c))
                    return true;
            }
        }
        return false;
    };
    walk(container);
    return charOffset;
}
function pointFromDOM(node, offset, root, deep = false) {
    if (!node)
        return null;
    if (deep) {
        // Collect the chain of block-element ancestors, root → leaf.
        const chain = [];
        let n = node;
        while (n && n !== root) {
            if (isBlockElement(n))
                chain.unshift(n);
            n = n.parentNode;
        }
        if (chain.length === 0)
            return null;
        // Each path entry is the element's index among its block siblings.
        const path = [];
        for (const el of chain) {
            const parent = el.parentNode;
            if (!parent)
                return null;
            // blockChildrenOf flattens table section wrappers, so a <tr>
            // whose DOM parent is a <tbody> still indexes correctly within
            // its table.
            const blockSiblings = blockChildrenOf(parent);
            const idx = blockSiblings.indexOf(el);
            if (idx < 0)
                return null;
            path.push(idx);
        }
        // Offset is measured within the leaf block (deepest in the chain).
        const leaf = chain[chain.length - 1];
        return { path, offset: textOffsetWithin(leaf, node, offset) };
    }
    // Top-level mode (default): walk up to the block child of root.
    let cur = node;
    let block = null;
    while (cur && cur !== root) {
        if (cur.parentNode === root && cur.nodeType === 1) {
            block = cur;
            break;
        }
        cur = cur.parentNode;
    }
    if (!block)
        return null;
    const blockIndex = Array.from(root.children).indexOf(block);
    if (blockIndex < 0)
        return null;
    return { path: [blockIndex], offset: textOffsetWithin(block, node, offset) };
}
/**
 * Inverse of pointFromDOM: take a SelectionPoint and find the DOM
 * text-node + offset that corresponds to it.
 */
function domNodeFromPoint(point, root, deep = false) {
    var _a;
    // Resolve the leaf block element the path addresses.
    let block = null;
    if (deep) {
        let container = root;
        for (const idx of point.path) {
            const blockChildren = blockChildrenOf(container);
            const next = blockChildren[idx];
            if (!next) {
                // Path ran off the tree — settle on the deepest resolved node.
                return { node: container, offset: 0 };
            }
            container = next;
        }
        block = container;
    }
    else {
        const blockIndex = point.path[0];
        if (blockIndex == null)
            return null;
        block = (_a = root.children[blockIndex]) !== null && _a !== void 0 ? _a : null;
    }
    if (!block)
        return null;
    let remaining = point.offset;
    let result = null;
    const walk = (n) => {
        var _a, _b;
        if (result)
            return;
        if (n.nodeType === 3) {
            const len = (_b = (_a = n.textContent) === null || _a === void 0 ? void 0 : _a.length) !== null && _b !== void 0 ? _b : 0;
            if (remaining <= len) {
                result = { node: n, offset: remaining };
            }
            else {
                remaining -= len;
            }
        }
        else {
            for (const c of Array.from(n.childNodes)) {
                walk(c);
                if (result)
                    return;
            }
        }
    };
    walk(block);
    if (!result) {
        // Fall back to the block itself at offset 0
        return { node: block, offset: 0 };
    }
    return result;
}
function lengthOf(n) {
    var _a, _b;
    if (n.nodeType === 3)
        return (_b = (_a = n.textContent) === null || _a === void 0 ? void 0 : _a.length) !== null && _b !== void 0 ? _b : 0;
    return n.childNodes.length;
}

},{"./schema.js":3,"./serializer.js":4,"./history.js":5,"./events.js":6,"./doc-utils.js":7,"./path.js":8,"./css.js":10}],
3:[function(require,module,exports){
"use strict";
/**
 * Schema compilation.
 *
 * Core registers two built-in node types that every document needs:
 *   - "doc"  — the root container of block nodes
 *   - "text" — the inline text leaf
 *
 * Everything else — paragraph, heading, bold, italic, … — comes from
 * extensions. This file walks the extension list, validates names, and
 * produces a CompiledSchema for fast lookup by name.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.flattenExtensions = flattenExtensions;
exports.compileSchema = compileSchema;
const DOC_NODE = {
    name: "doc",
    group: "block",
    content: "block+",
    toDOM: () => ["div", { class: "smeditor-doc" }, 0],
};
const TEXT_NODE = {
    name: "text",
    group: "inline",
    content: "none",
    // text rendering is special-cased by the serializer
    toDOM: () => ["span"],
};
/** Flatten extensions and bundles into a single Extension[] list. */
function flattenExtensions(list) {
    const out = [];
    for (const item of list) {
        if (isBundle(item)) {
            out.push(...item.extensions);
        }
        else {
            out.push(item);
        }
    }
    return out;
}
function isBundle(x) {
    return (typeof x === "object" &&
        x !== null &&
        Array.isArray(x.extensions));
}
/** Build a CompiledSchema from a flat extension list. */
function compileSchema(extensions) {
    const nodes = {
        doc: DOC_NODE,
        text: TEXT_NODE,
    };
    const marks = {};
    for (const ext of extensions) {
        if (ext.nodes) {
            for (const n of ext.nodes) {
                if (n.name === "doc" || n.name === "text") {
                    throw new Error(`[smeditor] extension "${ext.name}" tried to redefine reserved node "${n.name}"`);
                }
                if (nodes[n.name]) {
                    throw new Error(`[smeditor] duplicate node "${n.name}" registered by extension "${ext.name}"`);
                }
                nodes[n.name] = n;
            }
        }
        if (ext.marks) {
            for (const m of ext.marks) {
                if (marks[m.name]) {
                    throw new Error(`[smeditor] duplicate mark "${m.name}" registered by extension "${ext.name}"`);
                }
                marks[m.name] = m;
            }
        }
    }
    // Default paragraph fallback: if no paragraph node is registered, the
    // editor would have nowhere to insert plain text. We register a minimal
    // one so headless usage still works. Real apps should include
    // extension-paragraph (or StarterKit).
    if (!nodes.paragraph) {
        nodes.paragraph = {
            name: "paragraph",
            group: "block",
            content: "inline*",
            toDOM: () => ["p", 0],
            parseDOM: [{ tag: "p" }],
        };
    }
    return { nodes, marks };
}

},{}],
4:[function(require,module,exports){
"use strict";
/**
 * JSON ↔ HTML serialization.
 *
 * The document model is canonical JSON. HTML is just one transport format.
 * Two functions live here:
 *
 *   serializeToHTML(doc, schema) → string
 *   parseHTML(html, schema)      → DocumentJSON
 *
 * Both use the schema's `toDOM` and `parseDOM` declarations on nodes/marks.
 *
 * The serializer is environment-agnostic: it builds an HTML string without
 * touching the live DOM. The parser uses DOMParser when available (browser)
 * and falls back to a minimal regex-tokenizer when not (SSR / tests).
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.serializeToHTML = serializeToHTML;
exports.escapeHTML = escapeHTML;
exports.parseHTML = parseHTML;
// ============================================================================
// JSON → HTML
// ============================================================================
function serializeToHTML(doc, schema) {
    if (!doc || doc.type !== "doc" || !doc.content)
        return "";
    return doc.content.map((node) => renderNode(node, schema)).join("");
}
function renderNode(node, schema) {
    var _a, _b, _c;
    if (node.type === "text") {
        let html = escapeHTML((_a = node.text) !== null && _a !== void 0 ? _a : "");
        // Wrap text in mark elements, innermost first.
        if (node.marks && node.marks.length > 0) {
            for (const mark of node.marks) {
                const spec = schema.marks[mark.type];
                if (!(spec === null || spec === void 0 ? void 0 : spec.toDOM))
                    continue;
                html = wrapWithSpec(spec.toDOM(mark), html);
            }
        }
        return html;
    }
    const spec = schema.nodes[node.type];
    if (!(spec === null || spec === void 0 ? void 0 : spec.toDOM)) {
        // Unknown node — render children if any.
        return ((_b = node.content) !== null && _b !== void 0 ? _b : [])
            .map((c) => renderNode(c, schema))
            .join("");
    }
    let inner = ((_c = node.content) !== null && _c !== void 0 ? _c : [])
        .map((c) => renderNode(c, schema))
        .join("");
    // An empty leaf block (a blank paragraph, an empty table cell) must
    // still render with a <br> inside it — otherwise the element
    // collapses to zero height in contenteditable and the caret can't
    // be placed there. The parser drops this filler again on the way
    // back, so it never pollutes the JSON model.
    if (inner === "" &&
        !spec.atom &&
        typeof spec.content === "string" &&
        spec.content.includes("inline")) {
        inner = "<br>";
    }
    return wrapWithSpec(spec.toDOM(node), inner);
}
function wrapWithSpec(spec, inner) {
    const arr = spec;
    const tag = arr[0];
    // An attrs object (not 0, not an array) may follow the tag.
    let childStart = 1;
    let attrs = null;
    if (typeof arr[1] === "object" &&
        arr[1] !== null &&
        !Array.isArray(arr[1])) {
        attrs = arr[1];
        childStart = 2;
    }
    const attrStr = attrs ? renderAttrs(attrs) : "";
    if (isVoidTag(tag)) {
        // No trailing slash — `<br>`, `<hr>`, `<img …>` is exactly what a
        // browser's `innerHTML` reports back, so the editor's "did the
        // HTML change?" check stays accurate and the caret isn't lost on
        // no-op re-renders.
        return `<${tag}${attrStr}>`;
    }
    // Remaining entries are children: 0 is the content hole, an array is
    // a nested spec, a string is literal text.
    let body = "";
    for (let i = childStart; i < arr.length; i++) {
        const child = arr[i];
        if (child === 0) {
            body += inner;
        }
        else if (Array.isArray(child)) {
            body += wrapWithSpec(child, inner);
        }
        else if (typeof child === "string") {
            body += escapeHTML(child);
        }
    }
    return `<${tag}${attrStr}>${body}</${tag}>`;
}
function renderAttrs(attrs) {
    const parts = [];
    for (const [k, v] of Object.entries(attrs)) {
        if (v == null || v === false)
            continue;
        if (v === true) {
            parts.push(` ${k}`);
        }
        else {
            parts.push(` ${k}="${escapeAttr(String(v))}"`);
        }
    }
    return parts.join("");
}
const VOID_TAGS = new Set([
    "br",
    "hr",
    "img",
    "input",
    "meta",
    "link",
    "source",
]);
function isVoidTag(tag) {
    return VOID_TAGS.has(tag.toLowerCase());
}
function escapeHTML(s) {
    return s
        .replace(/&/g, "&amp;")
        .replace(/</g, "&lt;")
        .replace(/>/g, "&gt;");
}
function escapeAttr(s) {
    return s.replace(/&/g, "&amp;").replace(/"/g, "&quot;");
}
// ============================================================================
// HTML → JSON
// ============================================================================
function parseHTML(html, schema) {
    let root = null;
    if (typeof DOMParser === "undefined") {
        root = parseHTMLFragment(html);
    }
    else {
        const parser = new DOMParser();
        const wrapped = `<div id="__smeditor_root">${html}</div>`;
        const docEl = parser.parseFromString(wrapped, "text/html");
        root = docEl.getElementById("__smeditor_root");
    }
    if (!root)
        return emptyDoc();
    const content = [];
    for (const child of Array.from(root.childNodes)) {
        const parsed = parseDOMNode(child, schema, []);
        if (parsed) {
            if (Array.isArray(parsed))
                content.push(...parsed);
            else
                content.push(parsed);
        }
    }
    return normalizeTopLevel(content, schema);
}
function emptyDoc() {
    return { type: "doc", content: [{ type: "paragraph", content: [] }] };
}
function normalizeTopLevel(content, schema) {
    // Ensure top-level only contains block nodes; wrap stray inlines in paragraph.
    const blocks = [];
    let buffer = [];
    const flush = () => {
        if (buffer.length > 0) {
            blocks.push({ type: "paragraph", content: buffer });
            buffer = [];
        }
    };
    for (const node of content) {
        const spec = schema.nodes[node.type];
        if ((spec === null || spec === void 0 ? void 0 : spec.group) === "inline" || node.type === "text") {
            buffer.push(node);
        }
        else {
            flush();
            blocks.push(node);
        }
    }
    flush();
    if (blocks.length === 0) {
        blocks.push({ type: "paragraph", content: [] });
    }
    return { type: "doc", content: blocks };
}
/**
 * True when a `<br>` has no meaningful content after it among its
 * following siblings — i.e. it's a filler break at the end of a
 * block, not a genuine line break between two runs of text.
 */
const DROPPED_HTML_TAGS = new Set([
    "script",
    "style",
    "meta",
    "link",
    "title",
    "head",
    "xml",
]);
/** Tags that hold block-level content; used to detect formatting
 *  whitespace that sits between block siblings. */
const PARSER_BLOCK_TAGS = new Set([
    "p", "div", "h1", "h2", "h3", "h4", "h5", "h6",
    "ul", "ol", "li", "table", "thead", "tbody", "tfoot", "tr", "td", "th",
    "blockquote", "pre", "figure", "figcaption", "hr",
    "section", "article", "header", "footer", "main", "aside", "nav",
]);
/**
 * True for a whitespace-only text node that sits purely between block
 * siblings (the newlines/indentation of pretty-printed HTML). Such
 * nodes are formatting, not content — keeping them turned every blank
 * line of imported HTML into a spurious empty paragraph. Whitespace
 * inside an inline run (e.g. the space in `a <strong>b</strong>`) keeps
 * an inline/text sibling, so it is correctly preserved.
 */
function isInsignificantWhitespace(node) {
    var _a, _b;
    if (((_a = node.textContent) !== null && _a !== void 0 ? _a : "").trim() !== "")
        return false;
    const parent = node.parentNode;
    if (!parent)
        return false;
    let hasBlockSibling = false;
    for (const sib of Array.from(parent.childNodes)) {
        if (sib === node)
            continue;
        if (sib.nodeType === 3) {
            if (((_b = sib.textContent) !== null && _b !== void 0 ? _b : "").trim() !== "")
                return false;
        }
        else if (sib.nodeType === 1) {
            if (PARSER_BLOCK_TAGS.has(sib.tagName.toLowerCase())) {
                hasBlockSibling = true;
            }
            else {
                return false;
            }
        }
    }
    return hasBlockSibling;
}
function isTrailingBr(br) {
    // A `<br>` is droppable filler only when it carries no meaningful
    // content on *either* side — i.e. it is the empty-leaf placeholder the
    // serializer emits for a blank block. A `<br>` with real content
    // before it (e.g. `<p>a<br></p>`) is a genuine hard_break and must be
    // preserved so it survives the HTML round-trip; it falls through to
    // the hard_break node rule instead of being discarded.
    return !hasMeaningfulSibling(br, "next") && !hasMeaningfulSibling(br, "prev");
}
function hasMeaningfulSibling(node, dir) {
    var _a;
    let sib = dir === "next" ? node.nextSibling : node.previousSibling;
    while (sib) {
        if (sib.nodeType === 1)
            return true;
        if (sib.nodeType === 3 && ((_a = sib.textContent) !== null && _a !== void 0 ? _a : "").trim() !== "") {
            return true;
        }
        sib = dir === "next" ? sib.nextSibling : sib.previousSibling;
    }
    return false;
}
/**
 * Parse a DOM node into one or more DocumentNodes.
 * Returns null for nodes that should be dropped (comments, etc.).
 */
function parseDOMNode(domNode, schema, activeMarks) {
    var _a;
    // Text node
    if (domNode.nodeType === 3) {
        const text = (_a = domNode.textContent) !== null && _a !== void 0 ? _a : "";
        if (text === "")
            return null;
        // Drop formatting whitespace between block elements so pretty-printed
        // HTML doesn't import as a string of empty paragraphs.
        if (isInsignificantWhitespace(domNode))
            return null;
        return {
            type: "text",
            text,
            ...(activeMarks.length > 0 ? { marks: activeMarks.slice() } : {}),
        };
    }
    if (domNode.nodeType !== 1)
        return null;
    const el = domNode;
    const tag = el.tagName.toLowerCase();
    if (DROPPED_HTML_TAGS.has(tag) || tag.includes(":")) {
        return null;
    }
    // A <br> with nothing after it is a filler break — the serializer
    // emits one into every empty leaf block so the caret can land
    // there, and browsers insert one into a freshly-emptied block.
    // Either way it carries no document meaning, so drop it. A <br>
    // *followed* by more content is a genuine line break and falls
    // through to the hard_break / soft-break handling below.
    if (tag === "br" && isTrailingBr(domNode)) {
        return null;
    }
    // Try to match a node first — including <br> if a hard_break
    // extension is installed. Nodes take priority over marks because a
    // block element (`<p>`, `<td>`, `<li>`, …) maps to a structural node
    // whose own `getAttrs` turns its inline styles into node attributes
    // (a table cell's `background-color`/`text-align` become cell attrs).
    // The colour/background/font marks register a broad `tag:"*"` rule,
    // so checking marks first let them swallow block elements — a
    // `<td style="background-color:…">` was parsed as a `<span>` mark
    // wrapper, dropping the cell node and its alignment. Marks only apply
    // to inline elements (`<strong>`, `<a>`, `<span>`, …), which never
    // match a node, so they are still reached for those below.
    const nodeSpec = findMatchingNode(tag, el, schema);
    if (nodeSpec) {
        const attrs = extractAttrs(nodeSpec, el);
        const node = { type: nodeSpec.name };
        if (attrs)
            node.attrs = attrs;
        // Atom nodes are leaves — their inner DOM is decoration and their
        // data comes from getAttrs, so don't descend into children.
        if (!nodeSpec.atom) {
            const children = parseChildrenFlat(el, schema, activeMarks);
            if (children.length > 0)
                node.content = children;
        }
        return node;
    }
    // Then match marks. Multiple visual marks may live on the same
    // element, for example `<span style="color:red;background-color:yellow">`.
    // Earlier the parser stopped at the first matching `span` rule, so
    // text colour, background colour and text outline could silently drop
    // one another during HTML → JSON round-trips. Collect every matching
    // mark and carry them all into the children.
    const matchedMarks = findMatchingMarks(tag, el, schema);
    if (matchedMarks.length > 0) {
        return parseChildren(el, schema, [...activeMarks, ...matchedMarks]);
    }
    // Fallback: bare <br> becomes a soft line-break inside text.
    // (If the hard-break extension was registered, the node branch above
    // already produced a real hard_break node and we never get here.)
    if (tag === "br") {
        return { type: "text", text: "\n" };
    }
    // Unknown element — pass children through with current marks.
    return parseChildren(el, schema, activeMarks);
}
function parseChildren(el, schema, activeMarks) {
    const out = [];
    for (const child of Array.from(el.childNodes)) {
        const parsed = parseDOMNode(child, schema, activeMarks);
        if (parsed == null)
            continue;
        if (Array.isArray(parsed))
            out.push(...parsed);
        else
            out.push(parsed);
    }
    return out;
}
function parseChildrenFlat(el, schema, activeMarks) {
    return parseChildren(el, schema, activeMarks);
}
function findMatchingNode(tag, el, schema) {
    // A rule with a `getAttrs` discriminator (e.g. task_list matching only
    // `<ul class="task-list">`) is more specific than a catch-all tag rule
    // (e.g. bullet_list matching any `<ul>`). When both claim the same tag,
    // the specific one must win regardless of registration order — otherwise
    // a `<ul class="task-list">` parsed as bullet_list and a `<li class=
    // "task-item">` as list_item, so task lists silently degraded to plain
    // lists on every HTML round-trip (including each keystroke in a mounted
    // editor). Take the first discriminating match immediately; fall back to
    // the first catch-all only if nothing more specific matched.
    let fallback = null;
    for (const spec of Object.values(schema.nodes)) {
        if (!spec.parseDOM)
            continue;
        for (const rule of spec.parseDOM) {
            if (!rule.tag || !tagMatches(rule.tag, tag, el))
                continue;
            if (rule.getAttrs) {
                const result = rule.getAttrs(el);
                if (result === false)
                    continue;
                return spec;
            }
            if (!fallback)
                fallback = spec;
        }
    }
    return fallback;
}
function findMatchingMarks(tag, el, schema) {
    const marks = [];
    for (const spec of Object.values(schema.marks)) {
        if (!spec.parseDOM)
            continue;
        for (const rule of spec.parseDOM) {
            if (!rule.tag || !tagMatches(rule.tag, tag, el))
                continue;
            let attrs = null;
            if (rule.getAttrs) {
                const result = rule.getAttrs(el);
                if (result === false)
                    continue;
                if (result && typeof result === "object") {
                    attrs = result;
                }
            }
            const mark = { type: spec.name };
            // A getAttrs that matched but produced no attributes (e.g. the
            // span rule for underline returning `{}`) must NOT attach an
            // empty `attrs: {}`, or the same mark gets two JSON shapes
            // ({type} vs {type, attrs:{}}) and the HTML round-trip stops
            // being idempotent — every re-parse looks like a real change and
            // pushes a phantom history entry.
            if (attrs && Object.keys(attrs).length > 0)
                mark.attrs = attrs;
            marks.push(mark);
            break;
        }
    }
    return marks;
}
const FALLBACK_VOID_TAGS = new Set(["br", "hr", "img", "input", "meta", "link"]);
class MiniNode {
    constructor() {
        this.parentNode = null;
    }
    get nextSibling() {
        var _a;
        if (!this.parentNode)
            return null;
        const index = this.parentNode.childNodes.indexOf(this);
        return index >= 0 ? (_a = this.parentNode.childNodes[index + 1]) !== null && _a !== void 0 ? _a : null : null;
    }
    get previousSibling() {
        var _a;
        if (!this.parentNode)
            return null;
        const index = this.parentNode.childNodes.indexOf(this);
        return index > 0 ? (_a = this.parentNode.childNodes[index - 1]) !== null && _a !== void 0 ? _a : null : null;
    }
}
class MiniTextNode extends MiniNode {
    constructor(value) {
        super();
        this.value = value;
        this.nodeType = 3;
    }
    get textContent() {
        return this.value;
    }
}
class MiniElement extends MiniNode {
    constructor(tagName, attrs = {}) {
        super();
        this.nodeType = 1;
        this.childNodes = [];
        this.tagName = tagName.toUpperCase();
        this.attrs = attrs;
    }
    get textContent() {
        return this.childNodes.map((node) => node.textContent).join("");
    }
    appendChild(node) {
        node.parentNode = this;
        this.childNodes.push(node);
    }
    getAttribute(name) {
        var _a;
        return (_a = this.attrs[name.toLowerCase()]) !== null && _a !== void 0 ? _a : null;
    }
    querySelector(selector) {
        const wanted = selector.toLowerCase();
        for (const child of this.childNodes) {
            if (child instanceof MiniElement) {
                if (child.tagName.toLowerCase() === wanted)
                    return child;
                const found = child.querySelector(selector);
                if (found)
                    return found;
            }
        }
        return null;
    }
    closest(selector) {
        const wanted = selector.toLowerCase();
        let current = this;
        while (current) {
            if (current.tagName.toLowerCase() === wanted)
                return current;
            current = current.parentNode;
        }
        return null;
    }
}
function parseHTMLFragment(html) {
    const root = new MiniElement("div");
    const stack = [root];
    const tokenRe = /<!--[\s\S]*?-->|<\/[^>]+>|<[^>]+>|[^<]+/g;
    let match;
    while ((match = tokenRe.exec(html))) {
        const token = match[0];
        const parent = stack[stack.length - 1];
        if (token.startsWith("<!--")) {
            continue;
        }
        if (token.startsWith("</")) {
            const tag = token.slice(2, -1).trim().toLowerCase();
            const index = stack
                .map((element) => element.tagName.toLowerCase())
                .lastIndexOf(tag);
            if (index > 0)
                stack.length = index;
            continue;
        }
        if (token.startsWith("<")) {
            const parsed = parseFallbackTag(token);
            if (!parsed)
                continue;
            const element = new MiniElement(parsed.tagName, parsed.attrs);
            parent.appendChild(element);
            if (!parsed.selfClosing && !FALLBACK_VOID_TAGS.has(parsed.tagName)) {
                stack.push(element);
            }
            continue;
        }
        parent.appendChild(new MiniTextNode(decodeHTML(token)));
    }
    return root;
}
function parseFallbackTag(token) {
    var _a, _b, _c;
    const source = token.slice(1, -1).trim();
    if (!source || source.startsWith("!"))
        return null;
    const selfClosing = /\/$/.test(source);
    const cleaned = selfClosing ? source.slice(0, -1).trim() : source;
    const nameMatch = /^([^\s/>]+)/.exec(cleaned);
    if (!nameMatch)
        return null;
    const tagName = nameMatch[1].toLowerCase();
    const attrs = {};
    const attrSource = cleaned.slice(nameMatch[0].length);
    const attrRe = /([^\s=/>]+)(?:\s*=\s*(?:"([^"]*)"|'([^']*)'|([^\s"'=<>`]+)))?/g;
    let match;
    while ((match = attrRe.exec(attrSource))) {
        const name = match[1].toLowerCase();
        const value = (_c = (_b = (_a = match[2]) !== null && _a !== void 0 ? _a : match[3]) !== null && _b !== void 0 ? _b : match[4]) !== null && _c !== void 0 ? _c : "";
        attrs[name] = decodeHTML(value);
    }
    return { tagName, attrs, selfClosing };
}
function decodeHTML(value) {
    // Single pass so each entity is resolved exactly once. The old
    // chained replaces fed earlier output into later ones, so "&amp;lt;"
    // (literal text "&lt;") was wrongly decoded to "<" \u2014 diverging from
    // the real DOMParser and corrupting text/attribute values in SSR.
    return value.replace(/&(?:nbsp|amp|lt|gt|quot|apos|#(\d+)|#x([0-9a-fA-F]+));/gi, (match, dec, hex) => {
        if (dec !== undefined)
            return String.fromCharCode(Number(dec));
        if (hex !== undefined)
            return String.fromCharCode(parseInt(hex, 16));
        switch (match.slice(1, -1).toLowerCase()) {
            case "nbsp":
                return "\u00a0";
            case "amp":
                return "&";
            case "lt":
                return "<";
            case "gt":
                return ">";
            case "quot":
                return '"';
            case "apos":
                return "'";
            default:
                return match;
        }
    });
}
function tagMatches(ruleTag, actualTag, el) {
    const normalised = ruleTag.toLowerCase();
    const bracket = normalised.indexOf("[");
    if (bracket === -1) {
        return normalised === "*" || normalised === actualTag;
    }
    // Attribute-selector form: "span[data-mention-id]" or "a[href=x]".
    // The bare tag name must match, then every bracketed predicate is
    // verified against the element. Without this, extensions whose
    // parseDOM rule is an attribute selector (mention, comment) never
    // matched and their nodes/marks were lost on HTML import.
    const name = normalised.slice(0, bracket);
    if (name !== "*" && name !== actualTag)
        return false;
    if (!el)
        return false;
    const predicate = /\[\s*([a-z0-9_-]+)\s*(?:=\s*"?([^\]"]*)"?\s*)?\]/g;
    let m;
    while ((m = predicate.exec(normalised))) {
        const have = el.getAttribute(m[1]);
        if (have == null)
            return false;
        if (m[2] !== undefined && have.toLowerCase() !== m[2])
            return false;
    }
    return true;
}
function extractAttrs(spec, el) {
    if (!spec.parseDOM)
        return null;
    const tag = el.tagName.toLowerCase();
    for (const rule of spec.parseDOM) {
        if (rule.tag && tagMatches(rule.tag, tag, el)) {
            if (rule.getAttrs) {
                const result = rule.getAttrs(el);
                if (result && typeof result === "object") {
                    return result;
                }
            }
            return null;
        }
    }
    return null;
}

},{}],
5:[function(require,module,exports){
"use strict";
/**
 * History (undo/redo).
 *
 * Two stacks of document snapshots. Each non-history transaction pushes
 * the previous state to the undo stack and clears the redo stack.
 *
 * For MVP we snapshot full docs; this is O(doc size) per edit but simple
 * and bulletproof. A future optimization is to store inverse operations.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.History = void 0;
class History {
    constructor(options = {}) {
        var _a, _b;
        this.undoStack = [];
        this.redoStack = [];
        this.lastRecordTime = 0;
        this.lastCoalesceKey = null;
        this.limit = (_a = options.limit) !== null && _a !== void 0 ? _a : 100;
        this.coalesceMs = (_b = options.coalesceMs) !== null && _b !== void 0 ? _b : 500;
    }
    /**
     * Record the *previous* state before a change is applied.
     *
     * When `coalesceKey` is given and matches the previous record within
     * the coalesce window, the intermediate snapshot is dropped — a run
     * of typing in one block becomes one undo step instead of one per
     * keystroke. Structural edits and formatting commands pass no key, so
     * each is its own step.
     */
    record(entry, coalesceKey, now = Date.now()) {
        if (coalesceKey != null &&
            this.coalesceMs > 0 &&
            coalesceKey === this.lastCoalesceKey &&
            now - this.lastRecordTime < this.coalesceMs &&
            this.undoStack.length > 0) {
            // Same continuous edit run — keep the pre-run snapshot, just
            // refresh the timer. A new edit still invalidates the redo branch.
            this.lastRecordTime = now;
            this.redoStack.length = 0;
            return;
        }
        this.undoStack.push(entry);
        if (this.undoStack.length > this.limit) {
            this.undoStack.shift();
        }
        // Any new edit invalidates the redo branch.
        this.redoStack.length = 0;
        this.lastCoalesceKey = coalesceKey !== null && coalesceKey !== void 0 ? coalesceKey : null;
        this.lastRecordTime = now;
    }
    canUndo() {
        return this.undoStack.length > 0;
    }
    canRedo() {
        return this.redoStack.length > 0;
    }
    /**
     * Pop the most recent undo entry and push the *current* state onto
     * the redo stack. Caller must apply the returned entry.
     */
    undo(current) {
        const entry = this.undoStack.pop();
        if (!entry)
            return null;
        this.redoStack.push(current);
        if (this.redoStack.length > this.limit) {
            this.redoStack.shift();
        }
        // After an undo, the next edit must start a fresh step rather than
        // coalescing onto the snapshot we just restored.
        this.lastCoalesceKey = null;
        return entry;
    }
    redo(current) {
        const entry = this.redoStack.pop();
        if (!entry)
            return null;
        this.undoStack.push(current);
        if (this.undoStack.length > this.limit) {
            this.undoStack.shift();
        }
        this.lastCoalesceKey = null;
        return entry;
    }
    clear() {
        this.undoStack.length = 0;
        this.redoStack.length = 0;
        this.lastCoalesceKey = null;
    }
}
exports.History = History;

},{}],
6:[function(require,module,exports){
"use strict";
/**
 * Typed event emitter for the editor.
 *
 * Internal storage is a `Map<string, Set<Function>>` — the generic
 * `on(...)` signature gives us type safety at the call site, while the
 * untyped Map keeps TS happy with the mapped-type assignment problem.
 *
 * `on(...)` returns an unsubscribe function (same pattern as
 * EventTarget.removeEventListener but without the awkwardness).
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.EventEmitter = void 0;
class EventEmitter {
    constructor() {
        this.handlers = new Map();
    }
    on(event, handler) {
        let set = this.handlers.get(event);
        if (!set) {
            set = new Set();
            this.handlers.set(event, set);
        }
        const wrapped = handler;
        set.add(wrapped);
        return () => {
            set.delete(wrapped);
        };
    }
    emit(event, payload) {
        const set = this.handlers.get(event);
        if (!set || set.size === 0)
            return;
        // Snapshot — handlers may unsubscribe themselves during dispatch.
        for (const h of Array.from(set)) {
            try {
                h(payload);
            }
            catch (err) {
                // Never let a misbehaving listener kill the editor.
                console.error("[smeditor] event handler threw:", err);
            }
        }
    }
    clear() {
        this.handlers.clear();
    }
}
exports.EventEmitter = EventEmitter;

},{}],
7:[function(require,module,exports){
"use strict";
/**
 * Pure helpers for working with DocumentJSON.
 *
 * All functions are immutable: they return new document objects rather
 * than mutating the input. This keeps history snapshots safe and lets
 * us compare docs by reference for "did anything change?".
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.cloneDoc = cloneDoc;
exports.docsEqual = docsEqual;
exports.emptyDocument = emptyDocument;
exports.getBlockText = getBlockText;
exports.stripBlockPrefix = stripBlockPrefix;
exports.forEachTextNode = forEachTextNode;
exports.hasMark = hasMark;
exports.selectionHasMark = selectionHasMark;
exports.textsInSelection = textsInSelection;
exports.toggleMarkInDoc = toggleMarkInDoc;
exports.setMarkAcrossSelection = setMarkAcrossSelection;
exports.unsetMarkAcrossSelection = unsetMarkAcrossSelection;
exports.clearMarksAcrossSelection = clearMarksAcrossSelection;
exports.leafBlocksInSelection = leafBlocksInSelection;
exports.setBlockType = setBlockType;
const path_js_1 = require("./path.js");
const transform_js_1 = require("./transform.js");
/** Deep clone any JSON-safe value. Faster than JSON.parse(JSON.stringify) for our shapes. */
function cloneDoc(value) {
    if (value === null || typeof value !== "object")
        return value;
    if (Array.isArray(value)) {
        return value.map((v) => cloneDoc(v));
    }
    const out = {};
    for (const [k, v] of Object.entries(value)) {
        out[k] = cloneDoc(v);
    }
    return out;
}
/**
 * Returns true if the two docs are structurally equal.
 * Used to skip no-op transactions.
 */
function docsEqual(a, b) {
    return JSON.stringify(a) === JSON.stringify(b);
}
/** An empty doc containing a single empty paragraph. */
function emptyDocument() {
    return {
        type: "doc",
        content: [{ type: "paragraph", content: [] }],
    };
}
/**
 * Concatenated plain text of a block's inline content. Used by the
 * input-rule engine to test regexes against "the text typed so far".
 */
function getBlockText(block) {
    var _a;
    if (!block.content)
        return "";
    let out = "";
    for (const child of block.content) {
        if (child.type === "text")
            out += (_a = child.text) !== null && _a !== void 0 ? _a : "";
    }
    return out;
}
/**
 * Remove the first `count` characters from a block's inline content,
 * mutating `block` in place. Marks on the surviving text are preserved
 * — only leading characters are dropped.
 *
 * This is what markdown input-rule handlers use to strip a typed
 * prefix like "## " before changing the block type.
 */
function stripBlockPrefix(block, count) {
    var _a;
    if (!block.content || count <= 0)
        return;
    let remaining = count;
    const next = [];
    for (const child of block.content) {
        if (remaining <= 0) {
            next.push(child);
            continue;
        }
        if (child.type === "text") {
            const text = (_a = child.text) !== null && _a !== void 0 ? _a : "";
            if (text.length <= remaining) {
                // Whole node consumed by the prefix — drop it.
                remaining -= text.length;
            }
            else {
                next.push({ ...child, text: text.slice(remaining) });
                remaining = 0;
            }
        }
        else {
            // Non-text inline node — can't slice it, keep as-is.
            next.push(child);
        }
    }
    block.content = next;
}
/**
 * Walk every text node in the document, calling `visit` with its
 * containing block and index within that block.
 */
function forEachTextNode(doc, visit) {
    const walk = (node, path) => {
        if (!node.content)
            return;
        node.content.forEach((child, i) => {
            if (child.type === "text") {
                visit(child, path, i);
            }
            else {
                walk(child, [...path, i]);
            }
        });
    };
    walk(doc, []);
}
/** Whether a mark of a given type/attrs is present on a text node. */
function hasMark(text, type, attrs) {
    if (!text.marks)
        return false;
    return text.marks.some((m) => m.type === type && marksAttrsMatch(m, attrs));
}
function marksAttrsMatch(mark, attrs) {
    var _a;
    if (!attrs)
        return true;
    for (const [k, v] of Object.entries(attrs)) {
        if (((_a = mark.attrs) === null || _a === void 0 ? void 0 : _a[k]) !== v)
            return false;
    }
    return true;
}
/**
 * Returns true if every text node touched by the selection has the
 * given mark — used to drive "is bold active?" toggle UI state.
 */
function selectionHasMark(doc, selection, type, attrs) {
    if (!selection)
        return false;
    const ranges = leafRangesInSelection(doc, selection);
    const texts = ranges.flatMap(({ node, from, to }) => { var _a; return textNodesInInlineRange((_a = node.content) !== null && _a !== void 0 ? _a : [], from, to); });
    if (texts.length === 0)
        return false;
    return texts.every((t) => hasMark(t, type, attrs));
}
/** Collect every text node intersecting the selection. */
function textsInSelection(doc, selection) {
    const result = [];
    const { anchor, head } = selection;
    const start = comparePoints(anchor, head) <= 0 ? anchor : head;
    const end = start === anchor ? head : anchor;
    let started = false;
    let done = false;
    const walk = (node, path) => {
        if (done)
            return;
        if (node.type === "text") {
            if (pathStartsWith(path.slice(0, -1), start.path) || started) {
                // Collect every text node in range. Finalise only after the end
                // block's children are exhausted (below), not on its first text
                // node — otherwise a block split into several inline runs (mixed
                // marks) dropped all but the first run.
                result.push(node);
                if (samePath(path.slice(0, -1), end.path) && start === end) {
                    // Collapsed selection inside one text node — done immediately.
                    done = true;
                    return;
                }
                started = true;
            }
            return;
        }
        if (!node.content)
            return;
        node.content.forEach((child, i) => walk(child, [...path, i]));
        if (samePath(path, end.path))
            done = true;
    };
    walk(doc, []);
    // Fallback: for collapsed selection that didn't yield anything, return
    // the text node directly under start.path (if any).
    if (result.length === 0) {
        const parent = nodeAtPath(doc, start.path);
        if (parent === null || parent === void 0 ? void 0 : parent.content) {
            for (const c of parent.content) {
                if (c.type === "text")
                    result.push(c);
            }
        }
    }
    return result;
}
function comparePoints(a, b) {
    const len = Math.min(a.path.length, b.path.length);
    for (let i = 0; i < len; i++) {
        if (a.path[i] !== b.path[i])
            return a.path[i] - b.path[i];
    }
    if (a.path.length !== b.path.length) {
        return a.path.length - b.path.length;
    }
    return a.offset - b.offset;
}
function pathStartsWith(path, prefix) {
    if (path.length < prefix.length)
        return false;
    for (let i = 0; i < prefix.length; i++) {
        if (path[i] !== prefix[i])
            return false;
    }
    return true;
}
function samePath(a, b) {
    if (a.length !== b.length)
        return false;
    return a.every((v, i) => v === b[i]);
}
function nodeAtPath(doc, path) {
    let node = doc;
    for (const i of path) {
        if (!node.content || !node.content[i])
            return null;
        node = node.content[i];
    }
    return node;
}
/**
 * Toggle a mark across the current selection.
 * Returns a new doc, or null if no change was needed.
 */
function toggleMarkInDoc(doc, selection, type, attrs) {
    if (!selection)
        return null;
    const allHave = selectionHasMark(doc, selection, type, attrs);
    const mark = { type };
    if (attrs)
        mark.attrs = attrs;
    return editMarkAcrossSelection(doc, selection, (content, from, to) => {
        if (allHave) {
            return removeMatchingMarkFromInlineRange(content, from, to, type, attrs);
        }
        return (0, transform_js_1.applyMarkToInlineRange)(content, from, to, mark);
    });
}
/**
 * Apply (or replace) a mark across exactly the selected range.
 *
 * This is the deep-, range-aware primitive that inline-formatting
 * extensions (text colour, background colour, highlight, font size,
 * font family) build on. Earlier those commands marked *every* text
 * node in the anchor block, so selecting one word coloured the whole
 * paragraph — this targets only the characters the user selected and
 * reaches into table cells / list items through the path.
 *
 * Behaviour:
 *  - a real range  → the mark is set on just that span, replacing any
 *    existing mark of the same type so colours/sizes swap cleanly;
 *  - a collapsed caret → the mark is applied to the whole block the
 *    caret sits in, so a toolbar click with no selection still does
 *    something predictable.
 *
 * Returns a new document, or null when nothing changed.
 */
function setMarkAcrossSelection(doc, selection, mark) {
    return editMarkAcrossSelection(doc, selection, (content, from, to) => (0, transform_js_1.setMarkOnInlineRange)(content, from, to, mark));
}
/**
 * Remove every mark of `type` from the selected range (or the whole
 * block, for a collapsed caret). Counterpart to
 * {@link setMarkAcrossSelection}. Returns a new document, or null.
 */
function unsetMarkAcrossSelection(doc, selection, type) {
    return editMarkAcrossSelection(doc, selection, (content, from, to) => (0, transform_js_1.removeMarkFromInlineRange)(content, from, to, type));
}
/**
 * Strip *every* mark from the text nodes inside the selected range
 * (or the whole block, for a collapsed caret). What the eraser
 * button does. Returns a new document, or null when nothing changed.
 */
function clearMarksAcrossSelection(doc, selection) {
    return editMarkAcrossSelection(doc, selection, (content, from, to) => {
        // Reuse the offset-aware split machinery: cut [from, to) out,
        // drop marks on text nodes inside it, splice back.
        const lo = Math.min(from, to);
        const hi = Math.max(from, to);
        if (hi <= lo)
            return content.slice();
        const [before, rest] = (0, transform_js_1.splitInlineAt)(content, lo);
        const [middle, after] = (0, transform_js_1.splitInlineAt)(rest, hi - lo);
        const cleaned = middle.map((n) => {
            if (n.type !== "text" || !n.marks)
                return n;
            const next = { ...n };
            delete next.marks;
            return next;
        });
        return [...before, ...cleaned, ...after];
    });
}
/** Shared body for the mark-edit helpers above. */
function editMarkAcrossSelection(doc, selection, edit) {
    var _a, _b;
    if (!selection)
        return null;
    const newDoc = cloneDoc(doc);
    const ranges = leafRangesInSelection(newDoc, selection);
    if (ranges.length === 0)
        return null;
    let changed = false;
    for (const { node, from, to } of ranges) {
        const nextContent = edit((_a = node.content) !== null && _a !== void 0 ? _a : [], from, to);
        if (JSON.stringify(nextContent) !== JSON.stringify((_b = node.content) !== null && _b !== void 0 ? _b : [])) {
            node.content = nextContent;
            changed = true;
        }
    }
    return changed ? newDoc : null;
}
function leafRangesInSelection(doc, selection) {
    const { anchor, head } = selection;
    const start = comparePoints(anchor, head) <= 0 ? anchor : head;
    const end = start === anchor ? head : anchor;
    const collapsed = samePath(start.path, end.path) && start.offset === end.offset;
    return leafBlocksInSelection(doc, selection)
        .map(({ path, node }) => {
        var _a;
        const len = inlineContentLength((_a = node.content) !== null && _a !== void 0 ? _a : []);
        let from;
        let to;
        if (collapsed) {
            from = 0;
            to = len;
        }
        else {
            const isStart = samePath(path, start.path);
            const isEnd = samePath(path, end.path);
            from = isStart ? Math.min(start.offset, len) : 0;
            to = isEnd ? Math.min(end.offset, len) : len;
        }
        return { path, node, from, to };
    })
        .filter(({ from, to }) => to > from);
}
function inlineContentLength(content) {
    // Use the same visible-width measure as splitInlineAt / setMarkOnInlineRange:
    // an atomic inline node (a mention pill) contributes its full label
    // length, not 1. Counting it as 1 clamped the selectable range short, so
    // mark commands silently skipped the text that followed a mention.
    return content.reduce((sum, node) => sum + (0, transform_js_1.visibleLength)(node), 0);
}
function textNodesInInlineRange(content, from, to) {
    const lo = Math.min(from, to);
    const hi = Math.max(from, to);
    if (hi <= lo)
        return [];
    const [, rest] = (0, transform_js_1.splitInlineAt)(content, lo);
    const [middle] = (0, transform_js_1.splitInlineAt)(rest, hi - lo);
    return middle.filter((node) => node.type === "text");
}
function removeMatchingMarkFromInlineRange(content, from, to, type, attrs) {
    const lo = Math.min(from, to);
    const hi = Math.max(from, to);
    if (hi <= lo)
        return content.slice();
    const [before, rest] = (0, transform_js_1.splitInlineAt)(content, lo);
    const [middle, after] = (0, transform_js_1.splitInlineAt)(rest, hi - lo);
    const cleaned = middle.map((node) => {
        if (node.type !== "text" || !node.marks)
            return node;
        const marks = node.marks.filter((mark) => !(mark.type === type && marksAttrsMatch(mark, attrs)));
        const next = { ...node };
        if (marks.length)
            next.marks = marks;
        else
            delete next.marks;
        return next;
    });
    return [...before, ...cleaned, ...after];
}
/** Lexicographic comparison of two paths. */
function comparePath(a, b) {
    const len = Math.min(a.length, b.length);
    for (let i = 0; i < len; i++) {
        if (a[i] !== b[i])
            return a[i] - b[i];
    }
    return a.length - b.length;
}
/**
 * Every leaf block (paragraph, heading, …) the selection touches,
 * with its path. Works for both selection shapes: a top-level
 * selection yields the top-level blocks in range; a deep selection
 * yields the leaf blocks inside table cells / list items in range.
 *
 * This is the block-level counterpart to `textsInSelection` — block
 * commands resolve their targets through it instead of indexing
 * `path[0]`, which only ever sees the top level.
 */
function leafBlocksInSelection(doc, selection) {
    const { anchor, head } = selection;
    const a = comparePoints(anchor, head) <= 0 ? anchor : head;
    const b = a === anchor ? head : anchor;
    const leaves = [];
    const walk = (node, path) => {
        var _a;
        if (path.length > 0 && (0, path_js_1.isLeafBlock)(node)) {
            leaves.push({ path, node });
            return;
        }
        (_a = node.content) === null || _a === void 0 ? void 0 : _a.forEach((child, i) => walk(child, [...path, i]));
    };
    walk(doc, []);
    return leaves.filter((l) => isAtOrAfterBoundary(l.path, a.path) && isAtOrBeforeBoundary(l.path, b.path));
}
/**
 * A leaf block should count as inside a selection boundary when it is
 * exactly at the boundary or nested under the selected container.
 * Example: after wrapping a paragraph in a blockquote, the retained
 * selection can be `{ path: [0] }` (the blockquote). The editable leaf is
 * `[0, 0]`; treating descendants as included lets alignment, colours and
 * other tools keep working immediately after the wrap command.
 */
function isAtOrAfterBoundary(path, boundary) {
    return comparePath(path, boundary) >= 0 || pathStartsWith(path, boundary);
}
function isAtOrBeforeBoundary(path, boundary) {
    return comparePath(path, boundary) <= 0 || pathStartsWith(path, boundary);
}
/**
 * Change the type (and attrs) of every block touched by the selection.
 * Used by commands like `setHeading({ level: 2 })` or `setParagraph()`.
 */
function setBlockType(doc, selection, type, attrs) {
    if (!selection) {
        // No selection — change the first block (sensible default for tests).
        if (!doc.content[0])
            return null;
        const newDoc = cloneDoc(doc);
        newDoc.content[0].type = type;
        if (attrs)
            newDoc.content[0].attrs = { ...attrs };
        else
            delete newDoc.content[0].attrs;
        return newDoc;
    }
    const newDoc = cloneDoc(doc);
    // Resolve through leafBlocksInSelection so a deep selection retargets
    // the leaf block (a cell paragraph), not the top-level container.
    const leaves = leafBlocksInSelection(newDoc, selection);
    if (leaves.length === 0)
        return null;
    for (const { node } of leaves) {
        node.type = type;
        if (attrs)
            node.attrs = { ...attrs };
        else
            delete node.attrs;
    }
    return newDoc;
}

},{"./path.js":8,"./transform.js":9}],
8:[function(require,module,exports){
"use strict";
/**
 * Path resolution — addressing nodes below the top level.
 *
 * A `path` is an array of content indices from the document root:
 * `[2]` is `doc.content[2]`; `[2, 0, 1]` is
 * `doc.content[2].content[0].content[1]`. The selection model's
 * `SelectionPoint.path` uses this shape.
 *
 * Today every command treats `path` as length 1 — a top-level block
 * index. The deep selection model lets a path reach inside tables,
 * list items, and blockquotes. These pure helpers are step 1: they
 * resolve, transform, and classify nodes by path, without changing
 * what `pointFromDOM` produces or how any command behaves. Commands
 * migrate onto them one subsystem at a time.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.isLeafBlock = isLeafBlock;
exports.isContainerBlock = isContainerBlock;
exports.nodeAt = nodeAt;
exports.parentAt = parentAt;
exports.descendToLeafPath = descendToLeafPath;
exports.mapNodeAt = mapNodeAt;
exports.removeNodeAt = removeNodeAt;
exports.resolveSelectionTarget = resolveSelectionTarget;
exports.normalizeDeepPoint = normalizeDeepPoint;
// ============================================================================
// Classification
// ============================================================================
/** Node types that live in inline content. */
const INLINE_TYPES = new Set(["text", "hard_break", "mention"]);
/**
 * A leaf block holds inline content (a paragraph, heading, code
 * block) — the caret can sit inside it. A container block holds other
 * blocks (a table, list, list item, blockquote). An empty block is
 * treated as a leaf.
 */
function isLeafBlock(node) {
    if (!node.content || node.content.length === 0)
        return true;
    return node.content.some((c) => INLINE_TYPES.has(c.type));
}
/** The inverse of {@link isLeafBlock} — a block holding other blocks. */
function isContainerBlock(node) {
    return !isLeafBlock(node);
}
// ============================================================================
// Reading
// ============================================================================
/**
 * The node at `path`, or `null` if the path runs off the tree. An
 * empty path resolves to the document itself.
 */
function nodeAt(doc, path) {
    let node = doc;
    for (const idx of path) {
        const children = node.content;
        if (!children || children[idx] === undefined)
            return null;
        node = children[idx];
    }
    return node;
}
/** The parent of the node at `path` (i.e. the node at `path` minus its last index). */
function parentAt(doc, path) {
    if (path.length === 0)
        return null;
    return nodeAt(doc, path.slice(0, -1));
}
/**
 * Descend from the node at `path` to its first leaf block, returning
 * the extended path. Used to normalise a selection that landed on a
 * container (e.g. a click that resolved to a table rather than a cell
 * paragraph). A path already at a leaf is returned unchanged.
 */
function descendToLeafPath(doc, path) {
    const result = [...path];
    let node = nodeAt(doc, result);
    while (node &&
        isContainerBlock(node) &&
        node.content &&
        node.content.length > 0) {
        result.push(0);
        node = node.content[0];
    }
    return result;
}
// ============================================================================
// Transforming
// ============================================================================
/**
 * Return a new document with the node at `path` replaced by
 * `fn(node)`. Nodes along the path are shallow-cloned; everything
 * else is shared. An out-of-range path returns the document
 * unchanged. An empty path replaces the root.
 */
function mapNodeAt(doc, path, fn) {
    if (path.length === 0) {
        return fn(doc);
    }
    const recurse = (node, depth) => {
        const idx = path[depth];
        const children = node.content;
        if (!children || children[idx] === undefined)
            return node;
        const child = children[idx];
        const nextChild = depth === path.length - 1
            ? fn(child)
            : recurse(child, depth + 1);
        const nextContent = children.slice();
        nextContent[idx] = nextChild;
        return { ...node, content: nextContent };
    };
    return recurse(doc, 0);
}
/**
 * Return a new document with the node at `path` removed from its
 * parent. An out-of-range path returns the document unchanged.
 */
function removeNodeAt(doc, path) {
    if (path.length === 0)
        return doc;
    const parentPath = path.slice(0, -1);
    const idx = path[path.length - 1];
    return mapNodeAt(doc, parentPath, (parent) => {
        const children = parent.content;
        if (!children || children[idx] === undefined)
            return parent;
        const next = children.slice();
        next.splice(idx, 1);
        return { ...parent, content: next };
    });
}
// ============================================================================
// Selection target
// ============================================================================
/**
 * Resolve the block a selection's anchor sits in, by path. Returns the
 * node and its path, or `null` if there's no selection or the path is
 * off-tree.
 *
 * This is the compatibility seam for the deep selection model. A
 * command that used to do `doc.content[sel.anchor.path[0]]` calls this
 * instead: with a top-level path it resolves the same top-level block
 * as before; with a deep path it resolves the leaf block inside a
 * table cell or list item. The command body — which then mutates
 * `node.content` — is otherwise unchanged.
 */
function resolveSelectionTarget(doc, selection) {
    if (!selection)
        return null;
    const path = selection.anchor.path;
    const node = nodeAt(doc, path);
    if (!node)
        return null;
    return { node, path };
}
/**
 * Normalise a deep selection point. If the path resolves to a
 * container block (a table, a row, a list), the point came from a
 * click that landed on structure rather than editable text — descend
 * to the first leaf block and reset the offset to 0.
 *
 * A point already at a leaf, or one that resolves to nothing, is
 * returned unchanged.
 */
function normalizeDeepPoint(doc, point) {
    const node = nodeAt(doc, point.path);
    if (node && isContainerBlock(node)) {
        return { path: descendToLeafPath(doc, point.path), offset: 0 };
    }
    return point;
}

},{}],
9:[function(require,module,exports){
"use strict";
/**
 * Transform API — offset-aware document transforms.
 *
 * The selection model addresses a position as `{ path: [blockIndex],
 * offset }`, where `offset` is a count of *visible characters* into
 * the block — the same count `pointFromDOM` produces by summing the
 * `textContent` length of the DOM. A mention pill contributes the
 * length of its label text; a hard break contributes nothing.
 *
 * These functions operate on a block's inline content array. They're
 * pure — content in, new content out, no editor, no DOM — so they're
 * straightforward to test and to compose. Extensions (mentions,
 * comments) build their commands on them instead of hand-rolling
 * text-node surgery.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.visibleLength = visibleLength;
exports.inlineLength = inlineLength;
exports.blockVisibleText = blockVisibleText;
exports.splitInlineAt = splitInlineAt;
exports.insertInlineAt = insertInlineAt;
exports.deleteInlineRange = deleteInlineRange;
exports.replaceInlineRange = replaceInlineRange;
exports.applyMarkToInlineRange = applyMarkToInlineRange;
exports.removeMarkFromInlineRange = removeMarkFromInlineRange;
exports.setMarkOnInlineRange = setMarkOnInlineRange;
exports.splitBlock = splitBlock;
// ============================================================================
// Measurement
// ============================================================================
/** Visible character length of one inline node (text, or nested text). */
function visibleLength(node) {
    var _a;
    if (node.type === "text")
        return ((_a = node.text) !== null && _a !== void 0 ? _a : "").length;
    if (node.content) {
        return node.content.reduce((sum, c) => sum + visibleLength(c), 0);
    }
    return 0;
}
/** Total visible character length of a block's inline content. */
function inlineLength(block) {
    var _a;
    return ((_a = block.content) !== null && _a !== void 0 ? _a : []).reduce((sum, n) => sum + visibleLength(n), 0);
}
/** The block's visible text, including text inside atomic nodes. */
function blockVisibleText(block) {
    var _a;
    let out = "";
    const walk = (node) => {
        var _a, _b;
        if (node.type === "text") {
            out += (_a = node.text) !== null && _a !== void 0 ? _a : "";
            return;
        }
        for (const c of (_b = node.content) !== null && _b !== void 0 ? _b : [])
            walk(c);
    };
    for (const n of (_a = block.content) !== null && _a !== void 0 ? _a : [])
        walk(n);
    return out;
}
// ============================================================================
// Core split
// ============================================================================
function withMarks(text, marks) {
    const node = { type: "text", text };
    if (marks && marks.length)
        node.marks = marks;
    return node;
}
/**
 * Split an inline content array at `offset`, returning `[before,
 * after]`. A text node straddling the offset is cut in two, marks
 * preserved on both halves. An atomic node (mention) can't be cut —
 * it goes to whichever side its midpoint favours.
 */
function splitInlineAt(content, offset) {
    var _a;
    const before = [];
    const after = [];
    let cur = 0;
    for (const node of content) {
        const len = visibleLength(node);
        if (cur >= offset) {
            after.push(node);
        }
        else if (cur + len <= offset) {
            before.push(node);
        }
        else if (node.type === "text") {
            const at = offset - cur;
            const txt = (_a = node.text) !== null && _a !== void 0 ? _a : "";
            const left = txt.slice(0, at);
            const right = txt.slice(at);
            if (left)
                before.push(withMarks(left, node.marks));
            if (right)
                after.push(withMarks(right, node.marks));
        }
        else {
            // Atomic — assign whole to the nearer side.
            if (offset - cur <= len / 2)
                after.push(node);
            else
                before.push(node);
        }
        cur += len;
    }
    return [before, after];
}
// ============================================================================
// Inline edits
// ============================================================================
/** Insert `nodes` into a content array at `offset`. */
function insertInlineAt(content, offset, nodes) {
    const [before, after] = splitInlineAt(content, offset);
    return [...before, ...nodes, ...after];
}
/** Delete the visible range `[from, to)` from a content array. */
function deleteInlineRange(content, from, to) {
    if (to <= from)
        return content.slice();
    const [before] = splitInlineAt(content, from);
    const [, after] = splitInlineAt(content, to);
    return [...before, ...after];
}
/** Replace the visible range `[from, to)` with `nodes`. */
function replaceInlineRange(content, from, to, nodes) {
    const lo = Math.min(from, to);
    const hi = Math.max(from, to);
    const [before] = splitInlineAt(content, lo);
    const [, after] = splitInlineAt(content, hi);
    return [...before, ...nodes, ...after];
}
/**
 * Add `mark` to every text node in the visible range `[from, to)`.
 * Text straddling the range boundary is split so the mark applies
 * exactly to the selected span. A duplicate mark (same type + attrs)
 * is not added twice.
 */
function applyMarkToInlineRange(content, from, to, mark) {
    var _a;
    const lo = Math.min(from, to);
    const hi = Math.max(from, to);
    if (hi <= lo)
        return content.slice();
    const [before, rest] = splitInlineAt(content, lo);
    const [middle, after] = splitInlineAt(rest, hi - lo);
    const key = mark.type + ":" + JSON.stringify((_a = mark.attrs) !== null && _a !== void 0 ? _a : {});
    const marked = middle.map((n) => {
        if (n.type !== "text")
            return n;
        const marks = n.marks ? [...n.marks] : [];
        const has = marks.some((m) => { var _a; return m.type + ":" + JSON.stringify((_a = m.attrs) !== null && _a !== void 0 ? _a : {}) === key; });
        if (!has)
            marks.push(mark);
        return { ...n, marks };
    });
    return [...before, ...marked, ...after];
}
/**
 * Remove every mark of `type` from text nodes inside the visible
 * range `[from, to)`. Text straddling the boundary is split so the
 * removal applies exactly to the range. Marks outside the range are
 * untouched.
 */
function removeMarkFromInlineRange(content, from, to, type) {
    const lo = Math.min(from, to);
    const hi = Math.max(from, to);
    if (hi <= lo)
        return content.slice();
    const [before, rest] = splitInlineAt(content, lo);
    const [middle, after] = splitInlineAt(rest, hi - lo);
    const cleaned = middle.map((n) => {
        if (n.type !== "text" || !n.marks)
            return n;
        const marks = n.marks.filter((m) => m.type !== type);
        const next = { ...n };
        if (marks.length)
            next.marks = marks;
        else
            delete next.marks;
        return next;
    });
    return [...before, ...cleaned, ...after];
}
/**
 * Set `mark` on the visible range `[from, to)`, *replacing* any
 * existing mark of the same type that was already there. This is the
 * right primitive for single-valued marks (text color, font size,
 * …): applying a new colour swaps the old one instead of stacking a
 * second colour mark on the same text.
 */
function setMarkOnInlineRange(content, from, to, mark) {
    const cleared = removeMarkFromInlineRange(content, from, to, mark.type);
    return applyMarkToInlineRange(cleared, from, to, mark);
}
// ============================================================================
// Block-level split
// ============================================================================
/**
 * Split the block at `blockIndex` into two at the given visible
 * `offset`, returning a new document. The second block keeps the
 * first's type and attributes. Out-of-range input returns the
 * document unchanged.
 */
function splitBlock(doc, blockIndex, offset) {
    var _a, _b;
    const blocks = (_a = doc.content) !== null && _a !== void 0 ? _a : [];
    const block = blocks[blockIndex];
    if (!block)
        return doc;
    const [before, after] = splitInlineAt((_b = block.content) !== null && _b !== void 0 ? _b : [], offset);
    const first = { ...block, content: before };
    const second = { ...block, content: after };
    const next = blocks.slice();
    next.splice(blockIndex, 1, first, second);
    return { ...doc, content: next };
}

},{}],
10:[function(require,module,exports){
"use strict";
/**
 * Small CSS-value / URL helpers shared by marks and paste sanitizers.
 *
 * They intentionally accept only self-contained tokens. Values that need
 * another CSS declaration, nested functions, custom properties, quotes or URL
 * parsing are rejected before they can be rendered back into HTML.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.sanitizeCSSColor = sanitizeCSSColor;
exports.sanitizeCSSFontFamily = sanitizeCSSFontFamily;
exports.sanitizeCSSFontSize = sanitizeCSSFontSize;
exports.sanitizeCSSLineHeight = sanitizeCSSLineHeight;
exports.sanitizeCSSTextStrokeWidth = sanitizeCSSTextStrokeWidth;
exports.sanitizeURL = sanitizeURL;
exports.sanitizeLinkTarget = sanitizeLinkTarget;
exports.hardenLinkAttrs = hardenLinkAttrs;
const MAX_COLOR_LENGTH = 80;
const HEX_COLOR = /^#[0-9a-fA-F]{3,8}$/;
const CSS_IDENTIFIER = /^[a-zA-Z]+$/;
const NUMBER_OR_PERCENT = /^[+-]?(?:\d+|\d*\.\d+)%?$/;
const HUE = /^[+-]?(?:\d+|\d*\.\d+)(?:deg|rad|turn)?$/i;
const PERCENT = /^[+-]?(?:\d+|\d*\.\d+)%$/;
const UNSAFE_COLOR_CHARS = /[;:"'<>\\]/;
const UNSAFE_URL_CHARS = /[\u0000-\u001f\u007f\s<>"']/u;
const FONT_FAMILY_TOKEN = /^[a-zA-Z0-9 ,"'-]+$/;
const CSS_LENGTH_TOKEN = /^(\d+(?:\.\d+)?)(px|pt|em|rem|%)?$/;
const LINE_HEIGHT_TOKEN = /^(\d+(?:\.\d+)?)(px|em|rem|%)?$/;
const TEXT_STROKE_WIDTH_TOKEN = /^(\d+(?:\.\d+)?)px$/i;
const SAFE_TARGETS = new Set(["_blank", "_self", "_parent", "_top"]);
function sanitizeCSSColor(raw) {
    if (raw == null)
        return null;
    const value = String(raw).trim();
    if (!value || value.length > MAX_COLOR_LENGTH)
        return null;
    if (UNSAFE_COLOR_CHARS.test(value))
        return null;
    if (HEX_COLOR.test(value))
        return value;
    if (CSS_IDENTIFIER.test(value))
        return value;
    if (isRGBColor(value))
        return value;
    if (isHSLColor(value))
        return value;
    return null;
}
function sanitizeCSSFontFamily(raw) {
    if (raw == null)
        return null;
    const value = String(raw).trim();
    if (!value || value.length > 200)
        return null;
    return FONT_FAMILY_TOKEN.test(value) ? value : null;
}
function sanitizeCSSFontSize(raw) {
    const parsed = sanitizeCSSLength(raw, 400, "px", true);
    return parsed;
}
function sanitizeCSSLineHeight(raw) {
    if (raw == null)
        return null;
    const value = String(raw).trim();
    const match = LINE_HEIGHT_TOKEN.exec(value);
    if (!match)
        return null;
    const number = Number(match[1]);
    if (!Number.isFinite(number) || number <= 0 || number > 10)
        return null;
    return match[2] ? `${match[1]}${match[2]}` : match[1];
}
function sanitizeCSSTextStrokeWidth(raw) {
    if (raw == null)
        return null;
    const value = String(raw).trim();
    const match = TEXT_STROKE_WIDTH_TOKEN.exec(value);
    if (!match)
        return null;
    const number = Number(match[1]);
    if (!Number.isFinite(number) || number < 0 || number > 8)
        return null;
    return `${match[1]}px`;
}
function sanitizeURL(raw, options = {}) {
    var _a, _b, _c;
    if (raw == null)
        return null;
    const value = String(raw).trim();
    if (!value || UNSAFE_URL_CHARS.test(value))
        return null;
    if (value.startsWith("#")) {
        return options.allowFragments === false ? null : value;
    }
    if (options.allowFragments === false && value.includes("#"))
        return null;
    if (options.allowRelative !== false && isSafeRelativeURL(value)) {
        return value;
    }
    if (options.allowBareEmail && isBareEmail(value)) {
        return value;
    }
    if (/^data:/i.test(value)) {
        const maxLength = (_a = options.maxDataUrlLength) !== null && _a !== void 0 ? _a : 2000000;
        if (!options.allowDataUrls || value.length > maxLength)
            return null;
        return ((_b = options.dataUrlPattern) === null || _b === void 0 ? void 0 : _b.test(value)) ? value : null;
    }
    const protocols = normaliseProtocols((_c = options.protocols) !== null && _c !== void 0 ? _c : ["http", "https"]);
    try {
        const url = new URL(value);
        const protocol = url.protocol.replace(/:$/, "").toLowerCase();
        return protocols.has(protocol) ? value : null;
    }
    catch {
        return null;
    }
}
function sanitizeLinkTarget(raw) {
    if (raw == null)
        return null;
    const value = String(raw).trim();
    return SAFE_TARGETS.has(value) ? value : null;
}
function hardenLinkAttrs(attrs) {
    if (attrs.target === "_blank") {
        return { ...attrs, rel: "noopener noreferrer" };
    }
    const { rel: _rel, ...withoutRel } = attrs;
    return withoutRel;
}
function sanitizeCSSLength(raw, max, defaultUnit, allowPercent) {
    var _a;
    if (raw == null)
        return null;
    const value = String(raw).trim();
    const match = CSS_LENGTH_TOKEN.exec(value);
    if (!match)
        return null;
    if (!allowPercent && match[2] === "%")
        return null;
    const number = Number(match[1]);
    if (!Number.isFinite(number) || number <= 0 || number > max)
        return null;
    const unit = (_a = match[2]) !== null && _a !== void 0 ? _a : defaultUnit;
    return `${match[1]}${unit}`;
}
function isRGBColor(value) {
    const match = /^rgba?\((.*)\)$/i.exec(value);
    if (!match)
        return false;
    const parsed = parseFunctionalColor(match[1]);
    return Boolean(parsed &&
        parsed.channels.length === 3 &&
        parsed.channels.every((channel) => NUMBER_OR_PERCENT.test(channel)) &&
        (!parsed.alpha || NUMBER_OR_PERCENT.test(parsed.alpha)));
}
function isHSLColor(value) {
    const match = /^hsla?\((.*)\)$/i.exec(value);
    if (!match)
        return false;
    const parsed = parseFunctionalColor(match[1]);
    return Boolean(parsed &&
        parsed.channels.length === 3 &&
        HUE.test(parsed.channels[0]) &&
        PERCENT.test(parsed.channels[1]) &&
        PERCENT.test(parsed.channels[2]) &&
        (!parsed.alpha || NUMBER_OR_PERCENT.test(parsed.alpha)));
}
function parseFunctionalColor(body) {
    var _a, _b;
    if (/[^\d.%+\-/,\sA-Za-z]/.test(body))
        return null;
    if (body.includes(",")) {
        const parts = body.split(",").map((part) => part.trim());
        if (parts.length !== 3 && parts.length !== 4)
            return null;
        if (parts.some((part) => !part || part.includes("/")))
            return null;
        return {
            channels: parts.slice(0, 3),
            alpha: (_a = parts[3]) !== null && _a !== void 0 ? _a : null,
        };
    }
    const slashParts = body.split("/").map((part) => part.trim());
    if (slashParts.length > 2)
        return null;
    const channels = slashParts[0].split(/\s+/).filter(Boolean);
    const alpha = (_b = slashParts[1]) !== null && _b !== void 0 ? _b : null;
    if (channels.length !== 3)
        return null;
    if (alpha !== null && !alpha)
        return null;
    return { channels, alpha };
}
function isSafeRelativeURL(value) {
    if (value.startsWith("//"))
        return false;
    if (value.startsWith("/") || value.startsWith("?"))
        return true;
    if (value.includes(":"))
        return false;
    return /^[.\w~-][.\w~!$&'()*+,;=:@/-]*(?:\?[^#]*)?(?:#.*)?$/u.test(value);
}
function isBareEmail(value) {
    return /^[^\s@<>"']+@[^\s@<>"']+\.[^\s@<>"']+$/.test(value);
}
function normaliseProtocols(protocols) {
    return new Set(protocols
        .map((protocol) => protocol.trim().replace(/:$/, "").toLowerCase())
        .filter(Boolean));
}

},{}],
11:[function(require,module,exports){
"use strict";
/**
 * Block-level operations.
 *
 * Built on top of doc-utils. Lives in a separate module so the basic
 * mark/setBlockType helpers stay small and easy to read.
 *
 * All operations are immutable — they return a new document or null
 * if no change was needed.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.selectionBlockRange = selectionBlockRange;
exports.wrapBlocks = wrapBlocks;
exports.unwrapBlock = unwrapBlock;
exports.toggleWrap = toggleWrap;
exports.selectionInsideWrapper = selectionInsideWrapper;
exports.setBlockAttrs = setBlockAttrs;
exports.getBlockAttr = getBlockAttr;
const doc_utils_js_1 = require("./doc-utils.js");
const path_js_1 = require("./path.js");
/**
 * Index range of top-level blocks touched by the selection.
 * Returns [start, end] inclusive. Null when no selection.
 */
function selectionBlockRange(selection) {
    var _a, _b;
    if (!selection)
        return null;
    const a = (_a = selection.anchor.path[0]) !== null && _a !== void 0 ? _a : 0;
    const b = (_b = selection.head.path[0]) !== null && _b !== void 0 ? _b : a;
    return a <= b ? [a, b] : [b, a];
}
function samePath(a, b) {
    if (a.length !== b.length)
        return false;
    return a.every((value, index) => value === b[index]);
}
function selectionSiblingRange(selection) {
    if (!selection)
        return null;
    const anchorPath = selection.anchor.path;
    const headPath = selection.head.path;
    if (anchorPath.length === 0 || headPath.length === 0)
        return null;
    const anchorParent = anchorPath.slice(0, -1);
    const headParent = headPath.slice(0, -1);
    if (!samePath(anchorParent, headParent)) {
        const range = selectionBlockRange(selection);
        return range ? { parentPath: [], start: range[0], end: range[1] } : null;
    }
    const a = anchorPath[anchorPath.length - 1];
    const b = headPath[headPath.length - 1];
    return {
        parentPath: anchorParent,
        start: Math.min(a, b),
        end: Math.max(a, b),
    };
}
function replaceChildrenAt(doc, parentPath, fn) {
    const parent = (0, path_js_1.nodeAt)(doc, parentPath);
    if (!(parent === null || parent === void 0 ? void 0 : parent.content))
        return null;
    const newDoc = (0, doc_utils_js_1.cloneDoc)(doc);
    const nextDoc = (0, path_js_1.mapNodeAt)(newDoc, parentPath, (node) => {
        var _a;
        const children = (_a = node.content) !== null && _a !== void 0 ? _a : [];
        return { ...node, content: fn(children.slice()) };
    });
    return nextDoc;
}
function closestWrapperPath(doc, selection, wrapperType) {
    if (!selection)
        return null;
    const path = selection.anchor.path;
    for (let depth = path.length; depth >= 1; depth--) {
        const currentPath = path.slice(0, depth);
        const node = (0, path_js_1.nodeAt)(doc, currentPath);
        if ((node === null || node === void 0 ? void 0 : node.type) === wrapperType)
            return currentPath;
    }
    return null;
}
/**
 * Wrap each touched sibling block in a parent node of `wrapperType`.
 * If `itemType` is given, each block is first wrapped in `itemType`
 * (used for lists: paragraph → list_item → bullet_list).
 *
 * Consecutive wraps of the same `wrapperType` merge into one wrapper,
 * which is what users intuitively expect when they toggle "list" on
 * three adjacent paragraphs.
 *
 * Deep selections are scoped to their immediate parent container, so
 * toggling a list inside a blockquote, table cell or list item changes
 * that nested content instead of wrapping the top-level container.
 */
function wrapBlocks(doc, selection, wrapperType, itemType, wrapperAttrs) {
    let range = selectionSiblingRange(selection);
    if (!range) {
        // A real but unresolvable selection (malformed/empty paths) bails.
        // With no selection at all — common in headless usage and before
        // the caret is established — default to the first top-level block so
        // toggling a blockquote / list still does something predictable,
        // matching `setBlockType`'s no-selection behaviour.
        if (selection)
            return null;
        range = { parentPath: [], start: 0, end: 0 };
    }
    const { parentPath, start, end } = range;
    const parent = (0, path_js_1.nodeAt)(doc, parentPath);
    if (!(parent === null || parent === void 0 ? void 0 : parent.content))
        return null;
    const items = [];
    for (let i = start; i <= end; i++) {
        const block = parent.content[i];
        if (!block)
            continue;
        const clonedBlock = (0, doc_utils_js_1.cloneDoc)(block);
        if (itemType) {
            items.push({ type: itemType, content: [clonedBlock] });
        }
        else {
            items.push(clonedBlock);
        }
    }
    if (items.length === 0)
        return null;
    const wrapper = { type: wrapperType, content: items };
    if (wrapperAttrs)
        wrapper.attrs = { ...wrapperAttrs };
    return replaceChildrenAt(doc, parentPath, (children) => {
        children.splice(start, end - start + 1, wrapper);
        return children;
    });
}
/**
 * Unwrap a wrapper node at the given top-level index, hoisting its
 * children up to the document root. If `unwrapItemToo` is true, also
 * peel each child once more (used for lists: bullet_list > list_item
 * > paragraph → paragraph).
 */
function unwrapBlock(doc, blockIndex, unwrapItemToo = false) {
    return unwrapBlockAtPath(doc, [blockIndex], unwrapItemToo);
}
function unwrapBlockAtPath(doc, path, unwrapItemToo = false) {
    if (path.length === 0)
        return null;
    const wrapper = (0, path_js_1.nodeAt)(doc, path);
    if (!(wrapper === null || wrapper === void 0 ? void 0 : wrapper.content))
        return null;
    let hoisted = (0, doc_utils_js_1.cloneDoc)(wrapper.content);
    if (unwrapItemToo) {
        hoisted = hoisted.flatMap((item) => { var _a; return (_a = item.content) !== null && _a !== void 0 ? _a : []; });
    }
    if (hoisted.length === 0) {
        hoisted = [{ type: "paragraph", content: [] }];
    }
    const parentPath = path.slice(0, -1);
    const index = path[path.length - 1];
    return replaceChildrenAt(doc, parentPath, (children) => {
        if (!children[index])
            return children;
        children.splice(index, 1, ...hoisted);
        return children;
    });
}
/**
 * Toggle a wrapper around the selected blocks.
 *
 *  - If every touched block is already inside a wrapper of `wrapperType`,
 *    unwrap it (turning list back into paragraphs, etc.).
 *  - Otherwise, wrap the selection in a new wrapper of that type.
 *
 * For nested structures (list_item) caller passes `itemType`.
 */
function toggleWrap(doc, selection, wrapperType, itemType, wrapperAttrs) {
    const wrapperPath = closestWrapperPath(doc, selection, wrapperType);
    if (wrapperPath) {
        return unwrapBlockAtPath(doc, wrapperPath, Boolean(itemType));
    }
    return wrapBlocks(doc, selection, wrapperType, itemType, wrapperAttrs);
}
/**
 * Whether the selection lives inside a wrapper of `wrapperType`.
 * Used by toolbar buttons to compute `aria-pressed`.
 */
function selectionInsideWrapper(doc, selection, wrapperType) {
    var _a;
    if (!selection)
        return false;
    const path = selection.anchor.path;
    // Deep selections can sit several levels down (table → row → cell →
    // blockquote → paragraph). Walk the ancestor chain instead of only
    // checking the top-level block so wrapper-aware toolbar states and
    // toggles keep working in nested content.
    for (let depth = path.length; depth >= 1; depth--) {
        const node = (0, path_js_1.nodeAt)(doc, path.slice(0, depth));
        if ((node === null || node === void 0 ? void 0 : node.type) === wrapperType)
            return true;
    }
    const range = selectionBlockRange(selection);
    if (!range)
        return false;
    const [start] = range;
    return ((_a = doc.content[start]) === null || _a === void 0 ? void 0 : _a.type) === wrapperType;
}
/**
 * Merge a partial attrs object into every *leaf* block touched by the
 * selection. Existing attrs are preserved; only the listed keys are
 * overwritten. Passing `null` for a key removes it.
 *
 * Deep-aware: a selection inside a blockquote, table cell or list
 * item resolves to the leaf paragraph/heading there, not the
 * top-level container — so alignment, line height and indentation
 * actually land on the text the user sees.
 *
 * Used by extensions like text-align and line-height that decorate
 * existing blocks without replacing them.
 */
function setBlockAttrs(doc, selection, patch) {
    var _a;
    if (!selection)
        return null;
    const newDoc = (0, doc_utils_js_1.cloneDoc)(doc);
    const leaves = (0, doc_utils_js_1.leafBlocksInSelection)(newDoc, selection);
    if (leaves.length === 0)
        return null;
    let changed = false;
    for (const { node: block } of leaves) {
        const next = { ...((_a = block.attrs) !== null && _a !== void 0 ? _a : {}) };
        for (const [k, v] of Object.entries(patch)) {
            if (v === null || v === undefined) {
                delete next[k];
            }
            else {
                next[k] = v;
            }
        }
        if (Object.keys(next).length === 0) {
            delete block.attrs;
        }
        else {
            block.attrs = next;
        }
        changed = true;
    }
    return changed ? newDoc : null;
}
/**
 * Read a single attribute from the first *leaf* block touched by the
 * selection. Returns null if the selection or the attribute is absent.
 */
function getBlockAttr(doc, selection, key) {
    var _a, _b, _c;
    if (!selection)
        return null;
    const leaves = (0, doc_utils_js_1.leafBlocksInSelection)(doc, selection);
    return (_c = (_b = (_a = leaves[0]) === null || _a === void 0 ? void 0 : _a.node.attrs) === null || _b === void 0 ? void 0 : _b[key]) !== null && _c !== void 0 ? _c : null;
}

},{"./doc-utils.js":7,"./path.js":8}],
12:[function(require,module,exports){
"use strict";
/**
 * @smeditor/starter-kit
 *
 * The "batteries included" entry point — the MVP §20 toolset:
 * paragraphs, all six headings, bold/italic/underline/strike, link,
 * bullet/ordered/task lists, blockquote, code block, horizontal rule,
 * image, table, text alignment, hard breaks, clear-formatting, markdown
 * input shortcuts, an empty-state placeholder, and undo/redo.
 *
 * StarterKit is a bundle exposing `extensions: Extension[]`. Core's
 * `flattenExtensions` understands bundles, so it drops straight into
 * the `extensions` prop.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.HistoryExtension = exports.PlaceholderExtension = exports.MarkdownExtension = exports.TableExtension = exports.ImageExtension = exports.HorizontalRuleExtension = exports.ClearFormattingExtension = exports.TextAlignExtension = exports.TaskItemExtension = exports.TaskListExtension = exports.OrderedListExtension = exports.BulletListExtension = exports.ListItemExtension = exports.HardBreakExtension = exports.CodeBlockExtension = exports.BlockquoteExtension = exports.LinkExtension = exports.StrikeExtension = exports.UnderlineExtension = exports.ItalicExtension = exports.BoldExtension = exports.HeadingExtension = exports.ParagraphExtension = exports.StarterKit = void 0;
const extension_paragraph_1 = require("@smeditor/extension-paragraph");
Object.defineProperty(exports, "ParagraphExtension", { enumerable: true, get: function () { return extension_paragraph_1.ParagraphExtension; } });
const extension_heading_1 = require("@smeditor/extension-heading");
Object.defineProperty(exports, "HeadingExtension", { enumerable: true, get: function () { return extension_heading_1.HeadingExtension; } });
const extension_bold_1 = require("@smeditor/extension-bold");
Object.defineProperty(exports, "BoldExtension", { enumerable: true, get: function () { return extension_bold_1.BoldExtension; } });
const extension_italic_1 = require("@smeditor/extension-italic");
Object.defineProperty(exports, "ItalicExtension", { enumerable: true, get: function () { return extension_italic_1.ItalicExtension; } });
const extension_underline_1 = require("@smeditor/extension-underline");
Object.defineProperty(exports, "UnderlineExtension", { enumerable: true, get: function () { return extension_underline_1.UnderlineExtension; } });
const extension_strike_1 = require("@smeditor/extension-strike");
Object.defineProperty(exports, "StrikeExtension", { enumerable: true, get: function () { return extension_strike_1.StrikeExtension; } });
const extension_link_1 = require("@smeditor/extension-link");
Object.defineProperty(exports, "LinkExtension", { enumerable: true, get: function () { return extension_link_1.LinkExtension; } });
const extension_blockquote_1 = require("@smeditor/extension-blockquote");
Object.defineProperty(exports, "BlockquoteExtension", { enumerable: true, get: function () { return extension_blockquote_1.BlockquoteExtension; } });
const extension_code_block_1 = require("@smeditor/extension-code-block");
Object.defineProperty(exports, "CodeBlockExtension", { enumerable: true, get: function () { return extension_code_block_1.CodeBlockExtension; } });
const extension_hard_break_1 = require("@smeditor/extension-hard-break");
Object.defineProperty(exports, "HardBreakExtension", { enumerable: true, get: function () { return extension_hard_break_1.HardBreakExtension; } });
const extension_list_item_1 = require("@smeditor/extension-list-item");
Object.defineProperty(exports, "ListItemExtension", { enumerable: true, get: function () { return extension_list_item_1.ListItemExtension; } });
const extension_bullet_list_1 = require("@smeditor/extension-bullet-list");
Object.defineProperty(exports, "BulletListExtension", { enumerable: true, get: function () { return extension_bullet_list_1.BulletListExtension; } });
const extension_ordered_list_1 = require("@smeditor/extension-ordered-list");
Object.defineProperty(exports, "OrderedListExtension", { enumerable: true, get: function () { return extension_ordered_list_1.OrderedListExtension; } });
const extension_task_list_1 = require("@smeditor/extension-task-list");
Object.defineProperty(exports, "TaskListExtension", { enumerable: true, get: function () { return extension_task_list_1.TaskListExtension; } });
const extension_task_item_1 = require("@smeditor/extension-task-item");
Object.defineProperty(exports, "TaskItemExtension", { enumerable: true, get: function () { return extension_task_item_1.TaskItemExtension; } });
const extension_text_align_1 = require("@smeditor/extension-text-align");
Object.defineProperty(exports, "TextAlignExtension", { enumerable: true, get: function () { return extension_text_align_1.TextAlignExtension; } });
const extension_clear_formatting_1 = require("@smeditor/extension-clear-formatting");
Object.defineProperty(exports, "ClearFormattingExtension", { enumerable: true, get: function () { return extension_clear_formatting_1.ClearFormattingExtension; } });
const extension_horizontal_rule_1 = require("@smeditor/extension-horizontal-rule");
Object.defineProperty(exports, "HorizontalRuleExtension", { enumerable: true, get: function () { return extension_horizontal_rule_1.HorizontalRuleExtension; } });
const extension_image_1 = require("@smeditor/extension-image");
Object.defineProperty(exports, "ImageExtension", { enumerable: true, get: function () { return extension_image_1.ImageExtension; } });
const extension_table_1 = require("@smeditor/extension-table");
Object.defineProperty(exports, "TableExtension", { enumerable: true, get: function () { return extension_table_1.TableExtension; } });
const extension_markdown_1 = require("@smeditor/extension-markdown");
Object.defineProperty(exports, "MarkdownExtension", { enumerable: true, get: function () { return extension_markdown_1.MarkdownExtension; } });
const extension_placeholder_1 = require("@smeditor/extension-placeholder");
Object.defineProperty(exports, "PlaceholderExtension", { enumerable: true, get: function () { return extension_placeholder_1.PlaceholderExtension; } });
const extension_history_1 = require("@smeditor/extension-history");
Object.defineProperty(exports, "HistoryExtension", { enumerable: true, get: function () { return extension_history_1.HistoryExtension; } });
exports.StarterKit = {
    name: "starter-kit",
    extensions: [
        // Block nodes
        extension_paragraph_1.ParagraphExtension,
        extension_heading_1.HeadingExtension,
        extension_blockquote_1.BlockquoteExtension,
        extension_code_block_1.CodeBlockExtension,
        extension_list_item_1.ListItemExtension,
        extension_bullet_list_1.BulletListExtension,
        extension_ordered_list_1.OrderedListExtension,
        extension_task_item_1.TaskItemExtension,
        extension_task_list_1.TaskListExtension,
        extension_hard_break_1.HardBreakExtension,
        extension_horizontal_rule_1.HorizontalRuleExtension,
        extension_image_1.ImageExtension,
        extension_table_1.TableExtension,
        // Marks
        extension_bold_1.BoldExtension,
        extension_italic_1.ItalicExtension,
        extension_underline_1.UnderlineExtension,
        extension_strike_1.StrikeExtension,
        extension_link_1.LinkExtension,
        // Behaviour / commands
        extension_text_align_1.TextAlignExtension,
        extension_clear_formatting_1.ClearFormattingExtension,
        extension_markdown_1.MarkdownExtension,
        extension_placeholder_1.PlaceholderExtension,
        extension_history_1.HistoryExtension,
    ],
};
exports.default = exports.StarterKit;

},{"@smeditor/extension-paragraph":13,"@smeditor/extension-heading":14,"@smeditor/extension-bold":15,"@smeditor/extension-italic":16,"@smeditor/extension-underline":17,"@smeditor/extension-strike":18,"@smeditor/extension-link":19,"@smeditor/extension-blockquote":20,"@smeditor/extension-code-block":21,"@smeditor/extension-hard-break":22,"@smeditor/extension-list-item":23,"@smeditor/extension-bullet-list":24,"@smeditor/extension-ordered-list":25,"@smeditor/extension-task-list":26,"@smeditor/extension-task-item":27,"@smeditor/extension-text-align":28,"@smeditor/extension-clear-formatting":29,"@smeditor/extension-horizontal-rule":30,"@smeditor/extension-image":31,"@smeditor/extension-table":32,"@smeditor/extension-markdown":34,"@smeditor/extension-placeholder":37,"@smeditor/extension-history":38}],
13:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-paragraph
 *
 * The default block. Optional attributes, each set by its own
 * extension and harmless when that extension isn't loaded:
 *  - `textAlign`  — extension-text-align
 *  - `indent`     — extension-indent
 *  - `lineHeight` — extension-line-height
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.ParagraphExtension = void 0;
const core_1 = require("@smeditor/core");
const ALIGN_VALUES = new Set(["left", "center", "right", "justify"]);
function buildAttrs(node) {
    var _a, _b, _c, _d;
    const align = (_a = node.attrs) === null || _a === void 0 ? void 0 : _a.textAlign;
    const indent = Number((_c = (_b = node.attrs) === null || _b === void 0 ? void 0 : _b.indent) !== null && _c !== void 0 ? _c : 0);
    const lineHeight = (_d = node.attrs) === null || _d === void 0 ? void 0 : _d.lineHeight;
    const styles = [];
    const out = {};
    if (typeof align === "string" && ALIGN_VALUES.has(align) && align !== "left") {
        styles.push(`text-align: ${align}`);
    }
    if (typeof lineHeight === "string" && lineHeight) {
        styles.push(`line-height: ${lineHeight}`);
    }
    if (indent > 0) {
        out["data-indent"] = String(indent);
    }
    if (styles.length)
        out.style = styles.join("; ");
    return Object.keys(out).length ? out : null;
}
function readAttrs(el) {
    var _a;
    const style = (_a = el.getAttribute("style")) !== null && _a !== void 0 ? _a : "";
    const align = /text-align:\s*(left|center|right|justify)/i.exec(style);
    const lh = /line-height:\s*([^;]+)/i.exec(style);
    const indentRaw = el.getAttribute("data-indent");
    const indent = indentRaw ? Number(indentRaw) || null : null;
    const out = {};
    if (align)
        out.textAlign = align[1].toLowerCase();
    if (lh)
        out.lineHeight = lh[1].trim();
    if (indent)
        out.indent = indent;
    return Object.keys(out).length ? out : null;
}
function paragraphToDOM(node) {
    const attrs = buildAttrs(node);
    return attrs ? ["p", attrs, 0] : ["p", 0];
}
exports.ParagraphExtension = {
    name: "paragraph",
    nodes: [
        {
            name: "paragraph",
            group: "block",
            content: "inline*",
            attrs: {
                textAlign: { default: null },
                indent: { default: null },
                lineHeight: { default: null },
            },
            toDOM: paragraphToDOM,
            parseDOM: [{ tag: "p", getAttrs: readAttrs }],
        },
    ],
    commands: {
        setParagraph: () => (editor) => {
            const next = (0, core_1.setBlockType)(editor.getJSON(), editor.getSelection(), "paragraph");
            if (!next)
                return false;
            editor.dispatch({
                doc: next,
                selection: editor.getSelection(),
                addToHistory: true,
            });
            return true;
        },
    },
};
exports.default = exports.ParagraphExtension;

},{"@smeditor/core":1}],
14:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-heading
 *
 * H1-H6 sharing one node type with a `level` attribute, plus the
 * optional `textAlign`, `indent` and `lineHeight` attributes.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.HeadingExtension = void 0;
const core_1 = require("@smeditor/core");
const DEFAULT_LEVELS = [1, 2, 3, 4, 5, 6];
const ALIGN_VALUES = new Set(["left", "center", "right", "justify"]);
function buildAttrs(node) {
    var _a, _b, _c, _d;
    const align = (_a = node.attrs) === null || _a === void 0 ? void 0 : _a.textAlign;
    const indent = Number((_c = (_b = node.attrs) === null || _b === void 0 ? void 0 : _b.indent) !== null && _c !== void 0 ? _c : 0);
    const lineHeight = (_d = node.attrs) === null || _d === void 0 ? void 0 : _d.lineHeight;
    const styles = [];
    const out = {};
    if (typeof align === "string" && ALIGN_VALUES.has(align) && align !== "left") {
        styles.push(`text-align: ${align}`);
    }
    if (typeof lineHeight === "string" && lineHeight) {
        styles.push(`line-height: ${lineHeight}`);
    }
    if (indent > 0) {
        out["data-indent"] = String(indent);
    }
    if (styles.length)
        out.style = styles.join("; ");
    return Object.keys(out).length ? out : null;
}
function headingToDOM(node) {
    var _a, _b;
    const level = (_b = (_a = node.attrs) === null || _a === void 0 ? void 0 : _a.level) !== null && _b !== void 0 ? _b : 1;
    const tag = `h${clamp(level, 1, 6)}`;
    const attrs = buildAttrs(node);
    return attrs ? [tag, attrs, 0] : [tag, 0];
}
function makeExtension(options) {
    var _a;
    const levels = (_a = options.levels) !== null && _a !== void 0 ? _a : DEFAULT_LEVELS;
    return {
        name: "heading",
        nodes: [
            {
                name: "heading",
                group: "block",
                content: "inline*",
                attrs: {
                    level: { default: 1 },
                    textAlign: { default: null },
                    indent: { default: null },
                    lineHeight: { default: null },
                },
                toDOM: headingToDOM,
                parseDOM: levels.map((level) => ({
                    tag: `h${level}`,
                    getAttrs: (el) => {
                        var _a;
                        const style = (_a = el.getAttribute("style")) !== null && _a !== void 0 ? _a : "";
                        const align = /text-align:\s*(left|center|right|justify)/i.exec(style);
                        const lh = /line-height:\s*([^;]+)/i.exec(style);
                        const indentRaw = el.getAttribute("data-indent");
                        const indent = indentRaw ? Number(indentRaw) || null : null;
                        const out = { level };
                        if (align)
                            out.textAlign = align[1].toLowerCase();
                        if (lh)
                            out.lineHeight = lh[1].trim();
                        if (indent)
                            out.indent = indent;
                        return out;
                    },
                })),
            },
        ],
        commands: {
            setHeading: (opts = {}) => (editor) => {
                var _a;
                const level = clamp((_a = opts.level) !== null && _a !== void 0 ? _a : 1, 1, 6);
                const next = (0, core_1.setBlockType)(editor.getJSON(), editor.getSelection(), "heading", { level });
                if (!next)
                    return false;
                editor.dispatch({
                    doc: next,
                    selection: editor.getSelection(),
                    addToHistory: true,
                });
                return true;
            },
        },
        keyboardShortcuts: levels.reduce((acc, level) => {
            acc[`Mod-Alt-${level}`] = (editor) => {
                const fn = editor.commands.setHeading;
                return typeof fn === "function" ? fn({ level }) : false;
            };
            return acc;
        }, {}),
        configure(opts) {
            return makeExtension({ ...options, ...opts });
        },
    };
}
function clamp(n, min, max) {
    return Math.max(min, Math.min(max, n));
}
exports.HeadingExtension = makeExtension({});
exports.default = exports.HeadingExtension;

},{"@smeditor/core":1}],
15:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-bold
 *
 * Adds a "bold" mark to the schema, a `toggleBold` command, and the
 * Cmd/Ctrl-B keyboard shortcut.
 *
 * The HTML form is `<strong>` (semantic). On parse we also accept `<b>`
 * for paste compatibility from other editors and word processors.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.BoldExtension = void 0;
const core_1 = require("@smeditor/core");
exports.BoldExtension = {
    name: "bold",
    marks: [
        {
            name: "bold",
            inclusive: true,
            toDOM: () => ["strong", 0],
            parseDOM: [
                { tag: "strong" },
                { tag: "b" },
            ],
        },
    ],
    commands: {
        toggleBold: () => (editor) => {
            const next = (0, core_1.toggleMarkInDoc)(editor.getJSON(), editor.getSelection(), "bold");
            if (!next)
                return false;
            editor.dispatch({
                doc: next,
                selection: editor.getSelection(),
                addToHistory: true,
            });
            return true;
        },
    },
    keyboardShortcuts: {
        "Mod-b": (editor) => {
            const fn = editor.commands.toggleBold;
            return typeof fn === "function" ? fn() : false;
        },
    },
};
exports.default = exports.BoldExtension;

},{"@smeditor/core":1}],
16:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-italic
 *
 * Renders to `<em>`; parses both `<em>` and `<i>`.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.ItalicExtension = void 0;
const core_1 = require("@smeditor/core");
exports.ItalicExtension = {
    name: "italic",
    marks: [
        {
            name: "italic",
            inclusive: true,
            toDOM: () => ["em", 0],
            parseDOM: [
                { tag: "em" },
                { tag: "i" },
            ],
        },
    ],
    commands: {
        toggleItalic: () => (editor) => {
            const next = (0, core_1.toggleMarkInDoc)(editor.getJSON(), editor.getSelection(), "italic");
            if (!next)
                return false;
            editor.dispatch({
                doc: next,
                selection: editor.getSelection(),
                addToHistory: true,
            });
            return true;
        },
    },
    keyboardShortcuts: {
        "Mod-i": (editor) => {
            const fn = editor.commands.toggleItalic;
            return typeof fn === "function" ? fn() : false;
        },
    },
};
exports.default = exports.ItalicExtension;

},{"@smeditor/core":1}],
17:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-underline
 *
 * Renders to `<u>`; parses both `<u>` and the legacy CSS form
 * `<span style="text-decoration: underline">`. Shortcut: Mod-u.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.UnderlineExtension = void 0;
const core_1 = require("@smeditor/core");
exports.UnderlineExtension = {
    name: "underline",
    marks: [
        {
            name: "underline",
            inclusive: true,
            toDOM: () => ["u", 0],
            parseDOM: [
                { tag: "u" },
                {
                    // Catch <span style="text-decoration: underline"> from MS Word
                    // / Google Docs paste. We don't try to be exhaustive — common
                    // cases only.
                    tag: "span",
                    getAttrs: (el) => {
                        var _a;
                        const style = (_a = el.getAttribute("style")) !== null && _a !== void 0 ? _a : "";
                        return /text-decoration:\s*underline/i.test(style) ? null : false;
                    },
                },
            ],
        },
    ],
    commands: {
        toggleUnderline: () => (editor) => {
            const next = (0, core_1.toggleMarkInDoc)(editor.getJSON(), editor.getSelection(), "underline");
            if (!next)
                return false;
            editor.dispatch({
                doc: next,
                selection: editor.getSelection(),
                addToHistory: true,
            });
            return true;
        },
    },
    keyboardShortcuts: {
        "Mod-u": (editor) => {
            const fn = editor.commands.toggleUnderline;
            return typeof fn === "function" ? fn() : false;
        },
    },
};
exports.default = exports.UnderlineExtension;

},{"@smeditor/core":1}],
18:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-strike
 *
 * Strike-through mark. Renders to `<s>`; parses `<s>`, `<del>`,
 * `<strike>` (the obsolete HTML4 element), and inline
 * `text-decoration: line-through`. Shortcut: Mod-Shift-S.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.StrikeExtension = void 0;
const core_1 = require("@smeditor/core");
exports.StrikeExtension = {
    name: "strike",
    marks: [
        {
            name: "strike",
            inclusive: true,
            toDOM: () => ["s", 0],
            parseDOM: [
                { tag: "s" },
                { tag: "del" },
                { tag: "strike" },
                {
                    tag: "span",
                    getAttrs: (el) => {
                        var _a;
                        const style = (_a = el.getAttribute("style")) !== null && _a !== void 0 ? _a : "";
                        return /text-decoration:\s*line-through/i.test(style) ? null : false;
                    },
                },
            ],
        },
    ],
    commands: {
        toggleStrike: () => (editor) => {
            const next = (0, core_1.toggleMarkInDoc)(editor.getJSON(), editor.getSelection(), "strike");
            if (!next)
                return false;
            editor.dispatch({
                doc: next,
                selection: editor.getSelection(),
                addToHistory: true,
            });
            return true;
        },
    },
    keyboardShortcuts: {
        "Mod-Shift-s": (editor) => {
            const fn = editor.commands.toggleStrike;
            return typeof fn === "function" ? fn() : false;
        },
    },
};
exports.default = exports.StrikeExtension;

},{"@smeditor/core":1}],
19:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-link
 *
 * Hyperlink mark. Stores `href` (required) and optional `title` and
 * `target` attributes. Parses both `<a href="…">` and `<a href="…"
 * target="_blank">`.
 *
 * Security: href values are sanitized when commands run, when HTML is parsed
 * and again when JSON is serialized back to HTML. Apps that need a broader
 * allowlist can configure it via `LinkExtension.configure({ protocols: [...] })`.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.LinkExtension = void 0;
const core_1 = require("@smeditor/core");
const DEFAULTS = {
    protocols: ["http", "https", "mailto", "tel"],
    hardenExternal: true,
};
/**
 * Does any text node overlapping the visible range `[from, to)` already
 * carry a `link` mark? Used by the autolink rule to avoid re-linking a
 * URL that's already inside a link.
 */
function rangeHasLink(content, from, to) {
    var _a;
    let offset = 0;
    for (const node of content) {
        if (node.type !== "text" || typeof node.text !== "string")
            continue;
        const start = offset;
        const end = offset + node.text.length;
        offset = end;
        // Overlap with [from, to)?
        if (end <= from || start >= to)
            continue;
        if ((_a = node.marks) === null || _a === void 0 ? void 0 : _a.some((mark) => mark.type === "link"))
            return true;
    }
    return false;
}
function makeExtension(options) {
    const opts = { ...DEFAULTS, ...options };
    const sanitizeHref = (href) => (0, core_1.sanitizeURL)(href, {
        protocols: opts.protocols,
        allowBareEmail: true,
    });
    const linkAttrs = (params) => {
        const href = sanitizeHref(params.href);
        if (!href)
            return null;
        const attrs = { href };
        if (typeof params.title === "string" && params.title)
            attrs.title = params.title;
        const target = (0, core_1.sanitizeLinkTarget)(params.target);
        if (target)
            attrs.target = target;
        return attrs;
    };
    // Autolink: typing a bare URL followed by a space wraps the URL in a
    // link mark. The leading `(?:^|\s)` lets the URL sit either at the
    // start of the block or after whitespace; group 1 is the URL and
    // group 2 is the single trailing space that triggered the rule. Both
    // Tiptap and CKEditor offer this convenience.
    const autolinkRule = {
        match: /(?:^|\s)(https?:\/\/[^\s]+|www\.[^\s]+)(\s)$/,
        handler: (editor, m) => {
            var _a, _b;
            const sel = editor.getSelection();
            if (!sel)
                return false;
            const url = m[1];
            if (!url)
                return false;
            // The trailing space (m[2]) sits at caret - 1; the URL ends just
            // before it and starts m[1].length characters earlier.
            const caret = sel.anchor.offset;
            const urlEnd = caret - 1;
            const urlStart = urlEnd - url.length;
            if (urlStart < 0)
                return false;
            // `www.` URLs need an explicit scheme before they can be
            // sanitized; refuse anything sanitizeHref rejects (e.g. unsafe
            // protocols), leaving the typed text untouched.
            const candidate = url.startsWith("www.") ? `https://${url}` : url;
            const attrs = linkAttrs({ href: candidate });
            if (!attrs)
                return false;
            const path = sel.anchor.path;
            const doc = (0, core_1.cloneDoc)(editor.getJSON());
            const block = (0, core_1.nodeAt)(doc, path);
            if (!block)
                return false;
            // Don't re-link text that already carries a link mark.
            if (rangeHasLink((_a = block.content) !== null && _a !== void 0 ? _a : [], urlStart, urlEnd))
                return false;
            block.content = (0, core_1.setMarkOnInlineRange)((_b = block.content) !== null && _b !== void 0 ? _b : [], urlStart, urlEnd, { type: "link", attrs });
            // Keep the caret where it is (just after the trailing space). The
            // link mark is `inclusive: false`, so the space stays unlinked.
            editor.dispatch({
                doc,
                selection: {
                    anchor: { path, offset: caret },
                    head: { path, offset: caret },
                },
                addToHistory: true,
            });
            return true;
        },
    };
    return {
        name: "link",
        inputRules: [autolinkRule],
        marks: [
            {
                name: "link",
                // Links should NOT be inclusive — when the caret sits just past
                // a link and the user types, the new text should be unlinked.
                inclusive: false,
                attrs: {
                    href: {},
                    title: { default: null },
                    target: { default: null },
                },
                toDOM: (mark) => {
                    var _a;
                    const attrs = linkAttrs((_a = mark.attrs) !== null && _a !== void 0 ? _a : {});
                    if (!attrs)
                        return ["span", 0];
                    const htmlAttrs = { href: String(attrs.href) };
                    if (typeof attrs.title === "string")
                        htmlAttrs.title = attrs.title;
                    if (typeof attrs.target === "string")
                        htmlAttrs.target = attrs.target;
                    return [
                        "a",
                        opts.hardenExternal ? (0, core_1.hardenLinkAttrs)(htmlAttrs) : htmlAttrs,
                        0,
                    ];
                },
                parseDOM: [
                    {
                        tag: "a",
                        getAttrs: (el) => {
                            var _a, _b, _c;
                            const attrs = linkAttrs({
                                href: (_a = el.getAttribute("href")) !== null && _a !== void 0 ? _a : "",
                                title: (_b = el.getAttribute("title")) !== null && _b !== void 0 ? _b : undefined,
                                target: (_c = el.getAttribute("target")) !== null && _c !== void 0 ? _c : undefined,
                            });
                            return attrs !== null && attrs !== void 0 ? attrs : false;
                        },
                    },
                ],
            },
        ],
        commands: {
            /** setLink({ href, title?, target? }) — applies to the selected range. */
            setLink: (params) => (editor) => {
                const attrs = linkAttrs(params !== null && params !== void 0 ? params : {});
                if (!attrs)
                    return false;
                const selection = editor.getSelection();
                const next = (0, core_1.setMarkAcrossSelection)(editor.getJSON(), selection, {
                    type: "link",
                    attrs,
                });
                if (!next)
                    return false;
                editor.dispatch({ doc: next, selection, addToHistory: true });
                return true;
            },
            /** unsetLink() — remove the link mark from the selected range. */
            unsetLink: () => (editor) => {
                const selection = editor.getSelection();
                const next = (0, core_1.unsetMarkAcrossSelection)(editor.getJSON(), selection, "link");
                if (!next)
                    return false;
                editor.dispatch({ doc: next, selection, addToHistory: true });
                return true;
            },
        },
        keyboardShortcuts: {
            "Mod-k": (editor) => {
                // Prompt-based fallback. UI layers can replace this by binding
                // their own shortcut to setLink/unsetLink with a richer dialog.
                if (typeof window === "undefined")
                    return false;
                const current = window.prompt("Link URL", "https://");
                if (current == null || current === "") {
                    const fn = editor.commands.unsetLink;
                    return typeof fn === "function" ? fn() : false;
                }
                const fn = editor.commands.setLink;
                return typeof fn === "function" ? fn({ href: current }) : false;
            },
        },
        configure(o) {
            return makeExtension({ ...options, ...o });
        },
    };
}
exports.LinkExtension = makeExtension({});
exports.default = exports.LinkExtension;

},{"@smeditor/core":1}],
20:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-blockquote
 *
 * Wraps the selected block(s) in a <blockquote>. Toggleable: running
 * the command again on content already inside a blockquote unwraps it.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.BlockquoteExtension = void 0;
exports.isBlockquoteActive = isBlockquoteActive;
const core_1 = require("@smeditor/core");
exports.BlockquoteExtension = {
    name: "blockquote",
    nodes: [
        {
            name: "blockquote",
            group: "block",
            content: "block+",
            toDOM: () => ["blockquote", 0],
            parseDOM: [{ tag: "blockquote" }],
        },
    ],
    commands: {
        toggleBlockquote: () => (editor) => {
            const next = (0, core_1.toggleWrap)(editor.getJSON(), editor.getSelection(), "blockquote");
            if (!next)
                return false;
            editor.dispatch({
                doc: next,
                selection: editor.getSelection(),
                addToHistory: true,
            });
            return true;
        },
    },
    keyboardShortcuts: {
        "Mod-Shift-b": (editor) => {
            const fn = editor.commands.toggleBlockquote;
            return typeof fn === "function" ? fn() : false;
        },
    },
};
/**
 * Helper for toolbar buttons: is the selection inside a blockquote?
 * (Re-exported so consumers don't need to import core directly.)
 */
function isBlockquoteActive(editor) {
    return (0, core_1.selectionInsideWrapper)(editor.getJSON(), editor.getSelection(), "blockquote");
}
exports.default = exports.BlockquoteExtension;

},{"@smeditor/core":1}],
21:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-code-block
 *
 * Renders as <pre><code class="language-…">…</code></pre>. The language
 * attribute is preserved on round-trip; actual syntax highlighting is
 * out of scope (that's a separate extension).
 *
 * Content inside a code block is treated as plain text — marks like
 * bold or italic don't apply. (Marks aren't stripped at the schema
 * level here for simplicity; toolbar buttons should grey themselves
 * out when `editor.isActive('code_block')`.)
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.CodeBlockExtension = void 0;
const core_1 = require("@smeditor/core");
exports.CodeBlockExtension = {
    name: "code_block",
    nodes: [
        {
            name: "code_block",
            group: "block",
            content: "text*",
            attrs: { language: { default: null } },
            toDOM: (node) => {
                var _a;
                const lang = (_a = node.attrs) === null || _a === void 0 ? void 0 : _a.language;
                const codeAttrs = typeof lang === "string" && lang
                    ? { class: `language-${lang}` }
                    : {};
                // toDOM returns the outer element; nested <code> is added by
                // the serializer as raw HTML below. We use a single-tag
                // representation here and wrap inside `wrapHTML` at parse time.
                return ["pre", codeAttrs, 0];
            },
            parseDOM: [
                {
                    tag: "pre",
                    getAttrs: (el) => {
                        var _a, _b;
                        // Pull language off the inner <code> if present, fallback to
                        // the <pre> itself.
                        const code = el.querySelector("code");
                        const klass = (_b = (_a = code === null || code === void 0 ? void 0 : code.getAttribute("class")) !== null && _a !== void 0 ? _a : el.getAttribute("class")) !== null && _b !== void 0 ? _b : "";
                        const m = /language-([\w-]+)/.exec(klass);
                        return m ? { language: m[1] } : null;
                    },
                },
            ],
        },
    ],
    commands: {
        setCodeBlock: (opts = {}) => (editor) => {
            const next = (0, core_1.setBlockType)(editor.getJSON(), editor.getSelection(), "code_block", opts.language ? { language: opts.language } : undefined);
            if (!next)
                return false;
            editor.dispatch({
                doc: next,
                selection: editor.getSelection(),
                addToHistory: true,
            });
            return true;
        },
    },
};
exports.default = exports.CodeBlockExtension;

},{"@smeditor/core":1}],
22:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-hard-break
 *
 * Inline `<br/>` node. Without this extension, core's parser converts
 * `<br>` to a literal `\n` inside text (a soft break). With it,
 * `<br>` becomes a first-class inline node and survives round-trips.
 *
 * Keyboard: Shift-Enter inserts a hard break. Plain Enter is left
 * alone — that's the job of paragraph-splitting logic in a future
 * iteration.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.HardBreakExtension = void 0;
const core_1 = require("@smeditor/core");
exports.HardBreakExtension = {
    name: "hard_break",
    nodes: [
        {
            name: "hard_break",
            group: "inline",
            content: "none",
            toDOM: () => ["br"],
            parseDOM: [{ tag: "br" }],
        },
    ],
    commands: {
        insertHardBreak: () => (editor) => {
            var _a;
            const selection = editor.getSelection();
            if (!selection)
                return false;
            const doc = (0, core_1.cloneDoc)(editor.getJSON());
            const block = (0, core_1.nodeAt)(doc, selection.anchor.path);
            if (!block)
                return false;
            // Insert the break at the caret, splitting the inline run there,
            // instead of always appending at the end of the block (which put
            // every break in the wrong place and, being trailing, was then
            // dropped by the serializer on the next round-trip).
            block.content = (0, core_1.insertInlineAt)((_a = block.content) !== null && _a !== void 0 ? _a : [], selection.anchor.offset, [{ type: "hard_break" }]);
            editor.dispatch({
                doc,
                selection,
                addToHistory: true,
            });
            return true;
        },
    },
    keyboardShortcuts: {
        "Shift-Enter": (editor) => {
            const fn = editor.commands.insertHardBreak;
            return typeof fn === "function" ? fn() : false;
        },
    },
};
exports.default = exports.HardBreakExtension;

},{"@smeditor/core":1}],
23:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-list-item
 *
 * The `<li>` node, plus caret-aware list behaviour (§13):
 *
 *   - Enter in a list item splits it into a new item, carrying any
 *     text after the caret into the new one.
 *   - Enter in an empty list item lifts out of the list — the item
 *     becomes a paragraph after the list.
 *   - Backspace at the start of an empty item does the same.
 *
 * These rely on the deep selection model: the caret's path runs
 * through the list and item nodes, so the commands can locate the
 * item to split or lift. With `deepSelection` off the path is a
 * single top-level index, the commands no-op, and the browser's
 * native Enter handles things — so this is additive, never a
 * regression.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.ListItemExtension = void 0;
exports.splitListItem = splitListItem;
exports.liftListItem = liftListItem;
exports.sinkListItem = sinkListItem;
const core_1 = require("@smeditor/core");
const ITEM_TYPES = new Set(["list_item", "task_item"]);
/** Resolve the list-item context around the selection's anchor, if any. */
function listContext(doc, selection) {
    if (!selection)
        return null;
    const leafPath = selection.anchor.path;
    // Need at least [..., list, item, leaf].
    if (leafPath.length < 2)
        return null;
    const itemPath = leafPath.slice(0, -1);
    const item = (0, core_1.nodeAt)(doc, itemPath);
    if (!item || !ITEM_TYPES.has(item.type))
        return null;
    const leaf = (0, core_1.nodeAt)(doc, leafPath);
    if (!leaf)
        return null;
    return {
        itemPath,
        item,
        listPath: itemPath.slice(0, -1),
        itemIndex: itemPath[itemPath.length - 1],
        leafPath,
        leaf,
        offset: selection.anchor.offset,
    };
}
/** True if a leaf block has no visible text. */
function isEmptyLeaf(leaf) {
    var _a;
    const content = (_a = leaf.content) !== null && _a !== void 0 ? _a : [];
    return content.every((n) => { var _a; return n.type === "text" && !((_a = n.text) !== null && _a !== void 0 ? _a : ""); });
}
/**
 * Split the list item at the caret. Text before the caret stays in
 * the current item; text after moves to a new item inserted after
 * it. Returns the new document and the caret path (start of the new
 * item's leaf block), or null if the caret isn't in a list item.
 */
function splitListItem(doc, selection) {
    var _a, _b;
    const ctx = listContext(doc, selection);
    if (!ctx)
        return null;
    const [before, after] = (0, core_1.splitInlineAt)((_a = ctx.leaf.content) !== null && _a !== void 0 ? _a : [], ctx.offset);
    const list = (0, core_1.nodeAt)(doc, ctx.listPath);
    if (!(list === null || list === void 0 ? void 0 : list.content))
        return null;
    const items = list.content.slice();
    // The leaf's index within the item — blocks before it stay in the old
    // item, blocks after it move to the new one. A list item holds `block+`,
    // so it may have several children (multiple paragraphs, nested sub-lists).
    const leafIdx = ctx.leafPath[ctx.leafPath.length - 1];
    const itemBlocks = (_b = ctx.item.content) !== null && _b !== void 0 ? _b : [];
    const oldItem = {
        ...ctx.item,
        content: [...itemBlocks.slice(0, leafIdx), { ...ctx.leaf, content: before }],
    };
    const newItem = {
        type: ctx.item.type,
        content: [{ ...ctx.leaf, content: after }, ...itemBlocks.slice(leafIdx + 1)],
    };
    // A new task item starts unchecked.
    if (ctx.item.type === "task_item")
        newItem.attrs = { checked: false };
    items.splice(ctx.itemIndex, 1, oldItem, newItem);
    const newDoc = replaceNodeAt(doc, ctx.listPath, (l) => ({
        ...l,
        content: items,
    }));
    // Caret at the start of the new item's leaf block.
    const caret = [...ctx.listPath, ctx.itemIndex + 1, 0];
    return { doc: newDoc, caret };
}
/**
 * Lift the (empty) list item out of its list: the item is removed and
 * an empty paragraph is placed after the list, at the list's parent
 * level. If the list had only that item, the list is replaced by the
 * paragraph. Returns null if the caret isn't in a list item.
 */
function liftListItem(doc, selection) {
    var _a, _b;
    const ctx = listContext(doc, selection);
    if (!ctx)
        return null;
    // The list must itself sit inside something (doc, cell, blockquote).
    if (ctx.listPath.length < 1)
        return null;
    const listParentPath = ctx.listPath.slice(0, -1);
    const listIndex = ctx.listPath[ctx.listPath.length - 1];
    const list = (0, core_1.nodeAt)(doc, ctx.listPath);
    const remaining = ((_b = (_a = list === null || list === void 0 ? void 0 : list.content) === null || _a === void 0 ? void 0 : _a.length) !== null && _b !== void 0 ? _b : 0) - 1;
    const newDoc = replaceNodeAt(doc, listParentPath, (parent) => {
        var _a, _b;
        const children = ((_a = parent.content) !== null && _a !== void 0 ? _a : []).slice();
        const curList = children[listIndex];
        const items = ((_b = curList.content) !== null && _b !== void 0 ? _b : []).slice();
        items.splice(ctx.itemIndex, 1);
        const emptyPara = { type: "paragraph" };
        if (items.length === 0) {
            children.splice(listIndex, 1, emptyPara);
        }
        else {
            children.splice(listIndex, 1, { ...curList, content: items }, emptyPara);
        }
        return { ...parent, content: children };
    });
    // If the list survived, the paragraph follows it; else it took its place.
    const paraIndex = remaining > 0 ? listIndex + 1 : listIndex;
    return { doc: newDoc, caret: [...listParentPath, paraIndex] };
}
/**
 * Sink (nest) the current list item under the previous sibling, one
 * level deeper — the standard `Tab` behaviour. The item is appended to
 * a nested list inside the previous item: an existing trailing nested
 * list of the same kind is reused, otherwise a new one is created.
 * Returns null when the caret isn't in a list item or the item is the
 * first in its list (nothing to nest under).
 */
function sinkListItem(doc, selection) {
    var _a, _b, _c, _d;
    const ctx = listContext(doc, selection);
    if (!ctx)
        return null;
    if (ctx.itemIndex === 0)
        return null;
    const list = (0, core_1.nodeAt)(doc, ctx.listPath);
    if (!(list === null || list === void 0 ? void 0 : list.content))
        return null;
    const listType = list.type;
    const items = list.content.slice();
    const current = items[ctx.itemIndex];
    const prev = items[ctx.itemIndex - 1];
    if (!current || !prev)
        return null;
    const prevContent = ((_a = prev.content) !== null && _a !== void 0 ? _a : []).slice();
    const last = prevContent[prevContent.length - 1];
    let nestedIndex;
    let movedItemIndex;
    if (last && last.type === listType) {
        // Reuse the previous item's trailing nested list.
        const merged = {
            ...last,
            content: [...((_b = last.content) !== null && _b !== void 0 ? _b : []), current],
        };
        prevContent[prevContent.length - 1] = merged;
        nestedIndex = prevContent.length - 1;
        movedItemIndex = ((_d = (_c = merged.content) === null || _c === void 0 ? void 0 : _c.length) !== null && _d !== void 0 ? _d : 1) - 1;
    }
    else {
        prevContent.push({ type: listType, content: [current] });
        nestedIndex = prevContent.length - 1;
        movedItemIndex = 0;
    }
    const newPrev = { ...prev, content: prevContent };
    // Replace the previous item + current item with the merged previous item.
    items.splice(ctx.itemIndex - 1, 2, newPrev);
    const newDoc = replaceNodeAt(doc, ctx.listPath, (l) => ({
        ...l,
        content: items,
    }));
    // Caret stays in the same leaf, now nested one level deeper.
    const leafIdx = ctx.leafPath[ctx.leafPath.length - 1];
    const caret = [
        ...ctx.listPath,
        ctx.itemIndex - 1,
        nestedIndex,
        movedItemIndex,
        leafIdx,
    ];
    return { doc: newDoc, caret };
}
/** Immutable replace of the node at `path` (local copy of mapNodeAt). */
function replaceNodeAt(doc, path, fn) {
    if (path.length === 0)
        return fn(doc);
    const recurse = (node, depth) => {
        const idx = path[depth];
        const children = node.content;
        if (!children || children[idx] === undefined)
            return node;
        const next = children.slice();
        next[idx] =
            depth === path.length - 1
                ? fn(children[idx])
                : recurse(children[idx], depth + 1);
        return { ...node, content: next };
    };
    return recurse(doc, 0);
}
// ============================================================================
// Extension
// ============================================================================
function caretSelection(path) {
    return {
        anchor: { path, offset: 0 },
        head: { path, offset: 0 },
    };
}
exports.ListItemExtension = {
    name: "list_item",
    nodes: [
        {
            name: "list_item",
            group: "block",
            content: "block+",
            toDOM: () => ["li", 0],
            parseDOM: [{ tag: "li" }],
        },
    ],
    commands: {
        /** Split the current list item at the caret (Enter behaviour). */
        splitListItem: () => (editor) => {
            const result = splitListItem(editor.getJSON(), editor.getSelection());
            if (!result)
                return false;
            editor.dispatch({
                doc: result.doc,
                selection: caretSelection(result.caret),
                addToHistory: true,
            });
            return true;
        },
        /** Lift the current (empty) list item out of its list. */
        liftListItem: () => (editor) => {
            const result = liftListItem(editor.getJSON(), editor.getSelection());
            if (!result)
                return false;
            editor.dispatch({
                doc: result.doc,
                selection: caretSelection(result.caret),
                addToHistory: true,
            });
            return true;
        },
        /** Nest the current list item under the previous one (Tab behaviour). */
        sinkListItem: () => (editor) => {
            const result = sinkListItem(editor.getJSON(), editor.getSelection());
            if (!result)
                return false;
            editor.dispatch({
                doc: result.doc,
                selection: caretSelection(result.caret),
                addToHistory: true,
            });
            return true;
        },
    },
    keyboardShortcuts: {
        // Tab nests the current item under the previous one. When it can't
        // (first item / not in a list) it returns false so a later
        // extension — table cell navigation or block indent — handles Tab.
        Tab: (editor) => { var _a, _b, _c; return (_c = (_b = (_a = editor.commands).sinkListItem) === null || _b === void 0 ? void 0 : _b.call(_a)) !== null && _c !== void 0 ? _c : false; },
        Enter: (editor) => {
            var _a, _b, _c, _d, _e, _f;
            const ctx = listContext(editor.getJSON(), editor.getSelection());
            if (!ctx)
                return false;
            // An empty item lifts out; a non-empty one splits.
            if (isEmptyLeaf(ctx.leaf)) {
                return (_c = (_b = (_a = editor.commands).liftListItem) === null || _b === void 0 ? void 0 : _b.call(_a)) !== null && _c !== void 0 ? _c : false;
            }
            return (_f = (_e = (_d = editor.commands).splitListItem) === null || _e === void 0 ? void 0 : _e.call(_d)) !== null && _f !== void 0 ? _f : false;
        },
        Backspace: (editor) => {
            var _a, _b, _c;
            const ctx = listContext(editor.getJSON(), editor.getSelection());
            // Only act at the very start of an empty item — otherwise let
            // the browser delete a character normally.
            if (!ctx || ctx.offset !== 0 || !isEmptyLeaf(ctx.leaf))
                return false;
            return (_c = (_b = (_a = editor.commands).liftListItem) === null || _b === void 0 ? void 0 : _b.call(_a)) !== null && _c !== void 0 ? _c : false;
        },
    },
};
exports.default = exports.ListItemExtension;

},{"@smeditor/core":1}],
24:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-bullet-list
 *
 * `<ul>` wrapper. Requires `@smeditor/extension-list-item` to be
 * loaded as well (StarterKit handles that for you).
 *
 * `toggleBulletList()` wraps the selected blocks. If they're already
 * inside a bullet list, it unwraps them.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.BulletListExtension = void 0;
exports.isBulletListActive = isBulletListActive;
const core_1 = require("@smeditor/core");
exports.BulletListExtension = {
    name: "bullet_list",
    nodes: [
        {
            name: "bullet_list",
            group: "block",
            content: "list_item+",
            toDOM: () => ["ul", 0],
            parseDOM: [{ tag: "ul" }],
        },
    ],
    commands: {
        toggleBulletList: () => (editor) => {
            const next = (0, core_1.toggleWrap)(editor.getJSON(), editor.getSelection(), "bullet_list", "list_item");
            if (!next)
                return false;
            editor.dispatch({
                doc: next,
                selection: editor.getSelection(),
                addToHistory: true,
            });
            return true;
        },
    },
    keyboardShortcuts: {
        "Mod-Shift-8": (editor) => {
            const fn = editor.commands.toggleBulletList;
            return typeof fn === "function" ? fn() : false;
        },
    },
};
function isBulletListActive(editor) {
    return (0, core_1.selectionInsideWrapper)(editor.getJSON(), editor.getSelection(), "bullet_list");
}
exports.default = exports.BulletListExtension;

},{"@smeditor/core":1}],
25:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-ordered-list
 *
 * `<ol>` counterpart of bullet-list. Same toggle semantics, same
 * dependency on `@smeditor/extension-list-item`. A future iteration
 * will add a `start` attribute for lists that don't begin at 1.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.OrderedListExtension = void 0;
exports.isOrderedListActive = isOrderedListActive;
const core_1 = require("@smeditor/core");
exports.OrderedListExtension = {
    name: "ordered_list",
    nodes: [
        {
            name: "ordered_list",
            group: "block",
            content: "list_item+",
            attrs: { start: { default: 1 } },
            toDOM: (node) => {
                var _a;
                const start = (_a = node.attrs) === null || _a === void 0 ? void 0 : _a.start;
                const attrs = {};
                if (typeof start === "number" && start !== 1)
                    attrs.start = start;
                return Object.keys(attrs).length > 0 ? ["ol", attrs, 0] : ["ol", 0];
            },
            parseDOM: [
                {
                    tag: "ol",
                    getAttrs: (el) => {
                        const start = Number(el.getAttribute("start"));
                        return Number.isInteger(start) && start > 0
                            ? { start }
                            : null;
                    },
                },
            ],
        },
    ],
    commands: {
        toggleOrderedList: () => (editor) => {
            const next = (0, core_1.toggleWrap)(editor.getJSON(), editor.getSelection(), "ordered_list", "list_item");
            if (!next)
                return false;
            editor.dispatch({
                doc: next,
                selection: editor.getSelection(),
                addToHistory: true,
            });
            return true;
        },
    },
    keyboardShortcuts: {
        "Mod-Shift-7": (editor) => {
            const fn = editor.commands.toggleOrderedList;
            return typeof fn === "function" ? fn() : false;
        },
    },
};
function isOrderedListActive(editor) {
    return (0, core_1.selectionInsideWrapper)(editor.getJSON(), editor.getSelection(), "ordered_list");
}
exports.default = exports.OrderedListExtension;

},{"@smeditor/core":1}],
26:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-task-list
 *
 * `<ul class="smeditor-task-list">` container for `task_item` nodes.
 * Requires `@smeditor/extension-task-item` for the actual rows.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.TaskListExtension = void 0;
exports.isTaskListActive = isTaskListActive;
const core_1 = require("@smeditor/core");
exports.TaskListExtension = {
    name: "task_list",
    nodes: [
        {
            name: "task_list",
            group: "block",
            content: "task_item+",
            toDOM: () => ["ul", { class: "smeditor-task-list" }, 0],
            parseDOM: [
                {
                    tag: "ul",
                    getAttrs: (el) => {
                        var _a;
                        const cls = (_a = el.getAttribute("class")) !== null && _a !== void 0 ? _a : "";
                        return /smeditor-task-list|task-list/i.test(cls) ? {} : false;
                    },
                },
            ],
        },
    ],
    commands: {
        toggleTaskList: () => (editor) => {
            const next = (0, core_1.toggleWrap)(editor.getJSON(), editor.getSelection(), "task_list", "task_item");
            if (!next)
                return false;
            editor.dispatch({
                doc: next,
                selection: editor.getSelection(),
                addToHistory: true,
            });
            return true;
        },
    },
    keyboardShortcuts: {
        "Mod-Shift-9": (editor) => { var _a, _b, _c; return (_c = (_b = (_a = editor.commands).toggleTaskList) === null || _b === void 0 ? void 0 : _b.call(_a)) !== null && _c !== void 0 ? _c : false; },
    },
};
function isTaskListActive(editor) {
    return (0, core_1.selectionInsideWrapper)(editor.getJSON(), editor.getSelection(), "task_list");
}
exports.default = exports.TaskListExtension;

},{"@smeditor/core":1}],
27:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-task-item
 *
 * Checklist item with a `checked` boolean attribute. Pairs with
 * `@smeditor/extension-task-list` (the `<ul class="task-list">`
 * wrapper).
 *
 * Rendering note: we mark the `<li>` with `data-checked` so CSS can
 * draw a check-box pseudo-element. The default theme handles this.
 * For full interactive toggling (clicking the box flips checked)
 * we need a click handler on the editor surface, which the theme
 * registers via plain `pointerdown` listeners on the editor element —
 * that lives in the editor host, not in this extension.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.TaskItemExtension = void 0;
const core_1 = require("@smeditor/core");
exports.TaskItemExtension = {
    name: "task_item",
    nodes: [
        {
            name: "task_item",
            group: "block",
            content: "block+",
            attrs: { checked: { default: false } },
            toDOM: (node) => {
                var _a;
                const checked = Boolean((_a = node.attrs) === null || _a === void 0 ? void 0 : _a.checked);
                return [
                    "li",
                    {
                        class: `smeditor-task-item${checked ? " is-checked" : ""}`,
                        "data-checked": checked ? "true" : "false",
                    },
                    0,
                ];
            },
            parseDOM: [
                {
                    tag: "li",
                    getAttrs: (el) => {
                        var _a;
                        const cls = (_a = el.getAttribute("class")) !== null && _a !== void 0 ? _a : "";
                        if (!/smeditor-task-item|task-item/i.test(cls))
                            return false;
                        const checked = el.getAttribute("data-checked") === "true";
                        return { checked };
                    },
                },
            ],
        },
    ],
    commands: {
        /**
         * Toggle the `checked` attribute on the task_item under the
         * current selection.
         */
        toggleTaskChecked: () => (editor) => {
            var _a, _b;
            const sel = editor.getSelection();
            if (!sel)
                return false;
            const doc = (0, core_1.cloneDoc)(editor.getJSON());
            // Walk to find the nearest task_item ancestor of the selection.
            // Our selection paths are top-level-indexed; we recurse to find
            // the deepest task_item that wraps it.
            const path = sel.anchor.path;
            let node = doc;
            let target;
            for (const idx of path) {
                if (!(node === null || node === void 0 ? void 0 : node.content))
                    break;
                node = node.content[idx];
                if ((node === null || node === void 0 ? void 0 : node.type) === "task_item")
                    target = node;
            }
            if (!target)
                return false;
            target.attrs = {
                ...((_a = target.attrs) !== null && _a !== void 0 ? _a : {}),
                checked: !((_b = target.attrs) === null || _b === void 0 ? void 0 : _b.checked),
            };
            editor.dispatch({ doc, selection: sel, addToHistory: true });
            return true;
        },
    },
};
exports.default = exports.TaskItemExtension;

},{"@smeditor/core":1}],
28:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-text-align
 *
 * Sets a `textAlign` attribute on the block(s) under the selection.
 * Works with paragraph, heading, blockquote — any block that opts in
 * by accepting a `textAlign` attribute on its NodeSpec.
 *
 * Updated paragraph and heading extensions in this iteration declare
 * the attribute, and their `toDOM` emits `style="text-align: …"` so
 * the alignment survives the HTML round-trip without needing a custom
 * serializer.
 *
 * Allowed values: "left", "center", "right", "justify". An empty
 * value (or `setTextAlign(null)`) clears the attribute.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.TextAlignExtension = void 0;
exports.isTextAlignActive = isTextAlignActive;
const core_1 = require("@smeditor/core");
const VALID = new Set([
    "left",
    "center",
    "right",
    "justify",
]);
exports.TextAlignExtension = {
    name: "text_align",
    commands: {
        /**
         * setTextAlign({ align: "center" }) — applies to every block the
         * selection touches. Pass `{ align: null }` to clear.
         */
        setTextAlign: (params) => (editor) => {
            var _a, _b, _c, _d;
            const selection = editor.getSelection();
            if (!selection)
                return false;
            const align = (_a = params === null || params === void 0 ? void 0 : params.align) !== null && _a !== void 0 ? _a : null;
            if (align !== null && !VALID.has(align))
                return false;
            const doc = (0, core_1.cloneDoc)(editor.getJSON());
            // Resolve through leafBlocksInSelection so a deep selection
            // aligns the leaf blocks inside table cells, not just the top
            // level.
            const leaves = (0, core_1.leafBlocksInSelection)(doc, selection);
            if (leaves.length === 0)
                return false;
            let changed = false;
            for (const { node: block } of leaves) {
                const currentAlign = ((_c = (_b = block.attrs) === null || _b === void 0 ? void 0 : _b.textAlign) !== null && _c !== void 0 ? _c : null);
                if (currentAlign === align)
                    continue;
                if (align === null) {
                    if (block.attrs) {
                        const { textAlign, ...rest } = block.attrs;
                        if (Object.keys(rest).length === 0)
                            delete block.attrs;
                        else
                            block.attrs = rest;
                    }
                }
                else {
                    block.attrs = { ...((_d = block.attrs) !== null && _d !== void 0 ? _d : {}), textAlign: align };
                }
                changed = true;
            }
            if (!changed)
                return false;
            editor.dispatch({
                doc,
                selection,
                addToHistory: true,
            });
            return true;
        },
    },
    keyboardShortcuts: {
        "Mod-Shift-l": (editor) => { var _a, _b, _c; return (_c = (_b = (_a = editor.commands).setTextAlign) === null || _b === void 0 ? void 0 : _b.call(_a, { align: "left" })) !== null && _c !== void 0 ? _c : false; },
        "Mod-Shift-e": (editor) => { var _a, _b, _c; return (_c = (_b = (_a = editor.commands).setTextAlign) === null || _b === void 0 ? void 0 : _b.call(_a, { align: "center" })) !== null && _c !== void 0 ? _c : false; },
        "Mod-Shift-r": (editor) => { var _a, _b, _c; return (_c = (_b = (_a = editor.commands).setTextAlign) === null || _b === void 0 ? void 0 : _b.call(_a, { align: "right" })) !== null && _c !== void 0 ? _c : false; },
        "Mod-Shift-j": (editor) => { var _a, _b, _c; return (_c = (_b = (_a = editor.commands).setTextAlign) === null || _b === void 0 ? void 0 : _b.call(_a, { align: "justify" })) !== null && _c !== void 0 ? _c : false; },
    },
};
/**
 * Helper for toolbar buttons. Walks the selection path so a quote /
 * cell / list item resolves to the leaf paragraph and the dropdown
 * shows the right active state inside containers.
 */
function isTextAlignActive(editor, align) {
    var _a, _b, _c;
    const sel = editor.getSelection();
    if (!sel)
        return false;
    let node = editor.getJSON();
    let leaf = null;
    for (const idx of sel.anchor.path) {
        const child = (_a = node.content) === null || _a === void 0 ? void 0 : _a[idx];
        if (!child)
            break;
        leaf = child;
        node = child;
    }
    const current = ((_c = (_b = leaf === null || leaf === void 0 ? void 0 : leaf.attrs) === null || _b === void 0 ? void 0 : _b.textAlign) !== null && _c !== void 0 ? _c : "left");
    return current === align;
}
exports.default = exports.TextAlignExtension;

},{"@smeditor/core":1}],
29:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-clear-formatting
 *
 * The "eraser" button. Strips every mark from the text inside the
 * selected range (or the whole block, when the caret is collapsed),
 * reaching into table cells / list items / blockquotes through the
 * deep selection path. Does **not** touch block types — call
 * `setParagraph` separately if you also want to reset the block.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.ClearFormattingExtension = void 0;
const core_1 = require("@smeditor/core");
exports.ClearFormattingExtension = {
    name: "clear_formatting",
    commands: {
        clearFormatting: () => (editor) => {
            const selection = editor.getSelection();
            const next = (0, core_1.clearMarksAcrossSelection)(editor.getJSON(), selection);
            if (!next)
                return false;
            editor.dispatch({ doc: next, selection, addToHistory: true });
            return true;
        },
    },
    keyboardShortcuts: {
        "Mod-\\": (editor) => { var _a, _b, _c; return (_c = (_b = (_a = editor.commands).clearFormatting) === null || _b === void 0 ? void 0 : _b.call(_a)) !== null && _c !== void 0 ? _c : false; },
    },
};
exports.default = exports.ClearFormattingExtension;

},{"@smeditor/core":1}],
30:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-horizontal-rule
 *
 * `<hr>` divider as a self-contained block node. The block has no
 * content, only a presence — its job is purely visual separation.
 *
 * Inserted with `editor.commands.insertHorizontalRule()`. Replaces
 * the current empty block, or inserts a new block after the current
 * one and an empty paragraph after that for the caret to land in.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.HorizontalRuleExtension = void 0;
const core_1 = require("@smeditor/core");
exports.HorizontalRuleExtension = {
    name: "horizontal_rule",
    nodes: [
        {
            name: "horizontal_rule",
            group: "block",
            content: "",
            // `atom` would be ideal here — caret can't enter the node — but
            // we don't currently have an atom flag in the NodeSpec; the
            // editor treats this gracefully because the node has no content
            // and we always insert a paragraph after it.
            toDOM: () => ["hr"],
            parseDOM: [{ tag: "hr" }],
        },
    ],
    commands: {
        insertHorizontalRule: () => (editor) => {
            var _a, _b;
            const sel = editor.getSelection();
            if (!sel)
                return false;
            const doc = (0, core_1.cloneDoc)(editor.getJSON());
            const idx = (_a = sel.anchor.path[0]) !== null && _a !== void 0 ? _a : 0;
            const hr = { type: "horizontal_rule" };
            const trailingParagraph = { type: "paragraph" };
            const current = doc.content[idx];
            const currentIsEmpty = current &&
                (!current.content || current.content.length === 0 ||
                    (current.content.length === 1 &&
                        current.content[0].type === "text" &&
                        ((_b = current.content[0].text) !== null && _b !== void 0 ? _b : "") === ""));
            if (currentIsEmpty) {
                // Replace the empty block with hr + new paragraph.
                doc.content.splice(idx, 1, hr, trailingParagraph);
            }
            else {
                // Insert after the current block.
                doc.content.splice(idx + 1, 0, hr, trailingParagraph);
            }
            editor.dispatch({
                doc,
                selection: {
                    anchor: { path: [idx + (currentIsEmpty ? 1 : 2)], offset: 0 },
                    head: { path: [idx + (currentIsEmpty ? 1 : 2)], offset: 0 },
                },
                addToHistory: true,
            });
            return true;
        },
    },
};
exports.default = exports.HorizontalRuleExtension;

},{"@smeditor/core":1}],
31:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-image
 *
 * Block-level image node. Covers the MVP slice of §9 of the brief:
 * insert by URL, insert via extension-configured upload handler, paste/drop
 * image files from the React surface, alt/title metadata, dimensions and
 * left / center / right alignment.
 *
 * The upload itself remains the host app's job. Configure the extension with
 * `ImageExtension.configure({ upload })`; SMEditor only calls the handler and
 * inserts the returned safe URL.
 *
 * Deliberately *not* in this version: drag-resize handles, captions as editable
 * child nodes, inline (text-flow) images, embeds. Those need a resize-handle UI
 * and the figure/figcaption node pair — scheduled for a later iteration.
 *
 * The node is "void": it has no editable content, only attributes.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.ImageExtension = void 0;
const core_1 = require("@smeditor/core");
const ALIGN_VALUES = new Set(["left", "center", "right"]);
const DEFAULT_PROTOCOLS = ["http", "https"];
const DEFAULT_MAX_DATA_URL_LENGTH = 2000000;
const RASTER_DATA_URL = /^data:image\/(?:png|jpe?g|gif|webp|avif|bmp);base64,[a-z0-9+/=\s]+$/i;
function makeExtension(options = {}) {
    var _a, _b;
    const protocols = normaliseProtocols((_a = options.protocols) !== null && _a !== void 0 ? _a : DEFAULT_PROTOCOLS);
    const allowDataUrls = (_b = options.allowDataUrls) !== null && _b !== void 0 ? _b : true;
    const maxDataUrlLength = typeof options.maxDataUrlLength === "number" && options.maxDataUrlLength > 0
        ? options.maxDataUrlLength
        : DEFAULT_MAX_DATA_URL_LENGTH;
    const sanitizeSrc = (raw) => (0, core_1.sanitizeURL)(raw, {
        protocols: [...protocols],
        allowFragments: false,
        allowDataUrls,
        dataUrlPattern: RASTER_DATA_URL,
        maxDataUrlLength,
    });
    const normaliseAttrs = (attrs) => {
        const src = sanitizeSrc(attrs.src);
        if (!src)
            return null;
        const out = { src };
        if (typeof attrs.alt === "string" && attrs.alt)
            out.alt = attrs.alt;
        if (typeof attrs.title === "string" && attrs.title)
            out.title = attrs.title;
        if (attrs.align && ALIGN_VALUES.has(attrs.align))
            out.align = attrs.align;
        const width = sanitizeDimension(attrs.width);
        if (width !== null)
            out.width = width;
        const height = sanitizeDimension(attrs.height);
        if (height !== null)
            out.height = height;
        if (typeof attrs.caption === "string" && attrs.caption.trim()) {
            out.caption = attrs.caption.trim();
        }
        return out;
    };
    const imageToDOM = (node) => {
        var _a, _b;
        const attrs = normaliseAttrs(((_a = node.attrs) !== null && _a !== void 0 ? _a : {}));
        const imgAttrs = { class: "smeditor-image" };
        if (attrs) {
            imgAttrs.src = attrs.src;
            if (attrs.alt)
                imgAttrs.alt = attrs.alt;
            if (attrs.title)
                imgAttrs.title = attrs.title;
            if (attrs.width)
                imgAttrs.width = String(attrs.width);
            if (attrs.height)
                imgAttrs.height = String(attrs.height);
        }
        const align = attrs === null || attrs === void 0 ? void 0 : attrs.align;
        const caption = (_b = attrs === null || attrs === void 0 ? void 0 : attrs.caption) !== null && _b !== void 0 ? _b : "";
        if (caption) {
            const figAttrs = { class: "smeditor-figure" };
            if (align)
                figAttrs["data-align"] = align;
            return [
                "figure",
                figAttrs,
                ["img", imgAttrs],
                ["figcaption", { class: "smeditor-figcaption" }, caption],
            ];
        }
        if (align)
            imgAttrs["data-align"] = align;
        return ["img", imgAttrs];
    };
    const imgAttrsFrom = (el) => {
        var _a, _b, _c;
        const attrs = normaliseAttrs({
            src: (_a = el.getAttribute("src")) !== null && _a !== void 0 ? _a : "",
            alt: (_b = el.getAttribute("alt")) !== null && _b !== void 0 ? _b : undefined,
            title: (_c = el.getAttribute("title")) !== null && _c !== void 0 ? _c : undefined,
            align: readAlign(el.getAttribute("data-align")),
            width: readDimension(el.getAttribute("width")),
            height: readDimension(el.getAttribute("height")),
        });
        return attrs ? { ...attrs } : false;
    };
    const commands = {
        /**
         * insertImage({ src, alt?, title?, width?, height?, align? }) — inserts an
         * image block. If the current block is empty it's replaced; otherwise the
         * image goes right after it, followed by an empty paragraph for the caret.
         */
        insertImage: (opts) => (editor) => {
            var _a, _b;
            const attrs = normaliseAttrs(opts !== null && opts !== void 0 ? opts : {});
            if (!attrs)
                return false;
            const sel = editor.getSelection();
            const idx = (_a = sel === null || sel === void 0 ? void 0 : sel.anchor.path[0]) !== null && _a !== void 0 ? _a : 0;
            const doc = (0, core_1.cloneDoc)(editor.getJSON());
            const imageNode = { type: "image", attrs: { ...attrs } };
            const trailing = { type: "paragraph" };
            const current = doc.content[idx];
            const currentEmpty = current &&
                (!current.content ||
                    current.content.length === 0 ||
                    (current.content.length === 1 &&
                        current.content[0].type === "text" &&
                        ((_b = current.content[0].text) !== null && _b !== void 0 ? _b : "") === ""));
            let caretIdx;
            if (currentEmpty) {
                doc.content.splice(idx, 1, imageNode, trailing);
                caretIdx = idx + 1;
            }
            else {
                doc.content.splice(idx + 1, 0, imageNode, trailing);
                caretIdx = idx + 2;
            }
            editor.dispatch({
                doc,
                selection: {
                    anchor: { path: [caretIdx], offset: 0 },
                    head: { path: [caretIdx], offset: 0 },
                },
                addToHistory: true,
            });
            return true;
        },
        /** setImageAlign({ align }) — re-aligns the image under the caret. */
        setImageAlign: (opts) => (editor) => {
            var _a;
            const align = readAlign(opts === null || opts === void 0 ? void 0 : opts.align);
            if (!align)
                return false;
            const sel = editor.getSelection();
            if (!sel)
                return false;
            const doc = (0, core_1.cloneDoc)(editor.getJSON());
            const block = (0, core_1.nodeAt)(doc, sel.anchor.path);
            if (!block || block.type !== "image")
                return false;
            block.attrs = { ...((_a = block.attrs) !== null && _a !== void 0 ? _a : {}), align };
            editor.dispatch({ doc, selection: sel, addToHistory: true });
            return true;
        },
        /** updateImageAttrs({ alt?, title?, src?, width?, height?, caption? }) — edit image metadata. */
        updateImageAttrs: (opts) => (editor) => {
            var _a;
            const sel = editor.getSelection();
            if (!sel)
                return false;
            const doc = (0, core_1.cloneDoc)(editor.getJSON());
            const block = (0, core_1.nodeAt)(doc, sel.anchor.path);
            if (!block || block.type !== "image")
                return false;
            const attrs = { ...((_a = block.attrs) !== null && _a !== void 0 ? _a : {}) };
            if (opts.alt !== undefined)
                attrs.alt = opts.alt;
            if (opts.title !== undefined)
                attrs.title = opts.title;
            if (opts.src !== undefined) {
                const src = sanitizeSrc(opts.src);
                if (!src)
                    return false;
                attrs.src = src;
            }
            if (opts.align !== undefined) {
                const align = readAlign(opts.align);
                if (align)
                    attrs.align = align;
            }
            if (opts.width !== undefined) {
                applyDimensionAttr(attrs, "width", opts.width);
            }
            if (opts.height !== undefined) {
                applyDimensionAttr(attrs, "height", opts.height);
            }
            if (opts.caption !== undefined) {
                const text = opts.caption ? String(opts.caption).trim() : "";
                if (text)
                    attrs.caption = text;
                else
                    delete attrs.caption;
            }
            block.attrs = attrs;
            editor.dispatch({ doc, selection: sel, addToHistory: true });
            return true;
        },
        /** removeImage() — deletes the image block under the caret. */
        removeImage: () => (editor) => {
            var _a;
            const sel = editor.getSelection();
            if (!sel)
                return false;
            const idx = (_a = sel.anchor.path[0]) !== null && _a !== void 0 ? _a : 0;
            const doc = (0, core_1.cloneDoc)(editor.getJSON());
            const block = doc.content[idx];
            if (!block || block.type !== "image")
                return false;
            doc.content.splice(idx, 1);
            if (doc.content.length === 0) {
                doc.content.push({ type: "paragraph" });
            }
            const caretIdx = Math.max(0, idx - 1);
            editor.dispatch({
                doc,
                selection: {
                    anchor: { path: [caretIdx], offset: 0 },
                    head: { path: [caretIdx], offset: 0 },
                },
                addToHistory: true,
            });
            return true;
        },
    };
    if (options.upload) {
        commands.uploadImage =
            (file) => (editor) => {
                if (!isImageFile(file))
                    return false;
                const selection = editor.getSelection();
                void options
                    .upload(file)
                    .then((result) => normaliseUploadResult(result, file))
                    .then((attrs) => {
                    var _a, _b;
                    if (!attrs)
                        return;
                    if (selection)
                        editor.setSelection(selection);
                    (_b = (_a = editor.commands).insertImage) === null || _b === void 0 ? void 0 : _b.call(_a, attrs);
                })
                    .catch((error) => {
                    if (options.onUploadError) {
                        options.onUploadError(error);
                    }
                    else if (typeof console !== "undefined") {
                        console.error("SMEditor image upload failed:", error);
                    }
                });
                return true;
            };
    }
    return {
        name: "image",
        nodes: [
            {
                name: "image",
                group: "block",
                content: "",
                atom: true,
                attrs: {
                    src: { default: "" },
                    alt: { default: null },
                    title: { default: null },
                    align: { default: null },
                    width: { default: null },
                    height: { default: null },
                    caption: { default: null },
                },
                toDOM: imageToDOM,
                parseDOM: [
                    {
                        tag: "img",
                        getAttrs: (el) => {
                            if (el.closest("figure"))
                                return false;
                            return imgAttrsFrom(el);
                        },
                    },
                    {
                        tag: "figure",
                        getAttrs: (el) => {
                            var _a;
                            const img = el.querySelector("img");
                            if (!img)
                                return false;
                            const base = imgAttrsFrom(img);
                            if (!base)
                                return false;
                            const align = readAlign(el.getAttribute("data-align"));
                            if (align)
                                base.align = align;
                            const cap = el.querySelector("figcaption");
                            const text = (_a = cap === null || cap === void 0 ? void 0 : cap.textContent) === null || _a === void 0 ? void 0 : _a.trim();
                            if (text)
                                base.caption = text;
                            return base;
                        },
                    },
                ],
            },
        ],
        commands,
        configure(opts) {
            return makeExtension({ ...options, ...opts });
        },
    };
    function normaliseUploadResult(result, file) {
        const attrs = typeof result === "string"
            ? { src: result, alt: file.name }
            : result
                ? { alt: file.name, ...result }
                : null;
        return attrs ? normaliseAttrs(attrs) : null;
    }
}
function normaliseProtocols(protocols) {
    return new Set(protocols
        .map((protocol) => protocol.trim().replace(/:$/, "").toLowerCase())
        .filter(Boolean));
}
function readAlign(value) {
    return typeof value === "string" && ALIGN_VALUES.has(value)
        ? value
        : undefined;
}
function readDimension(value) {
    if (typeof value === "number")
        return sanitizeDimension(value);
    if (typeof value !== "string")
        return null;
    return sanitizeDimension(Number(value));
}
function sanitizeDimension(value) {
    if (value == null)
        return null;
    const number = Math.round(Number(value));
    return Number.isFinite(number) && number >= 1 && number <= 4000
        ? number
        : null;
}
function applyDimensionAttr(attrs, key, value) {
    if (value === null || value === "") {
        delete attrs[key];
        return;
    }
    const next = sanitizeDimension(value);
    if (next !== null)
        attrs[key] = next;
}
function isImageFile(file) {
    return Boolean(file && /^image\//i.test(file.type));
}
exports.ImageExtension = makeExtension({});
exports.default = exports.ImageExtension;

},{"@smeditor/core":1}],
32:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-table
 *
 * Tables — the §10 module. Three nodes (`table`, `table_row`,
 * `table_cell`) plus structural commands.
 *
 * Span awareness (new in v0.0.2): every structural command runs
 * against the visual grid built by `./table-map`, so `colspan` /
 * `rowspan` cells behave correctly — adding a column widens spanning
 * cells instead of shifting the grid out of alignment, merge / split
 * work, and so on.
 *
 * Selection note: the editor's selection model is top-level-indexed,
 * so the commands locate the active `<td>`/`<th>` through the live
 * DOM (`getSelection()` + `closest`) and translate that into a
 * logical `CellRef`. The DOM `<tr>` index is the visual row index and
 * the `<td>` index within it is the logical cell index — together
 * they're exactly a `CellRef`.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.__test = exports.rectOf = exports.cellAt = exports.buildTableMap = exports.TableExtension = void 0;
const core_1 = require("@smeditor/core");
const table_map_js_1 = require("./table-map.js");
Object.defineProperty(exports, "buildTableMap", { enumerable: true, get: function () { return table_map_js_1.buildTableMap; } });
Object.defineProperty(exports, "cellAt", { enumerable: true, get: function () { return table_map_js_1.cellAt; } });
Object.defineProperty(exports, "rectOf", { enumerable: true, get: function () { return table_map_js_1.rectOf; } });
const CELL_ALIGNS = new Set([
    "left",
    "center",
    "right",
    "justify",
]);
function sanitizeColor(raw) {
    return (0, core_1.sanitizeCSSColor)(raw);
}
function sanitizeAlign(raw) {
    if (typeof raw !== "string")
        return null;
    const value = raw.trim().toLowerCase();
    return CELL_ALIGNS.has(value) ? value : null;
}
function styleDeclaration(style, name) {
    var _a, _b;
    const match = new RegExp(`(?:^|;)\\s*${name}\\s*:\\s*([^;]+)`, "i").exec(style);
    return (_b = (_a = match === null || match === void 0 ? void 0 : match[1]) === null || _a === void 0 ? void 0 : _a.trim()) !== null && _b !== void 0 ? _b : null;
}
function setCellAttr(cell, key, value) {
    var _a, _b, _c;
    const current = (_b = (_a = cell.attrs) === null || _a === void 0 ? void 0 : _a[key]) !== null && _b !== void 0 ? _b : null;
    if (current === value)
        return false;
    if (value === null) {
        if (!cell.attrs || !(key in cell.attrs))
            return false;
        const next = { ...cell.attrs };
        delete next[key];
        if (Object.keys(next).length === 0)
            delete cell.attrs;
        else
            cell.attrs = next;
        return true;
    }
    cell.attrs = { ...((_c = cell.attrs) !== null && _c !== void 0 ? _c : {}), [key]: value };
    return true;
}
function refKey(ref) {
    return `${ref.rowIndex}:${ref.cellIndex}`;
}
function activeCell(table, visual) {
    var _a, _b, _c, _d;
    const map = (0, table_map_js_1.buildTableMap)(table);
    const ref = (0, table_map_js_1.cellAt)(map, visual.row, visual.col);
    return ref
        ? (_d = (_c = (_b = (_a = table.content) === null || _a === void 0 ? void 0 : _a[ref.rowIndex]) === null || _b === void 0 ? void 0 : _b.content) === null || _c === void 0 ? void 0 : _c[ref.cellIndex]) !== null && _d !== void 0 ? _d : null
        : null;
}
function columnCells(table, col) {
    var _a, _b, _c;
    const map = (0, table_map_js_1.buildTableMap)(table);
    const cells = [];
    const seen = new Set();
    for (let row = 0; row < map.height; row++) {
        const ref = (0, table_map_js_1.cellAt)(map, row, col);
        if (!ref)
            continue;
        const key = refKey(ref);
        if (seen.has(key))
            continue;
        seen.add(key);
        const cell = (_c = (_b = (_a = table.content) === null || _a === void 0 ? void 0 : _a[ref.rowIndex]) === null || _b === void 0 ? void 0 : _b.content) === null || _c === void 0 ? void 0 : _c[ref.cellIndex];
        if (cell)
            cells.push(cell);
    }
    return cells;
}
function cellAttrs(el, header) {
    var _a, _b, _c, _d;
    const out = { header };
    const colspan = Number((_a = el.getAttribute("colspan")) !== null && _a !== void 0 ? _a : "1");
    const rowspan = Number((_b = el.getAttribute("rowspan")) !== null && _b !== void 0 ? _b : "1");
    if (colspan > 1)
        out.colspan = colspan;
    if (rowspan > 1)
        out.rowspan = rowspan;
    const style = (_c = el.getAttribute("style")) !== null && _c !== void 0 ? _c : "";
    const w = /(?:^|;)\s*width\s*:\s*(\d+(?:\.\d+)?)px/i.exec(style);
    if (w)
        out.colwidth = Math.round(Number(w[1]));
    const backgroundColor = sanitizeColor((_d = styleDeclaration(style, "background-color")) !== null && _d !== void 0 ? _d : styleDeclaration(style, "background"));
    if (backgroundColor)
        out.backgroundColor = backgroundColor;
    const textAlign = sanitizeAlign(styleDeclaration(style, "text-align"));
    if (textAlign)
        out.textAlign = textAlign;
    return out;
}
function cellToDOM(node) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _j;
    const header = Boolean((_a = node.attrs) === null || _a === void 0 ? void 0 : _a.header);
    const tag = header ? "th" : "td";
    const attrs = {};
    const colspan = Number((_c = (_b = node.attrs) === null || _b === void 0 ? void 0 : _b.colspan) !== null && _c !== void 0 ? _c : 1);
    const rowspan = Number((_e = (_d = node.attrs) === null || _d === void 0 ? void 0 : _d.rowspan) !== null && _e !== void 0 ? _e : 1);
    if (colspan > 1)
        attrs.colspan = colspan;
    if (rowspan > 1)
        attrs.rowspan = rowspan;
    const style = [];
    const colwidth = Number((_g = (_f = node.attrs) === null || _f === void 0 ? void 0 : _f.colwidth) !== null && _g !== void 0 ? _g : 0);
    if (colwidth > 0)
        style.push(`width: ${Math.round(colwidth)}px`);
    const backgroundColor = sanitizeColor((_h = node.attrs) === null || _h === void 0 ? void 0 : _h.backgroundColor);
    if (backgroundColor)
        style.push(`background-color: ${backgroundColor}`);
    const textAlign = sanitizeAlign((_j = node.attrs) === null || _j === void 0 ? void 0 : _j.textAlign);
    if (textAlign)
        style.push(`text-align: ${textAlign}`);
    if (style.length > 0)
        attrs.style = style.join("; ");
    return Object.keys(attrs).length ? [tag, attrs, 0] : [tag, 0];
}
const tableNode = {
    name: "table",
    group: "block",
    content: "table_row+",
    toDOM: () => ["table", { class: "smeditor-table" }, 0],
    parseDOM: [{ tag: "table" }],
};
function sanitizeRowHeight(raw) {
    const n = Math.round(Number(raw));
    return Number.isFinite(n) && n >= 8 && n <= 2000 ? n : null;
}
const tableRowNode = {
    name: "table_row",
    group: "block",
    content: "table_cell+",
    attrs: { rowheight: { default: null } },
    toDOM: (node) => {
        var _a;
        const h = sanitizeRowHeight((_a = node.attrs) === null || _a === void 0 ? void 0 : _a.rowheight);
        return h ? ["tr", { style: `height: ${h}px` }, 0] : ["tr", 0];
    },
    parseDOM: [
        {
            tag: "tr",
            getAttrs: (el) => {
                var _a;
                const style = (_a = el.getAttribute("style")) !== null && _a !== void 0 ? _a : "";
                const m = /(?:^|;)\s*height\s*:\s*(\d+(?:\.\d+)?)px/i.exec(style);
                const h = m ? sanitizeRowHeight(m[1]) : null;
                return h ? { rowheight: h } : null;
            },
        },
    ],
};
const tableCellNode = {
    name: "table_cell",
    group: "block",
    content: "block+",
    attrs: {
        header: { default: false },
        colspan: { default: 1 },
        rowspan: { default: 1 },
        colwidth: { default: null },
    },
    toDOM: cellToDOM,
    parseDOM: [
        { tag: "td", getAttrs: (el) => cellAttrs(el, false) },
        { tag: "th", getAttrs: (el) => cellAttrs(el, true) },
    ],
};
// ============================================================================
// Cell construction (used only by insertTable)
// ============================================================================
function emptyCell(header = false) {
    const cell = {
        type: "table_cell",
        content: [{ type: "paragraph" }],
    };
    if (header)
        cell.attrs = { header: true };
    return cell;
}
function emptyRow(cols, header = false) {
    return {
        type: "table_row",
        content: Array.from({ length: cols }, () => emptyCell(header)),
    };
}
function findCellContext(editor) {
    if (typeof window === "undefined")
        return null;
    const root = editor.element;
    if (!root)
        return null;
    const sel = window.getSelection();
    if (!sel || sel.rangeCount === 0)
        return null;
    let node = sel.anchorNode;
    let cell = null;
    while (node && node !== root) {
        if (node.nodeType === 1) {
            const tag = node.tagName.toLowerCase();
            if (tag === "td" || tag === "th") {
                cell = node;
                break;
            }
        }
        node = node.parentNode;
    }
    if (!cell)
        return null;
    const tr = cell.closest("tr");
    const table = cell.closest("table");
    if (!tr || !table)
        return null;
    const cellIndex = Array.from(tr.children).indexOf(cell);
    const rowIndex = Array.from(table.querySelectorAll("tr")).indexOf(tr);
    const tableIndex = Array.from(root.children).indexOf(table);
    if (tableIndex < 0 || rowIndex < 0 || cellIndex < 0)
        return null;
    return { tableIndex, ref: { rowIndex, cellIndex } };
}
function caretAt(idx) {
    return {
        anchor: { path: [idx], offset: 0 },
        head: { path: [idx], offset: 0 },
    };
}
/** A collapsed selection at the start of `path`. */
function caretPath(path) {
    return {
        anchor: { path, offset: 0 },
        head: { path, offset: 0 },
    };
}
/**
 * Locate the table coordinates of a deep selection path. Returns the
 * table's path, the table node, and the row / cell indices, or null
 * if the path doesn't run through a table.
 */
function cellCoords(doc, path) {
    for (let k = 1; k + 1 < path.length; k++) {
        const node = (0, core_1.nodeAt)(doc, path.slice(0, k));
        if ((node === null || node === void 0 ? void 0 : node.type) === "table") {
            return {
                tablePath: path.slice(0, k),
                table: node,
                rowIndex: path[k],
                cellIndex: path[k + 1],
            };
        }
    }
    return null;
}
/**
 * The leaf-block path of the cell after the current one — next cell
 * in the row, or the first cell of the next row. Returns null at the
 * last cell of the table.
 */
function nextCellLeaf(doc, path) {
    var _a, _b, _c;
    const c = cellCoords(doc, path);
    if (!c)
        return null;
    const rows = (_a = c.table.content) !== null && _a !== void 0 ? _a : [];
    const cells = (_c = (_b = rows[c.rowIndex]) === null || _b === void 0 ? void 0 : _b.content) !== null && _c !== void 0 ? _c : [];
    let row = c.rowIndex;
    let cell = c.cellIndex + 1;
    if (cell >= cells.length) {
        row += 1;
        cell = 0;
    }
    if (row >= rows.length)
        return null;
    return (0, core_1.descendToLeafPath)(doc, [...c.tablePath, row, cell]);
}
/** The leaf-block path of the cell before the current one, or null. */
function prevCellLeaf(doc, path) {
    var _a, _b, _c, _d;
    const c = cellCoords(doc, path);
    if (!c)
        return null;
    const rows = (_a = c.table.content) !== null && _a !== void 0 ? _a : [];
    let row = c.rowIndex;
    let cell = c.cellIndex - 1;
    if (cell < 0) {
        row -= 1;
        if (row < 0)
            return null;
        cell = ((_d = (_c = (_b = rows[row]) === null || _b === void 0 ? void 0 : _b.content) === null || _c === void 0 ? void 0 : _c.length) !== null && _d !== void 0 ? _d : 0) - 1;
    }
    if (cell < 0)
        return null;
    return (0, core_1.descendToLeafPath)(doc, [...c.tablePath, row, cell]);
}
/**
 * Shared command body: locate the table + active cell, clone the doc,
 * compute the cell's visual rectangle, hand it to `mutate`, dispatch.
 */
function withCell(editor, mutate) {
    var _a, _b, _c, _d;
    const sourceDoc = editor.getJSON();
    let ctx = findCellContext(editor);
    // Structural table tools are often triggered from toolbar buttons.
    // In that moment the browser selection may already have moved to the
    // button, especially in React playgrounds. Fall back to SMEditor's
    // own deep selection, which is enough to locate the active table cell.
    if (!ctx) {
        const sel = editor.getSelection();
        const coords = sel ? cellCoords(sourceDoc, sel.anchor.path) : null;
        if (coords && coords.tablePath.length === 1) {
            ctx = {
                tableIndex: coords.tablePath[0],
                ref: { rowIndex: coords.rowIndex, cellIndex: coords.cellIndex },
            };
        }
    }
    if (!ctx)
        return false;
    const doc = (0, core_1.cloneDoc)(sourceDoc);
    const table = doc.content[ctx.tableIndex];
    if (!table || table.type !== "table" || !table.content)
        return false;
    const map = (0, table_map_js_1.buildTableMap)(table);
    const rect = (0, table_map_js_1.rectOf)(map, ctx.ref);
    if (!rect)
        return false;
    const changed = mutate(table, {
        row: rect.top,
        col: rect.left,
        rectRight: rect.right,
        rectBottom: rect.bottom,
    });
    if (!changed)
        return false;
    // Keep the caret in the cell the command acted on. Resetting to the
    // table's top-level index made a deep selection normalise to the
    // *first* cell, so a follow-up cell command (toggle header column, set
    // background/align) silently retargeted the wrong column. When the
    // table survives and deep selection is on, point the caret back at the
    // active cell's leaf block — clamped, since structural commands may
    // have removed the row/column the caret was in.
    let selection = caretAt(ctx.tableIndex);
    if (((_a = editor.options.deepSelection) !== null && _a !== void 0 ? _a : false) && table.type === "table") {
        const rows = (_b = table.content) !== null && _b !== void 0 ? _b : [];
        const rowIdx = Math.min(ctx.ref.rowIndex, rows.length - 1);
        const cells = (_d = (_c = rows[rowIdx]) === null || _c === void 0 ? void 0 : _c.content) !== null && _d !== void 0 ? _d : [];
        const cellIdx = Math.min(ctx.ref.cellIndex, cells.length - 1);
        if (rowIdx >= 0 && cellIdx >= 0) {
            selection = caretPath((0, core_1.descendToLeafPath)(doc, [ctx.tableIndex, rowIdx, cellIdx]));
        }
    }
    // If the table emptied out, the mutator converts it to a paragraph and
    // `table.type` is no longer "table", so the caret falls back to the
    // top-level index above.
    editor.dispatch({
        doc,
        selection,
        addToHistory: true,
    });
    return true;
}
exports.TableExtension = {
    name: "table",
    nodes: [tableNode, tableRowNode, tableCellNode],
    commands: {
        /** insertTable({ rows, cols, withHeaderRow }) — default 3×3 + header. */
        insertTable: (opts = {}) => (editor) => {
            var _a, _b, _c, _d, _e;
            const rows = Math.max(1, Math.min((_a = opts.rows) !== null && _a !== void 0 ? _a : 3, 20));
            const cols = Math.max(1, Math.min((_b = opts.cols) !== null && _b !== void 0 ? _b : 3, 20));
            const withHeaderRow = (_c = opts.withHeaderRow) !== null && _c !== void 0 ? _c : true;
            const tableRows = [];
            for (let r = 0; r < rows; r++) {
                tableRows.push(emptyRow(cols, withHeaderRow && r === 0));
            }
            const table = { type: "table", content: tableRows };
            const trailing = { type: "paragraph" };
            const sel = editor.getSelection();
            const idx = (_d = sel === null || sel === void 0 ? void 0 : sel.anchor.path[0]) !== null && _d !== void 0 ? _d : 0;
            const doc = (0, core_1.cloneDoc)(editor.getJSON());
            const current = doc.content[idx];
            const currentEmpty = current &&
                (!current.content ||
                    current.content.length === 0 ||
                    (current.content.length === 1 &&
                        current.content[0].type === "text" &&
                        ((_e = current.content[0].text) !== null && _e !== void 0 ? _e : "") === ""));
            let caretIdx;
            if (currentEmpty) {
                doc.content.splice(idx, 1, table, trailing);
                caretIdx = idx;
            }
            else {
                doc.content.splice(idx + 1, 0, table, trailing);
                caretIdx = idx + 1;
            }
            editor.dispatch({
                doc,
                selection: caretAt(caretIdx),
                addToHistory: true,
            });
            return true;
        },
        // ---- Rows ----------------------------------------------------------
        addRowBefore: () => (editor) => withCell(editor, (table, v) => {
            (0, table_map_js_1.addRowAt)(table, v.row);
            return true;
        }),
        addRowAfter: () => (editor) => withCell(editor, (table, v) => {
            (0, table_map_js_1.addRowAt)(table, v.rectBottom);
            return true;
        }),
        deleteRow: () => (editor) => withCell(editor, (table, v) => {
            var _a, _b;
            (0, table_map_js_1.removeRowAt)(table, v.row);
            if (((_b = (_a = table.content) === null || _a === void 0 ? void 0 : _a.length) !== null && _b !== void 0 ? _b : 0) === 0) {
                table.type = "paragraph";
                delete table.content;
            }
            return true;
        }),
        // ---- Columns -------------------------------------------------------
        addColumnBefore: () => (editor) => withCell(editor, (table, v) => {
            (0, table_map_js_1.addColumnAt)(table, v.col);
            return true;
        }),
        addColumnAfter: () => (editor) => withCell(editor, (table, v) => {
            (0, table_map_js_1.addColumnAt)(table, v.rectRight);
            return true;
        }),
        deleteColumn: () => (editor) => withCell(editor, (table, v) => {
            (0, table_map_js_1.removeColumnAt)(table, v.col);
            const empty = !table.content ||
                table.content.every((row) => { var _a, _b; return ((_b = (_a = row.content) === null || _a === void 0 ? void 0 : _a.length) !== null && _b !== void 0 ? _b : 0) === 0; });
            if (empty) {
                table.type = "paragraph";
                delete table.content;
            }
            return true;
        }),
        // ---- Merge / split -------------------------------------------------
        mergeCellRight: () => (editor) => withCell(editor, (table, v) => (0, table_map_js_1.mergeRight)(table, v.row, v.col)),
        mergeCellDown: () => (editor) => withCell(editor, (table, v) => (0, table_map_js_1.mergeDown)(table, v.row, v.col)),
        splitCell: () => (editor) => withCell(editor, (table, v) => (0, table_map_js_1.splitCell)(table, v.row, v.col)),
        /**
         * mergeCells() — merge whatever cell range is currently selected.
         * The cell-range UI marks cells with the
         * `smeditor-cell--selected` class; this reads that range out of
         * the DOM, turns it into a visual rectangle through the grid
         * model, and merges it. Returns false if fewer than two cells are
         * selected or the rectangle is ragged.
         */
        mergeCells: () => (editor) => {
            if (typeof window === "undefined")
                return false;
            const root = editor.element;
            if (!root)
                return false;
            const selected = Array.from(root.querySelectorAll("td.smeditor-cell--selected, th.smeditor-cell--selected"));
            if (selected.length < 2)
                return false;
            const tableEl = selected[0].closest("table");
            if (!tableEl)
                return false;
            const tableIndex = Array.from(root.children).indexOf(tableEl);
            if (tableIndex < 0)
                return false;
            // DOM cells → logical CellRefs.
            const allRows = Array.from(tableEl.querySelectorAll("tr"));
            const refs = [];
            for (const cell of selected) {
                const tr = cell.closest("tr");
                if (!tr)
                    continue;
                const rowIndex = allRows.indexOf(tr);
                const cellIndex = Array.from(tr.children).indexOf(cell);
                if (rowIndex >= 0 && cellIndex >= 0) {
                    refs.push({ rowIndex, cellIndex });
                }
            }
            if (refs.length < 2)
                return false;
            const doc = (0, core_1.cloneDoc)(editor.getJSON());
            const table = doc.content[tableIndex];
            if (!table || table.type !== "table" || !table.content)
                return false;
            // Bounding visual rectangle of the selected cells.
            const map = (0, table_map_js_1.buildTableMap)(table);
            let top = Infinity;
            let left = Infinity;
            let bottom = -Infinity;
            let right = -Infinity;
            for (const ref of refs) {
                const r = (0, table_map_js_1.rectOf)(map, ref);
                if (!r)
                    continue;
                top = Math.min(top, r.top);
                left = Math.min(left, r.left);
                bottom = Math.max(bottom, r.bottom);
                right = Math.max(right, r.right);
            }
            if (top === Infinity)
                return false;
            if (!(0, table_map_js_1.mergeCellRange)(table, { top, left, bottom, right }))
                return false;
            editor.dispatch({
                doc,
                selection: caretAt(tableIndex),
                addToHistory: true,
            });
            return true;
        },
        // ---- Whole-table ---------------------------------------------------
        deleteTable: () => (editor) => withCell(editor, (table) => {
            table.type = "paragraph";
            delete table.content;
            delete table.attrs;
            return true;
        }),
        setCellBackground: (opts = {}) => (editor) => withCell(editor, (table, v) => {
            var _a;
            const cell = activeCell(table, v);
            if (!cell)
                return false;
            return setCellAttr(cell, "backgroundColor", sanitizeColor((_a = opts.color) !== null && _a !== void 0 ? _a : null));
        }),
        setCellAlign: (opts = {}) => (editor) => withCell(editor, (table, v) => {
            var _a;
            const cell = activeCell(table, v);
            if (!cell)
                return false;
            return setCellAttr(cell, "textAlign", sanitizeAlign((_a = opts.align) !== null && _a !== void 0 ? _a : null));
        }),
        /**
         * setColumnWidth({ tableIndex, col, width }) — set a column's
         * pixel width. `tableIndex` is the table's top-level block index;
         * `col` is a visual column index. Used by the column-resize UI.
         */
        setColumnWidth: (opts) => (editor) => {
            var _a, _b;
            const doc = (0, core_1.cloneDoc)(editor.getJSON());
            const table = doc.content[(_a = opts === null || opts === void 0 ? void 0 : opts.tableIndex) !== null && _a !== void 0 ? _a : -1];
            if (!table || table.type !== "table" || !table.content)
                return false;
            (0, table_map_js_1.setColumnWidth)(table, opts.col, opts.width);
            editor.dispatch({
                doc,
                selection: (_b = editor.getSelection()) !== null && _b !== void 0 ? _b : caretAt(opts.tableIndex),
                addToHistory: true,
            });
            return true;
        },
        /**
         * setRowHeight({ tableIndex, row, height }) — set a row's pixel
         * height. `tableIndex` is the table's top-level block index; `row`
         * is the logical row index. Used by the row-resize UI.
         */
        setRowHeight: (opts) => (editor) => {
            var _a, _b;
            const doc = (0, core_1.cloneDoc)(editor.getJSON());
            const table = doc.content[(_a = opts === null || opts === void 0 ? void 0 : opts.tableIndex) !== null && _a !== void 0 ? _a : -1];
            if (!table || table.type !== "table" || !table.content)
                return false;
            const row = table.content[opts.row];
            if (!row)
                return false;
            const h = sanitizeRowHeight(opts.height);
            if (!setCellAttr(row, "rowheight", h))
                return false;
            editor.dispatch({
                doc,
                selection: (_b = editor.getSelection()) !== null && _b !== void 0 ? _b : caretAt(opts.tableIndex),
                addToHistory: true,
            });
            return true;
        },
        toggleHeaderRow: () => (editor) => withCell(editor, (table) => {
            var _a, _b, _c;
            const firstRow = (_a = table.content) === null || _a === void 0 ? void 0 : _a[0];
            if (!(firstRow === null || firstRow === void 0 ? void 0 : firstRow.content))
                return false;
            const makeHeader = !((_c = (_b = firstRow.content[0]) === null || _b === void 0 ? void 0 : _b.attrs) === null || _c === void 0 ? void 0 : _c.header);
            let changed = false;
            for (const cell of firstRow.content) {
                changed =
                    setCellAttr(cell, "header", makeHeader ? true : null) || changed;
            }
            return changed;
        }),
        toggleHeaderColumn: () => (editor) => withCell(editor, (table, v) => {
            const cells = columnCells(table, v.col);
            if (cells.length === 0)
                return false;
            const makeHeader = !cells.every((cell) => { var _a; return Boolean((_a = cell.attrs) === null || _a === void 0 ? void 0 : _a.header); });
            let changed = false;
            for (const cell of cells) {
                changed =
                    setCellAttr(cell, "header", makeHeader ? true : null) || changed;
            }
            return changed;
        }),
        /**
         * goToNextCell — move the caret to the next table cell. Past the
         * last cell it appends a row and moves into it. Needs a deep
         * selection path; returns false outside a table.
         */
        goToNextCell: () => (editor) => {
            const sel = editor.getSelection();
            if (!sel)
                return false;
            const doc = editor.getJSON();
            const target = nextCellLeaf(doc, sel.anchor.path);
            if (target) {
                editor.dispatch({
                    doc,
                    selection: caretPath(target),
                    addToHistory: false,
                });
                return true;
            }
            // Past the last cell — append a row and move into it.
            const c = cellCoords(doc, sel.anchor.path);
            if (!c)
                return false;
            const newDoc = (0, core_1.cloneDoc)(doc);
            const table = (0, core_1.nodeAt)(newDoc, c.tablePath);
            if (!(table === null || table === void 0 ? void 0 : table.content))
                return false;
            const newRowIndex = table.content.length;
            (0, table_map_js_1.addRowAt)(table, newRowIndex);
            const leaf = (0, core_1.descendToLeafPath)(newDoc, [
                ...c.tablePath,
                newRowIndex,
                0,
            ]);
            editor.dispatch({
                doc: newDoc,
                selection: caretPath(leaf),
                addToHistory: true,
            });
            return true;
        },
        /** goToPrevCell — move the caret to the previous table cell. */
        goToPrevCell: () => (editor) => {
            const sel = editor.getSelection();
            if (!sel)
                return false;
            const doc = editor.getJSON();
            const target = prevCellLeaf(doc, sel.anchor.path);
            if (!target)
                return false;
            editor.dispatch({
                doc,
                selection: caretPath(target),
                addToHistory: false,
            });
            return true;
        },
    },
    keyboardShortcuts: {
        // Tab / Shift-Tab navigate cells when the caret is in a table.
        // Outside a table the commands return false, so a later extension
        // (indent) handles Tab instead.
        Tab: (editor) => { var _a, _b, _c; return (_c = (_b = (_a = editor.commands).goToNextCell) === null || _b === void 0 ? void 0 : _b.call(_a)) !== null && _c !== void 0 ? _c : false; },
        "Shift-Tab": (editor) => { var _a, _b, _c; return (_c = (_b = (_a = editor.commands).goToPrevCell) === null || _b === void 0 ? void 0 : _b.call(_a)) !== null && _c !== void 0 ? _c : false; },
    },
};
exports.default = exports.TableExtension;
/** Test-only access to the pure cell-navigation helpers. */
exports.__test = { nextCellLeaf, prevCellLeaf };

},{"@smeditor/core":1,"./table-map.js":33}],
33:[function(require,module,exports){
"use strict";
/**
 * Table grid model.
 *
 * A table's JSON is a list of *logical* rows, each a list of *logical*
 * cells. Once cells carry `colspan` / `rowspan`, that logical shape no
 * longer matches the *visual* grid the user sees — a 2-colspan cell is
 * one logical cell but two visual columns.
 *
 * `TableMap` builds the visual grid: for every (row, col) coordinate
 * it records which logical cell occupies it. Every structural command
 * — add/remove column or row, merge, split — is expressed against the
 * visual grid through this map, so spans are handled correctly.
 *
 * This module is pure: functions take a `table` DocumentNode and
 * mutate it (or its clone — the caller clones first). No DOM, no
 * editor. That makes the whole thing unit-testable, which matters a
 * lot for table logic.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.buildTableMap = buildTableMap;
exports.cellAt = cellAt;
exports.rectOf = rectOf;
exports.addColumnAt = addColumnAt;
exports.removeColumnAt = removeColumnAt;
exports.addRowAt = addRowAt;
exports.removeRowAt = removeRowAt;
exports.mergeRight = mergeRight;
exports.mergeDown = mergeDown;
exports.splitCell = splitCell;
exports.mergeCellRange = mergeCellRange;
exports.setColumnWidth = setColumnWidth;
// ============================================================================
// Build
// ============================================================================
function span(cell, key) {
    var _a, _b;
    const v = Number((_b = (_a = cell.attrs) === null || _a === void 0 ? void 0 : _a[key]) !== null && _b !== void 0 ? _b : 1);
    return Number.isFinite(v) && v >= 1 ? Math.floor(v) : 1;
}
/**
 * Build the visual grid for a table node.
 *
 * Cells are laid out left-to-right, top-to-bottom; a cell claims
 * `colspan × rowspan` visual cells starting at the first free column
 * in its row. A `rowspan` cell pre-occupies cells in the rows below,
 * so later rows skip past them.
 */
function buildTableMap(table) {
    var _a, _b;
    const rows = (_a = table.content) !== null && _a !== void 0 ? _a : [];
    const height = rows.length;
    const grid = rows.map(() => []);
    let width = 0;
    for (let r = 0; r < height; r++) {
        const cells = (_b = rows[r].content) !== null && _b !== void 0 ? _b : [];
        let col = 0;
        for (let ci = 0; ci < cells.length; ci++) {
            const cell = cells[ci];
            const colspan = span(cell, "colspan");
            const rowspan = span(cell, "rowspan");
            // Skip columns already claimed by a rowspan from a row above.
            while (grid[r][col])
                col++;
            const ref = { rowIndex: r, cellIndex: ci };
            for (let dr = 0; dr < rowspan && r + dr < height; dr++) {
                for (let dc = 0; dc < colspan; dc++) {
                    grid[r + dr][col + dc] = ref;
                }
            }
            col += colspan;
        }
        width = Math.max(width, grid[r].length);
    }
    // Pad every row to the full width so lookups never go out of bounds.
    for (let r = 0; r < height; r++) {
        for (let c = 0; c < width; c++) {
            if (grid[r][c] === undefined)
                grid[r][c] = null;
        }
    }
    return { width, height, grid };
}
/** The logical cell at a visual coordinate, or null. */
function cellAt(map, row, col) {
    var _a, _b;
    return (_b = (_a = map.grid[row]) === null || _a === void 0 ? void 0 : _a[col]) !== null && _b !== void 0 ? _b : null;
}
/** The visual rectangle a logical cell occupies, or null if absent. */
function rectOf(map, ref) {
    let top = Infinity;
    let left = Infinity;
    let bottom = -Infinity;
    let right = -Infinity;
    for (let r = 0; r < map.height; r++) {
        for (let c = 0; c < map.width; c++) {
            const cell = map.grid[r][c];
            if (cell && cell.rowIndex === ref.rowIndex && cell.cellIndex === ref.cellIndex) {
                top = Math.min(top, r);
                left = Math.min(left, c);
                bottom = Math.max(bottom, r + 1);
                right = Math.max(right, c + 1);
            }
        }
    }
    if (top === Infinity)
        return null;
    return { top, left, bottom, right };
}
// ============================================================================
// Cell construction
// ============================================================================
function emptyCell(header = false) {
    const cell = {
        type: "table_cell",
        content: [{ type: "paragraph" }],
    };
    if (header)
        cell.attrs = { header: true };
    return cell;
}
function isHeaderRow(row) {
    var _a, _b, _c;
    return Boolean((_c = (_b = (_a = row.content) === null || _a === void 0 ? void 0 : _a[0]) === null || _b === void 0 ? void 0 : _b.attrs) === null || _c === void 0 ? void 0 : _c.header);
}
function setSpan(cell, key, value) {
    var _a;
    if (value <= 1) {
        if (cell.attrs) {
            delete cell.attrs[key];
            if (Object.keys(cell.attrs).length === 0)
                delete cell.attrs;
        }
    }
    else {
        cell.attrs = { ...((_a = cell.attrs) !== null && _a !== void 0 ? _a : {}), [key]: value };
    }
}
// ============================================================================
// Column operations
// ============================================================================
/**
 * Insert a column at visual position `at` (0..width). Cells that span
 * across the insertion line are widened; everything else gets a fresh
 * cell.
 */
function addColumnAt(table, at) {
    var _a;
    const map = buildTableMap(table);
    const rows = (_a = table.content) !== null && _a !== void 0 ? _a : [];
    const widened = new Set();
    for (let r = 0; r < map.height; r++) {
        const before = at > 0 ? cellAt(map, r, at - 1) : null;
        const here = at < map.width ? cellAt(map, r, at) : null;
        // A cell straddles the line when the same logical cell sits on
        // both sides of it — widen it once.
        if (before && here && refEq(before, here)) {
            const key = refKey(before);
            if (!widened.has(key)) {
                widened.add(key);
                const cell = rows[before.rowIndex].content[before.cellIndex];
                setSpan(cell, "colspan", span(cell, "colspan") + 1);
            }
            continue;
        }
        // Otherwise insert a new cell into this logical row, before the
        // cell currently at `at` (or at the end of the row).
        const row = rows[r];
        if (!row.content)
            row.content = [];
        const insertIndex = here ? here.cellIndex : row.content.length;
        row.content.splice(insertIndex, 0, emptyCell(isHeaderRow(row)));
    }
}
/** Remove the visual column at `at`. Spanning cells shrink instead. */
function removeColumnAt(table, at) {
    var _a;
    const map = buildTableMap(table);
    if (at < 0 || at >= map.width)
        return;
    const rows = (_a = table.content) !== null && _a !== void 0 ? _a : [];
    const shrunk = new Set();
    // Collect removals per row, apply high-index-first so indices stay valid.
    const removals = [];
    for (let r = 0; r < map.height; r++) {
        const ref = cellAt(map, r, at);
        if (!ref)
            continue;
        const cell = rows[ref.rowIndex].content[ref.cellIndex];
        const colspan = span(cell, "colspan");
        if (colspan > 1) {
            const key = refKey(ref);
            if (!shrunk.has(key)) {
                shrunk.add(key);
                setSpan(cell, "colspan", colspan - 1);
            }
        }
        else {
            removals.push({ row: ref.rowIndex, cellIndex: ref.cellIndex });
        }
    }
    // De-dupe and delete.
    const seen = new Set();
    removals
        .filter((x) => {
        const k = `${x.row}:${x.cellIndex}`;
        if (seen.has(k))
            return false;
        seen.add(k);
        return true;
    })
        .sort((a, b) => b.cellIndex - a.cellIndex)
        .forEach((x) => { var _a; return (_a = rows[x.row].content) === null || _a === void 0 ? void 0 : _a.splice(x.cellIndex, 1); });
}
// ============================================================================
// Row operations
// ============================================================================
/** Insert a row at visual position `at` (0..height). */
function addRowAt(table, at) {
    var _a;
    const map = buildTableMap(table);
    const rows = (_a = table.content) !== null && _a !== void 0 ? _a : [];
    const newCells = [];
    const extended = new Set();
    for (let c = 0; c < map.width;) {
        const above = at > 0 ? cellAt(map, at - 1, c) : null;
        const here = at < map.height ? cellAt(map, at, c) : null;
        // A rowspan cell crossing the line gets taller; skip its columns.
        if (above && here && refEq(above, here)) {
            const key = refKey(above);
            if (!extended.has(key)) {
                extended.add(key);
                const cell = rows[above.rowIndex].content[above.cellIndex];
                setSpan(cell, "rowspan", span(cell, "rowspan") + 1);
            }
            const cell = rows[above.rowIndex].content[above.cellIndex];
            c += span(cell, "colspan");
            continue;
        }
        newCells.push(emptyCell());
        c += 1;
    }
    rows.splice(at, 0, { type: "table_row", content: newCells });
}
/** Remove the visual row at `at`. Rowspan cells shrink or relocate. */
function removeRowAt(table, at) {
    var _a, _b;
    const map = buildTableMap(table);
    if (at < 0 || at >= map.height)
        return;
    const rows = (_a = table.content) !== null && _a !== void 0 ? _a : [];
    // Before deleting the row, fix up rowspan cells.
    for (let c = 0; c < map.width; c++) {
        const ref = cellAt(map, at, c);
        if (!ref)
            continue;
        const cell = (_b = rows[ref.rowIndex].content) === null || _b === void 0 ? void 0 : _b[ref.cellIndex];
        if (!cell)
            continue;
        const rowspan = span(cell, "rowspan");
        if (rowspan > 1) {
            if (ref.rowIndex === at) {
                // The cell *starts* in the doomed row — move it down one row,
                // shrinking the span, so its content survives.
                const moved = { ...cell };
                setSpan(moved, "rowspan", rowspan - 1);
                const nextRow = rows[at + 1];
                if (nextRow) {
                    if (!nextRow.content)
                        nextRow.content = [];
                    // Insert at the logical index matching the moved cell's visual
                    // column (`c`), mirroring splitCell. `unshift` always prepended
                    // at index 0, which jumped a non-leftmost rowspan cell to the
                    // first column.
                    const movedLeft = c;
                    const nextMap = buildTableMap(table);
                    let insertAt = nextRow.content.length;
                    for (let ci = 0; ci < nextRow.content.length; ci++) {
                        const rr = rectOf(nextMap, { rowIndex: at + 1, cellIndex: ci });
                        if (rr && rr.left >= movedLeft) {
                            insertAt = ci;
                            break;
                        }
                    }
                    nextRow.content.splice(insertAt, 0, moved);
                }
            }
            else {
                // The cell merely passes through — just shorten it.
                setSpan(cell, "rowspan", rowspan - 1);
            }
        }
    }
    rows.splice(at, 1);
}
// ============================================================================
// Merge / split
// ============================================================================
/**
 * Merge the cell at visual (row,col) with its right neighbour. Returns
 * false when there's no aligned neighbour (different heights can't
 * merge cleanly).
 */
function mergeRight(table, row, col) {
    var _a, _b;
    const map = buildTableMap(table);
    const ref = cellAt(map, row, col);
    if (!ref)
        return false;
    const rect = rectOf(map, ref);
    if (!rect || rect.right >= map.width)
        return false;
    const neighbourRef = cellAt(map, rect.top, rect.right);
    if (!neighbourRef)
        return false;
    const neighbourRect = rectOf(map, neighbourRef);
    if (!neighbourRect)
        return false;
    // Heights must line up for a clean merge.
    if (neighbourRect.top !== rect.top || neighbourRect.bottom !== rect.bottom) {
        return false;
    }
    const rows = table.content;
    const cell = rows[ref.rowIndex].content[ref.cellIndex];
    const neighbour = rows[neighbourRef.rowIndex].content[neighbourRef.cellIndex];
    setSpan(cell, "colspan", span(cell, "colspan") + span(neighbour, "colspan"));
    // Move the neighbour's content into the merged cell.
    cell.content = [...((_a = cell.content) !== null && _a !== void 0 ? _a : []), ...((_b = neighbour.content) !== null && _b !== void 0 ? _b : [])];
    rows[neighbourRef.rowIndex].content.splice(neighbourRef.cellIndex, 1);
    return true;
}
/**
 * Merge the cell at visual (row,col) with the cell directly below it.
 */
function mergeDown(table, row, col) {
    var _a, _b;
    const map = buildTableMap(table);
    const ref = cellAt(map, row, col);
    if (!ref)
        return false;
    const rect = rectOf(map, ref);
    if (!rect || rect.bottom >= map.height)
        return false;
    const neighbourRef = cellAt(map, rect.bottom, rect.left);
    if (!neighbourRef)
        return false;
    const neighbourRect = rectOf(map, neighbourRef);
    if (!neighbourRect)
        return false;
    if (neighbourRect.left !== rect.left || neighbourRect.right !== rect.right) {
        return false;
    }
    const rows = table.content;
    const cell = rows[ref.rowIndex].content[ref.cellIndex];
    const neighbour = rows[neighbourRef.rowIndex].content[neighbourRef.cellIndex];
    setSpan(cell, "rowspan", span(cell, "rowspan") + span(neighbour, "rowspan"));
    cell.content = [...((_a = cell.content) !== null && _a !== void 0 ? _a : []), ...((_b = neighbour.content) !== null && _b !== void 0 ? _b : [])];
    rows[neighbourRef.rowIndex].content.splice(neighbourRef.cellIndex, 1);
    return true;
}
/**
 * Split a merged cell back into 1×1 cells. The original cell keeps its
 * content; the freed visual cells become empty cells.
 */
function splitCell(table, row, col) {
    var _a;
    const map = buildTableMap(table);
    const ref = cellAt(map, row, col);
    if (!ref)
        return false;
    const rect = rectOf(map, ref);
    if (!rect)
        return false;
    const colspan = rect.right - rect.left;
    const rowspan = rect.bottom - rect.top;
    if (colspan <= 1 && rowspan <= 1)
        return false; // nothing to split
    const rows = table.content;
    const cell = rows[ref.rowIndex].content[ref.cellIndex];
    const header = Boolean((_a = cell.attrs) === null || _a === void 0 ? void 0 : _a.header);
    setSpan(cell, "colspan", 1);
    setSpan(cell, "rowspan", 1);
    // Fill the freed visual cells, working row by row. For the cell's
    // own row we add (colspan-1) cells right after it; for the rows it
    // used to span we insert a run of `colspan` cells.
    for (let r = rect.top; r < rect.bottom; r++) {
        const targetRow = rows[r];
        if (!targetRow.content)
            targetRow.content = [];
        if (r === rect.top) {
            const insertAt = ref.cellIndex + 1;
            for (let i = 0; i < colspan - 1; i++) {
                targetRow.content.splice(insertAt, 0, emptyCell(header));
            }
        }
        else {
            // Find the logical insertion index that matches visual column
            // rect.left in this row.
            const rowMap = buildTableMap(table);
            let insertAt = targetRow.content.length;
            for (let ci = 0; ci < targetRow.content.length; ci++) {
                const rr = rectOf(rowMap, { rowIndex: r, cellIndex: ci });
                if (rr && rr.left >= rect.left) {
                    insertAt = ci;
                    break;
                }
            }
            for (let i = 0; i < colspan; i++) {
                targetRow.content.splice(insertAt, 0, emptyCell(header));
            }
        }
    }
    return true;
}
/**
 * Merge every cell inside a visual rectangle into one.
 *
 * The rectangle must be "clean": every cell it touches must lie fully
 * inside it — a cell sticking out (a span crossing the boundary)
 * makes the merge impossible, and the function returns false. The
 * top-left cell becomes the merged cell, taking a colspan / rowspan
 * that covers the whole rectangle; the others' content is appended to
 * it in reading order and they're removed.
 */
function mergeCellRange(table, rect) {
    var _a, _b, _c;
    const map = buildTableMap(table);
    if (rect.top < 0 ||
        rect.left < 0 ||
        rect.bottom > map.height ||
        rect.right > map.width ||
        rect.right - rect.left < 1 ||
        rect.bottom - rect.top < 1) {
        return false;
    }
    // Collect the distinct cells inside the rectangle, checking each
    // one is fully contained.
    const refs = [];
    const seen = new Set();
    for (let r = rect.top; r < rect.bottom; r++) {
        for (let c = rect.left; c < rect.right; c++) {
            const ref = cellAt(map, r, c);
            if (!ref)
                return false; // hole in the grid
            const key = refKey(ref);
            if (seen.has(key))
                continue;
            seen.add(key);
            const cr = rectOf(map, ref);
            if (!cr ||
                cr.top < rect.top ||
                cr.left < rect.left ||
                cr.bottom > rect.bottom ||
                cr.right > rect.right) {
                return false; // ragged — a cell crosses the boundary
            }
            refs.push(ref);
        }
    }
    if (refs.length < 2)
        return false; // nothing to merge
    const rows = table.content;
    const anchorRef = cellAt(map, rect.top, rect.left);
    const anchorKey = refKey(anchorRef);
    const anchorCell = rows[anchorRef.rowIndex].content[anchorRef.cellIndex];
    // Append every other cell's content in reading order.
    const others = refs
        .filter((r) => refKey(r) !== anchorKey)
        .sort((a, b) => a.rowIndex - b.rowIndex || a.cellIndex - b.cellIndex);
    let content = [...((_a = anchorCell.content) !== null && _a !== void 0 ? _a : [])];
    for (const ref of others) {
        const cell = rows[ref.rowIndex].content[ref.cellIndex];
        content = [...content, ...((_b = cell.content) !== null && _b !== void 0 ? _b : [])];
    }
    anchorCell.content = content;
    setSpan(anchorCell, "colspan", rect.right - rect.left);
    setSpan(anchorCell, "rowspan", rect.bottom - rect.top);
    // Remove the others — per row, highest cell index first so the
    // lower indices stay valid.
    const byRow = new Map();
    for (const ref of others) {
        const list = (_c = byRow.get(ref.rowIndex)) !== null && _c !== void 0 ? _c : [];
        list.push(ref.cellIndex);
        byRow.set(ref.rowIndex, list);
    }
    for (const [rowIdx, cellIdxs] of byRow) {
        cellIdxs
            .sort((a, b) => b - a)
            .forEach((ci) => rows[rowIdx].content.splice(ci, 1));
    }
    return true;
}
// ============================================================================
// Internal helpers
// ============================================================================
function refEq(a, b) {
    return a.rowIndex === b.rowIndex && a.cellIndex === b.cellIndex;
}
function refKey(r) {
    return `${r.rowIndex}:${r.cellIndex}`;
}
// ============================================================================
// Column width
// ============================================================================
/**
 * Set a pixel `colwidth` on every single-column cell that starts in
 * the given visual column. With `table-layout: fixed` (the default
 * theme), that fixes the column's rendered width.
 *
 * Cells spanning multiple columns are skipped — their width is the
 * sum of the columns they cover, so a single value wouldn't be
 * meaningful.
 */
function setColumnWidth(table, visualCol, width) {
    var _a, _b;
    const map = buildTableMap(table);
    if (visualCol < 0 || visualCol >= map.width)
        return;
    const rows = (_a = table.content) !== null && _a !== void 0 ? _a : [];
    const w = Math.max(24, Math.round(width));
    const done = new Set();
    for (let r = 0; r < map.height; r++) {
        const ref = cellAt(map, r, visualCol);
        if (!ref)
            continue;
        const key = refKey(ref);
        if (done.has(key))
            continue;
        done.add(key);
        const rect = rectOf(map, ref);
        if (rect && rect.left === visualCol && rect.right - rect.left === 1) {
            const cell = rows[ref.rowIndex].content[ref.cellIndex];
            cell.attrs = { ...((_b = cell.attrs) !== null && _b !== void 0 ? _b : {}), colwidth: w };
        }
    }
}

},{}],
34:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-markdown
 *
 * Markdown input shortcuts. Each rule watches the text typed at the
 * start of a block and, when it matches, rewrites the block:
 *
 *   `# ` … `###### `  → Heading 1-6
 *   `> `              → Blockquote
 *   `- ` / `* ` / `+ `→ Bullet list
 *   `1. `             → Ordered list
 *   ` ``` `           → Code block
 *   `--- `            → Horizontal rule
 *
 * These cover §11 "Markdown shortcuts" of the brief. The rules call
 * the same commands the toolbar buttons use, so behaviour stays
 * consistent however the user triggers it.
 *
 * Implementation note: a rule fires from `editor`'s input-rule engine,
 * which hands the handler the editor plus the regex match. The handler
 * strips the typed prefix, then either changes the block type
 * directly or delegates to a command.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.fromMarkdown = exports.toMarkdown = exports.MarkdownExtension = void 0;
const core_1 = require("@smeditor/core");
/** Place the caret at the start of the block identified by `path`. */
function caretAtBlockStart(path) {
    return {
        anchor: { path, offset: 0 },
        head: { path, offset: 0 },
    };
}
/**
 * Strip the matched prefix from the current block and turn the block
 * into `type` with `attrs`. Used by the heading rule.
 *
 * The target is resolved by the FULL selection path (via `nodeAt`), so a
 * rule that fires from text inside a table cell / nested list paragraph
 * retypes that leaf block rather than the top-level container.
 */
function retypeBlock(editor, prefixLength, type, attrs) {
    var _a;
    const sel = editor.getSelection();
    if (!sel)
        return false;
    const path = sel.anchor.path;
    const doc = (0, core_1.cloneDoc)(editor.getJSON());
    const block = (0, core_1.nodeAt)(doc, path);
    if (!block)
        return false;
    (0, core_1.stripBlockPrefix)(block, prefixLength);
    block.type = type;
    if (attrs)
        block.attrs = { ...((_a = block.attrs) !== null && _a !== void 0 ? _a : {}), ...attrs };
    else if (block.attrs)
        delete block.attrs;
    editor.dispatch({
        doc,
        selection: caretAtBlockStart(path),
        addToHistory: true,
    });
    return true;
}
/**
 * Strip the matched prefix, then run a command (used for list / quote
 * rules where the command does the wrapping).
 *
 * The target leaf is resolved by the FULL selection path. The
 * prefix-strip is dispatched with `addToHistory:false` so that the strip
 * plus the delegated command collapse into a single undo step (the
 * delegated command records the one history entry).
 */
function stripThenCommand(editor, prefixLength, run) {
    const sel = editor.getSelection();
    if (!sel)
        return false;
    const path = sel.anchor.path;
    const doc = (0, core_1.cloneDoc)(editor.getJSON());
    const block = (0, core_1.nodeAt)(doc, path);
    if (!block)
        return false;
    (0, core_1.stripBlockPrefix)(block, prefixLength);
    editor.dispatch({
        doc,
        selection: caretAtBlockStart(path),
        addToHistory: false,
    });
    return run(editor);
}
/**
 * Inline markdown shortcut: when the user finishes typing a wrapped
 * span (e.g. `**bold**`), replace the visible range with a single text
 * node carrying `markType`, dropping the delimiters. The mark is only
 * applied when the schema actually has it, so the rule is a no-op (the
 * text stays literal) in a kit that doesn't load that mark.
 */
function inlineMarkRule(match, markType) {
    return {
        match,
        handler: (editor, m) => {
            var _a;
            if (!editor.schema.marks[markType])
                return false;
            const sel = editor.getSelection();
            if (!sel)
                return false;
            const inner = m[1];
            if (!inner)
                return false;
            const path = sel.anchor.path;
            const caret = sel.anchor.offset;
            const start = caret - m[0].length;
            if (start < 0)
                return false;
            const doc = (0, core_1.cloneDoc)(editor.getJSON());
            const block = (0, core_1.nodeAt)(doc, path);
            if (!block)
                return false;
            const marked = {
                type: "text",
                text: inner,
                marks: [{ type: markType }],
            };
            block.content = (0, core_1.replaceInlineRange)((_a = block.content) !== null && _a !== void 0 ? _a : [], start, caret, [marked]);
            const offset = start + inner.length;
            editor.dispatch({
                doc,
                selection: { anchor: { path, offset }, head: { path, offset } },
                addToHistory: true,
            });
            return true;
        },
    };
}
const rules = [
    // Headings — # … ######  (note the trailing space in the typed text)
    {
        match: /^(#{1,6})\s$/,
        handler: (editor, m) => retypeBlock(editor, m[0].length, "heading", { level: m[1].length }),
    },
    // Blockquote — "> "
    {
        match: /^>\s$/,
        handler: (editor, m) => stripThenCommand(editor, m[0].length, (e) => { var _a, _b, _c; return (_c = (_b = (_a = e.commands).toggleBlockquote) === null || _b === void 0 ? void 0 : _b.call(_a)) !== null && _c !== void 0 ? _c : false; }),
    },
    // Bullet list — "- ", "* ", "+ "
    {
        match: /^[-*+]\s$/,
        handler: (editor, m) => stripThenCommand(editor, m[0].length, (e) => { var _a, _b, _c; return (_c = (_b = (_a = e.commands).toggleBulletList) === null || _b === void 0 ? void 0 : _b.call(_a)) !== null && _c !== void 0 ? _c : false; }),
    },
    // Ordered list — "1. " (any starting number)
    {
        match: /^\d+\.\s$/,
        handler: (editor, m) => stripThenCommand(editor, m[0].length, (e) => { var _a, _b, _c; return (_c = (_b = (_a = e.commands).toggleOrderedList) === null || _b === void 0 ? void 0 : _b.call(_a)) !== null && _c !== void 0 ? _c : false; }),
    },
    // Code block — "```"
    {
        match: /^```$/,
        handler: (editor, m) => stripThenCommand(editor, m[0].length, (e) => { var _a, _b, _c; return (_c = (_b = (_a = e.commands).setCodeBlock) === null || _b === void 0 ? void 0 : _b.call(_a)) !== null && _c !== void 0 ? _c : false; }),
    },
    // Horizontal rule — "--- "
    {
        match: /^---\s$/,
        handler: (editor, m) => stripThenCommand(editor, m[0].length, (e) => { var _a, _b, _c; return (_c = (_b = (_a = e.commands).insertHorizontalRule) === null || _b === void 0 ? void 0 : _b.call(_a)) !== null && _c !== void 0 ? _c : false; }),
    },
    // Inline marks — completing a wrapped span autoformats it. Order
    // matters: the double-delimiter forms are tested before the single
    // ones so `**bold**` isn't mistaken for `*italic*`.
    inlineMarkRule(/\*\*([^*\n]+)\*\*$/, "bold"),
    inlineMarkRule(/(?<!\*)\*([^*\n]+)\*$/, "italic"),
    inlineMarkRule(/~~([^~\n]+)~~$/, "strike"),
    inlineMarkRule(/`([^`\n]+)`$/, "code"),
];
exports.MarkdownExtension = {
    name: "markdown",
    inputRules: rules,
};
exports.default = exports.MarkdownExtension;
// Markdown <-> document conversion (§16). Pure functions — usable
// without an editor instance.
var to_markdown_js_1 = require("./to-markdown.js");
Object.defineProperty(exports, "toMarkdown", { enumerable: true, get: function () { return to_markdown_js_1.toMarkdown; } });
var from_markdown_js_1 = require("./from-markdown.js");
Object.defineProperty(exports, "fromMarkdown", { enumerable: true, get: function () { return from_markdown_js_1.fromMarkdown; } });

},{"@smeditor/core":1,"./to-markdown.js":35,"./from-markdown.js":36}],
35:[function(require,module,exports){
"use strict";
/**
 * Markdown serialization — `DocumentJSON` → Markdown text.
 *
 * Covers the export side of §16. A pure function: document in,
 * string out, no DOM, no editor.
 *
 * Scope: the common CommonMark constructs plus GitHub tables and
 * strikethrough. Constructs Markdown can't express (text / background
 * color, font, sub/sup, highlight, indent, line height, cell merges)
 * degrade gracefully — the text survives, the styling is dropped.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.toMarkdown = toMarkdown;
// ============================================================================
// Inline
// ============================================================================
/** Escape characters that would otherwise be Markdown syntax. */
/**
 * Escape characters that would otherwise be Markdown syntax.
 *
 * `.` and `!` are deliberately *not* escaped: a period is only
 * syntactic as part of an ordered-list marker (`1.` at the start of a
 * line) and `!` only before a `[` (image). Escaping every occurrence
 * would litter ordinary prose with backslashes. The rare cost is that
 * a paragraph whose text literally starts with `1. ` re-parses as a
 * list on import.
 */
function escapeText(text) {
    return text.replace(/([\\`*_{}\[\]()#+\-~|])/g, "\\$1");
}
/**
 * Serialize one inline node. Marks are applied innermost-first;
 * `code` wins outright because Markdown can't format inside a code
 * span.
 */
function inlineToMarkdown(node) {
    var _a, _b, _c, _d, _e, _f, _g, _h;
    if (node.type === "hard_break")
        return "  \n";
    if (node.type === "image") {
        const alt = String((_b = (_a = node.attrs) === null || _a === void 0 ? void 0 : _a.alt) !== null && _b !== void 0 ? _b : "");
        const src = String((_d = (_c = node.attrs) === null || _c === void 0 ? void 0 : _c.src) !== null && _d !== void 0 ? _d : "");
        return `![${alt}](${src})`;
    }
    if (node.type !== "text")
        return "";
    let text = (_e = node.text) !== null && _e !== void 0 ? _e : "";
    const marks = (_f = node.marks) !== null && _f !== void 0 ? _f : [];
    const has = (t) => marks.some((m) => m.type === t);
    // Code span — content is literal, no nested formatting.
    if (has("code")) {
        return `\`${text}\``;
    }
    text = escapeText(text);
    if (has("strike"))
        text = `~~${text}~~`;
    // Bold + italic combine as ***…***.
    if (has("bold") && has("italic"))
        text = `***${text}***`;
    else if (has("bold"))
        text = `**${text}**`;
    else if (has("italic"))
        text = `*${text}*`;
    const link = marks.find((m) => m.type === "link");
    if (link) {
        const href = String((_h = (_g = link.attrs) === null || _g === void 0 ? void 0 : _g.href) !== null && _h !== void 0 ? _h : "");
        text = `[${text}](${href})`;
    }
    return text;
}
function inlineContent(node) {
    if (!node.content)
        return "";
    return node.content.map(inlineToMarkdown).join("");
}
/** Plain text of a node tree — used for code blocks and table cells. */
function plainText(node) {
    var _a;
    if (node.type === "text")
        return (_a = node.text) !== null && _a !== void 0 ? _a : "";
    if (!node.content)
        return "";
    return node.content.map(plainText).join("");
}
// ============================================================================
// Blocks
// ============================================================================
function listToMarkdown(node, ordered) {
    var _a;
    const items = (_a = node.content) !== null && _a !== void 0 ? _a : [];
    return items
        .map((item, i) => {
        var _a;
        const marker = ordered ? `${i + 1}.` : "-";
        // An item's blocks; first line gets the marker, the rest are
        // indented to line up under it.
        const body = ((_a = item.content) !== null && _a !== void 0 ? _a : [])
            .map((b) => blockToMarkdown(b))
            .join("\n\n");
        const lines = body.split("\n");
        return lines
            .map((line, li) => li === 0 ? `${marker} ${line}` : `   ${line}`)
            .join("\n");
    })
        .join("\n");
}
function tableToMarkdown(node) {
    var _a;
    const rows = (_a = node.content) !== null && _a !== void 0 ? _a : [];
    if (rows.length === 0)
        return "";
    const cellText = (cell) => {
        var _a;
        return ((_a = cell.content) !== null && _a !== void 0 ? _a : [])
            .map((b) => inlineContent(b))
            .join(" ")
            .replace(/\|/g, "\\|")
            .trim();
    };
    const matrix = rows.map((row) => { var _a; return ((_a = row.content) !== null && _a !== void 0 ? _a : []).map(cellText); });
    const cols = Math.max(...matrix.map((r) => r.length), 1);
    const pad = (r) => {
        const copy = [...r];
        while (copy.length < cols)
            copy.push("");
        return copy;
    };
    const out = [];
    const header = pad(matrix[0]);
    out.push(`| ${header.join(" | ")} |`);
    out.push(`| ${header.map(() => "---").join(" | ")} |`);
    for (let i = 1; i < matrix.length; i++) {
        out.push(`| ${pad(matrix[i]).join(" | ")} |`);
    }
    return out.join("\n");
}
function blockToMarkdown(node) {
    var _a, _b, _c, _d, _e, _f, _g, _h;
    switch (node.type) {
        case "paragraph":
            return inlineContent(node);
        case "heading": {
            const level = Math.min(6, Math.max(1, Number((_b = (_a = node.attrs) === null || _a === void 0 ? void 0 : _a.level) !== null && _b !== void 0 ? _b : 1)));
            return `${"#".repeat(level)} ${inlineContent(node)}`;
        }
        case "blockquote":
            return ((_c = node.content) !== null && _c !== void 0 ? _c : [])
                .map((b) => blockToMarkdown(b))
                .join("\n\n")
                .split("\n")
                .map((line) => (line ? `> ${line}` : ">"))
                .join("\n");
        case "code_block":
            return `\`\`\`\n${plainText(node)}\n\`\`\``;
        case "horizontal_rule":
            return "---";
        case "bullet_list":
            return listToMarkdown(node, false);
        case "ordered_list":
            return listToMarkdown(node, true);
        case "task_list":
            // Render task items as a GitHub task list.
            return ((_d = node.content) !== null && _d !== void 0 ? _d : [])
                .map((item) => {
                var _a, _b;
                const checked = ((_a = item.attrs) === null || _a === void 0 ? void 0 : _a.checked) ? "x" : " ";
                const body = ((_b = item.content) !== null && _b !== void 0 ? _b : [])
                    .map((b) => inlineContent(b))
                    .join(" ");
                return `- [${checked}] ${body}`;
            })
                .join("\n");
        case "image":
            return `![${String((_f = (_e = node.attrs) === null || _e === void 0 ? void 0 : _e.alt) !== null && _f !== void 0 ? _f : "")}](${String((_h = (_g = node.attrs) === null || _g === void 0 ? void 0 : _g.src) !== null && _h !== void 0 ? _h : "")})`;
        case "table":
            return tableToMarkdown(node);
        default:
            // Unknown block — fall back to its text content.
            return inlineContent(node);
    }
}
// ============================================================================
// Entry point
// ============================================================================
/**
 * Serialize a SMEditor document to a Markdown string.
 */
function toMarkdown(doc) {
    var _a;
    const blocks = (_a = doc.content) !== null && _a !== void 0 ? _a : [];
    const body = blocks
        .map((b) => blockToMarkdown(b))
        .filter((s) => s.length > 0)
        .join("\n\n");
    return body ? `${body}\n` : "";
}

},{}],
36:[function(require,module,exports){
"use strict";
/**
 * Markdown parsing — Markdown text → `DocumentJSON`.
 *
 * Covers the import side of §16. A pure function, no DOM.
 *
 * A compact, line-oriented parser handling the everyday CommonMark
 * constructs: ATX and setext headings, nested bullet / ordered / task
 * lists, blockquotes, fenced code, rules, GitHub tables, and the
 * common inline marks (bold, italic, code, strike) plus inline and
 * reference links / images. It is not a byte-exact CommonMark
 * implementation — HTML blocks and a few rare corners are out of
 * scope — but it round-trips everything SMEditor itself produces and
 * the bulk of hand-written Markdown. Anything unrecognised becomes a
 * paragraph.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.fromMarkdown = fromMarkdown;
/** A line like `[label]: https://example.com "Optional title"`. */
const LINK_DEF = /^ {0,3}\[([^\]]+)\]:\s*<?([^\s>]+)>?(?:\s+["'(](.*)["')])?\s*$/;
/**
 * Pull link reference definitions out of the document. Returns the
 * definitions plus the remaining lines (definition lines blanked, so
 * indices are preserved for the block scanner).
 */
function extractLinkDefs(lines) {
    const defs = new Map();
    const rest = lines.slice();
    for (let i = 0; i < rest.length; i++) {
        const m = LINK_DEF.exec(rest[i]);
        if (m) {
            const key = m[1].trim().toLowerCase();
            if (!defs.has(key)) {
                defs.set(key, m[3] ? { href: m[2], title: m[3] } : { href: m[2] });
            }
            rest[i] = "";
        }
    }
    return { defs, rest };
}
/**
 * Parse inline Markdown into an array of text / image nodes. Handles
 * (in precedence order) code spans, images, inline + reference links,
 * bold+italic, bold, italic, strike.
 */
function parseInline(text, defs) {
    const out = [];
    const emit = (raw, marks) => {
        if (!raw)
            return;
        const node = { type: "text", text: raw };
        if (marks.length)
            node.marks = marks;
        out.push(node);
    };
    let i = 0;
    let plain = "";
    const flushPlain = () => {
        if (plain) {
            emit(unescape(plain), []);
            plain = "";
        }
    };
    while (i < text.length) {
        const rest = text.slice(i);
        // Escaped character — take the next char literally.
        if (rest[0] === "\\" && rest.length > 1) {
            plain += rest[1];
            i += 2;
            continue;
        }
        // Image: ![alt](src)
        let m = /^!\[([^\]]*)\]\(([^)]*)\)/.exec(rest);
        if (m) {
            flushPlain();
            out.push({ type: "image", attrs: { src: m[2], alt: m[1] } });
            i += m[0].length;
            continue;
        }
        // Reference image: ![alt][label] / ![alt][]
        m = /^!\[([^\]]*)\]\[([^\]]*)\]/.exec(rest);
        if (m) {
            const def = defs.get((m[2].trim() || m[1].trim()).toLowerCase());
            if (def) {
                flushPlain();
                out.push({ type: "image", attrs: { src: def.href, alt: m[1] } });
                i += m[0].length;
                continue;
            }
        }
        // Inline link: [text](href)
        m = /^\[([^\]]*)\]\(([^)]*)\)/.exec(rest);
        if (m) {
            flushPlain();
            emit(m[1], [{ type: "link", attrs: { href: m[2] } }]);
            i += m[0].length;
            continue;
        }
        // Reference link, full / collapsed: [text][label] / [text][]
        m = /^\[([^\]]+)\]\[([^\]]*)\]/.exec(rest);
        if (m) {
            const def = defs.get((m[2].trim() || m[1].trim()).toLowerCase());
            if (def) {
                flushPlain();
                emit(m[1], [{ type: "link", attrs: { href: def.href } }]);
                i += m[0].length;
                continue;
            }
        }
        // Reference link, shortcut: [label] — only if the label is defined,
        // otherwise the brackets are literal text.
        m = /^\[([^\]]+)\]/.exec(rest);
        if (m) {
            const def = defs.get(m[1].trim().toLowerCase());
            if (def) {
                flushPlain();
                emit(m[1], [{ type: "link", attrs: { href: def.href } }]);
                i += m[0].length;
                continue;
            }
        }
        // Code span: `code`
        m = /^`([^`]+)`/.exec(rest);
        if (m) {
            flushPlain();
            emit(m[1], [{ type: "code" }]);
            i += m[0].length;
            continue;
        }
        // Bold + italic: ***text***
        m = /^\*\*\*([^*]+)\*\*\*/.exec(rest);
        if (m) {
            flushPlain();
            emit(m[1], [{ type: "bold" }, { type: "italic" }]);
            i += m[0].length;
            continue;
        }
        // Bold: **text**
        m = /^\*\*([^*]+)\*\*/.exec(rest);
        if (m) {
            flushPlain();
            emit(m[1], [{ type: "bold" }]);
            i += m[0].length;
            continue;
        }
        // Strike: ~~text~~
        m = /^~~([^~]+)~~/.exec(rest);
        if (m) {
            flushPlain();
            emit(m[1], [{ type: "strike" }]);
            i += m[0].length;
            continue;
        }
        // Italic: *text*  (single asterisk, non-greedy)
        m = /^\*([^*]+)\*/.exec(rest);
        if (m) {
            flushPlain();
            emit(m[1], [{ type: "italic" }]);
            i += m[0].length;
            continue;
        }
        plain += rest[0];
        i += 1;
    }
    flushPlain();
    return out;
}
function unescape(text) {
    return text.replace(/\\([\\`*_{}\[\]()#+\-.!~|])/g, "$1");
}
// ============================================================================
// Lists (nesting-aware)
// ============================================================================
const BULLET = /^(\s*)[-*+]\s+/;
const ORDERED = /^(\s*)\d+\.\s+/;
const TASK = /^(\s*)[-*+]\s+\[[ xX]\]\s/;
/** Leading-space count of a line. */
function indentOf(line) {
    const m = /^(\s*)/.exec(line);
    return m ? m[1].length : 0;
}
/** Which list marker, if any, a line carries. */
function listKind(line) {
    if (TASK.test(line))
        return "task";
    if (BULLET.test(line))
        return "bullet";
    if (ORDERED.test(line))
        return "ordered";
    return null;
}
/** Strip the marker, returning the item's text content. */
function itemText(line, kind) {
    if (kind === "task") {
        return line.replace(/^\s*[-*+]\s+\[[ xX]\]\s+/, "");
    }
    if (kind === "ordered")
        return line.replace(ORDERED, "");
    return line.replace(BULLET, "");
}
/** A line continues the current list-item if it's indented and not a marker. */
function isTaskChecked(line) {
    const m = /^\s*[-*+]\s+\[([ xX])\]/.exec(line);
    return !!m && m[1].toLowerCase() === "x";
}
/**
 * Parse a list starting at `start`. A line indented at least two
 * spaces past the current level opens a nested list attached to the
 * preceding item. Returns the list node and the index after it.
 */
function parseList(lines, start, defs) {
    var _a;
    const baseIndent = indentOf(lines[start]);
    const baseKind = listKind(lines[start]);
    const items = [];
    let i = start;
    while (i < lines.length) {
        const line = lines[i];
        if (line.trim() === "") {
            // A blank line followed by a same-level item keeps the list
            // going (a loose list); anything else ends it.
            const peek = i + 1;
            if (peek < lines.length &&
                listKind(lines[peek]) &&
                indentOf(lines[peek]) <= baseIndent + 1) {
                i++;
                continue;
            }
            break;
        }
        const kind = listKind(line);
        if (!kind)
            break;
        const indent = indentOf(line);
        if (indent < baseIndent)
            break; // belongs to an outer list
        if (indent >= baseIndent + 2 && items.length > 0) {
            // Nested list — attach to the last item.
            const { node, next } = parseList(lines, i, defs);
            const last = items[items.length - 1];
            last.content = [...((_a = last.content) !== null && _a !== void 0 ? _a : []), node];
            i = next;
            continue;
        }
        // A same-level item.
        const text = itemText(line, kind);
        if (baseKind === "task") {
            items.push({
                type: "task_item",
                attrs: { checked: isTaskChecked(line) },
                content: [{ type: "paragraph", content: parseInline(text, defs) }],
            });
        }
        else {
            items.push({
                type: "list_item",
                content: [{ type: "paragraph", content: parseInline(text, defs) }],
            });
        }
        i++;
    }
    const listType = baseKind === "task"
        ? "task_list"
        : baseKind === "ordered"
            ? "ordered_list"
            : "bullet_list";
    return { node: { type: listType, content: items }, next: i };
}
// ============================================================================
// Block scanning
// ============================================================================
function isTableSeparator(line) {
    return /^\s*\|?\s*:?-{1,}:?\s*(\|\s*:?-{1,}:?\s*)*\|?\s*$/.test(line);
}
function splitTableRow(line) {
    return line
        .trim()
        .replace(/^\||\|$/g, "")
        .split("|")
        .map((c) => c.trim());
}
/** Parse a Markdown string into a SMEditor document. */
function fromMarkdown(md) {
    const raw = md.replace(/\r\n?/g, "\n").split("\n");
    const { defs, rest: lines } = extractLinkDefs(raw);
    const blocks = [];
    let i = 0;
    const paragraph = (text) => ({
        type: "paragraph",
        content: parseInline(text, defs),
    });
    const cell = (text) => ({
        type: "table_cell",
        content: [paragraph(text)],
    });
    while (i < lines.length) {
        const line = lines[i];
        // Blank line — skip.
        if (line.trim() === "") {
            i++;
            continue;
        }
        // Fenced code block.
        const fence = /^(```|~~~)/.exec(line);
        if (fence) {
            const body = [];
            i++;
            while (i < lines.length && !lines[i].startsWith(fence[1])) {
                body.push(lines[i]);
                i++;
            }
            i++; // closing fence
            blocks.push({
                type: "code_block",
                content: [{ type: "text", text: body.join("\n") }],
            });
            continue;
        }
        // Horizontal rule.
        if (/^\s*(-{3,}|\*{3,}|_{3,})\s*$/.test(line)) {
            blocks.push({ type: "horizontal_rule" });
            i++;
            continue;
        }
        // ATX heading.
        const heading = /^(#{1,6})\s+(.*)$/.exec(line);
        if (heading) {
            blocks.push({
                type: "heading",
                attrs: { level: heading[1].length },
                content: parseInline(heading[2].trim(), defs),
            });
            i++;
            continue;
        }
        // Setext heading — a text line underlined with === or ---.
        if (i + 1 < lines.length &&
            line.trim() !== "" &&
            !listKind(line) &&
            /^\s*(=+|-+)\s*$/.test(lines[i + 1]) &&
            !/^\s*-{3,}\s*$/.test(line)) {
            const level = lines[i + 1].trim().startsWith("=") ? 1 : 2;
            blocks.push({
                type: "heading",
                attrs: { level },
                content: parseInline(line.trim(), defs),
            });
            i += 2;
            continue;
        }
        // Blockquote — collect consecutive `>` lines.
        if (/^\s*>\s?/.test(line)) {
            const quoted = [];
            while (i < lines.length && /^\s*>\s?/.test(lines[i])) {
                quoted.push(lines[i].replace(/^\s*>\s?/, ""));
                i++;
            }
            blocks.push({
                type: "blockquote",
                content: [paragraph(quoted.join(" ").trim())],
            });
            continue;
        }
        // Table — a header row followed by a separator row.
        if (line.includes("|") &&
            i + 1 < lines.length &&
            isTableSeparator(lines[i + 1])) {
            const header = splitTableRow(line);
            i += 2; // header + separator
            const rows = [
                {
                    type: "table_row",
                    content: header.map((t) => {
                        const c = cell(t);
                        c.attrs = { header: true };
                        return c;
                    }),
                },
            ];
            while (i < lines.length && lines[i].includes("|") && lines[i].trim()) {
                rows.push({
                    type: "table_row",
                    content: splitTableRow(lines[i]).map(cell),
                });
                i++;
            }
            blocks.push({ type: "table", content: rows });
            continue;
        }
        // Lists — bullet / ordered / task, nesting-aware.
        if (listKind(line)) {
            const { node, next } = parseList(lines, i, defs);
            blocks.push(node);
            i = next;
            continue;
        }
        // Paragraph — gather consecutive non-blank, non-special lines.
        const para = [];
        while (i < lines.length && lines[i].trim() !== "") {
            const l = lines[i];
            if (/^(#{1,6})\s/.test(l) ||
                /^\s*>/.test(l) ||
                /^(```|~~~)/.test(l) ||
                listKind(l) ||
                /^\s*(-{3,}|\*{3,}|_{3,})\s*$/.test(l)) {
                if (para.length > 0)
                    break;
            }
            // A setext underline ends the paragraph (and re-tags it above).
            if (para.length > 0 && /^\s*(=+|-+)\s*$/.test(l))
                break;
            para.push(l);
            i++;
        }
        if (para.length)
            blocks.push(paragraph(para.join(" ")));
    }
    if (blocks.length === 0)
        blocks.push({ type: "paragraph" });
    return { type: "doc", content: blocks };
}

},{}],
37:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-placeholder
 *
 * Adds a placeholder to an empty editor. This is purely visual — the
 * document model is unchanged. We hook into `onCreate` and the editor's
 * `update`/`selectionUpdate` events to keep a `data-placeholder`
 * attribute in sync on the first empty paragraph.
 *
 * Styling comes from the theme. The default theme already targets
 * `[data-placeholder]::before`. Override that selector for custom
 * looks.
 *
 * Text is taken from `editor.options.placeholder` ("Start typing…" by
 * default) or from the extension's own `configure({ text })`.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.PlaceholderExtension = void 0;
const DEFAULT_TEXT = "Start typing…";
function makeExtension(options) {
    let unsubUpdate = null;
    let unsubSelection = null;
    const sync = (editor) => {
        var _a, _b;
        const el = editor.element;
        if (!el)
            return;
        const firstChild = el.firstElementChild;
        if (!firstChild)
            return;
        // We consider the editor "empty" when its first (and only) block
        // is empty of text — that matches the common UX of placeholder
        // disappearing the moment the user types anything.
        const json = editor.getJSON();
        const firstBlock = json.content[0];
        const isEmpty = json.content.length === 1 &&
            (!(firstBlock === null || firstBlock === void 0 ? void 0 : firstBlock.content) || firstBlock.content.length === 0);
        const text = (_b = (_a = options.text) !== null && _a !== void 0 ? _a : editor.options.placeholder) !== null && _b !== void 0 ? _b : DEFAULT_TEXT;
        if (isEmpty) {
            firstChild.setAttribute("data-placeholder", text);
        }
        else {
            firstChild.removeAttribute("data-placeholder");
        }
    };
    return {
        name: "placeholder",
        onCreate(editor) {
            // Initial paint after the editor mounts on the next tick.
            queueMicrotask(() => sync(editor));
            unsubUpdate = editor.on("update", () => sync(editor));
            unsubSelection = editor.on("selectionUpdate", () => sync(editor));
        },
        onDestroy() {
            unsubUpdate === null || unsubUpdate === void 0 ? void 0 : unsubUpdate();
            unsubSelection === null || unsubSelection === void 0 ? void 0 : unsubSelection();
            unsubUpdate = null;
            unsubSelection = null;
        },
        configure(o) {
            return makeExtension({ ...options, ...o });
        },
    };
}
exports.PlaceholderExtension = makeExtension({});
exports.default = exports.PlaceholderExtension;

},{}],
38:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-history
 *
 * The history stack and the built-in `undo` / `redo` commands live in
 * core (every editor needs them). This package exists for two reasons:
 *
 *   1. Discoverability — users who follow the "everything is an extension"
 *      mental model can `import { HistoryExtension } from
 *      "@smeditor/extension-history"` and expect it to do something.
 *
 *   2. Shortcut declaration — core registers Cmd-Z / Cmd-Shift-Z as a
 *      fallback, but having an explicit extension lets a user *replace*
 *      them with their own bindings without monkey-patching.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.HistoryExtension = void 0;
exports.HistoryExtension = {
    name: "history",
    keyboardShortcuts: {
        "Mod-z": (editor) => typeof editor.commands.undo === "function"
            ? editor.commands.undo()
            : false,
        "Mod-Shift-z": (editor) => typeof editor.commands.redo === "function"
            ? editor.commands.redo()
            : false,
        "Mod-y": (editor) => typeof editor.commands.redo === "function"
            ? editor.commands.redo()
            : false,
    },
};
exports.default = exports.HistoryExtension;

},{}],
39:[function(require,module,exports){
"use strict";
/**
 * @smeditor/full-kit
 *
 * The larger MVP-safe bundle. Includes everything in StarterKit plus
 * inline-formatting, color, text outline, sub/superscript, typography, indent
 * and word-count extensions.
 *
 * Architectural note: FullKit reuses StarterKit's extension list
 * directly — it doesn't duplicate paragraph/heading/bold/image/etc.
 * A fix to StarterKit automatically flows through to FullKit users.
 *
 * Horizontal rule, image and markdown shortcuts moved into StarterKit
 * in v0.0.4 (they're MVP-tier), so FullKit no longer lists them — it
 * gets them via `...StarterKit.extensions`.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.StarterKit = exports.getPlainText = exports.countCharacters = exports.countWords = exports.WordCountExtension = exports.LineHeightExtension = exports.FontSizeExtension = exports.FontFamilyExtension = exports.IndentExtension = exports.TableExtension = exports.BackgroundColorExtension = exports.TextStrokeExtension = exports.TextColorExtension = exports.HighlightExtension = exports.SuperscriptExtension = exports.SubscriptExtension = exports.InlineCodeExtension = exports.FullKit = void 0;
const starter_kit_1 = require("@smeditor/starter-kit");
Object.defineProperty(exports, "StarterKit", { enumerable: true, get: function () { return starter_kit_1.StarterKit; } });
const extension_inline_code_1 = require("@smeditor/extension-inline-code");
Object.defineProperty(exports, "InlineCodeExtension", { enumerable: true, get: function () { return extension_inline_code_1.InlineCodeExtension; } });
const extension_subscript_1 = require("@smeditor/extension-subscript");
Object.defineProperty(exports, "SubscriptExtension", { enumerable: true, get: function () { return extension_subscript_1.SubscriptExtension; } });
const extension_superscript_1 = require("@smeditor/extension-superscript");
Object.defineProperty(exports, "SuperscriptExtension", { enumerable: true, get: function () { return extension_superscript_1.SuperscriptExtension; } });
const extension_highlight_1 = require("@smeditor/extension-highlight");
Object.defineProperty(exports, "HighlightExtension", { enumerable: true, get: function () { return extension_highlight_1.HighlightExtension; } });
const extension_text_color_1 = require("@smeditor/extension-text-color");
Object.defineProperty(exports, "TextColorExtension", { enumerable: true, get: function () { return extension_text_color_1.TextColorExtension; } });
const extension_text_stroke_1 = require("@smeditor/extension-text-stroke");
Object.defineProperty(exports, "TextStrokeExtension", { enumerable: true, get: function () { return extension_text_stroke_1.TextStrokeExtension; } });
const extension_background_color_1 = require("@smeditor/extension-background-color");
Object.defineProperty(exports, "BackgroundColorExtension", { enumerable: true, get: function () { return extension_background_color_1.BackgroundColorExtension; } });
const extension_indent_1 = require("@smeditor/extension-indent");
Object.defineProperty(exports, "IndentExtension", { enumerable: true, get: function () { return extension_indent_1.IndentExtension; } });
const extension_table_1 = require("@smeditor/extension-table");
Object.defineProperty(exports, "TableExtension", { enumerable: true, get: function () { return extension_table_1.TableExtension; } });
const extension_font_family_1 = require("@smeditor/extension-font-family");
Object.defineProperty(exports, "FontFamilyExtension", { enumerable: true, get: function () { return extension_font_family_1.FontFamilyExtension; } });
const extension_font_size_1 = require("@smeditor/extension-font-size");
Object.defineProperty(exports, "FontSizeExtension", { enumerable: true, get: function () { return extension_font_size_1.FontSizeExtension; } });
const extension_line_height_1 = require("@smeditor/extension-line-height");
Object.defineProperty(exports, "LineHeightExtension", { enumerable: true, get: function () { return extension_line_height_1.LineHeightExtension; } });
const extension_word_count_1 = require("@smeditor/extension-word-count");
Object.defineProperty(exports, "WordCountExtension", { enumerable: true, get: function () { return extension_word_count_1.WordCountExtension; } });
Object.defineProperty(exports, "countWords", { enumerable: true, get: function () { return extension_word_count_1.countWords; } });
Object.defineProperty(exports, "countCharacters", { enumerable: true, get: function () { return extension_word_count_1.countCharacters; } });
Object.defineProperty(exports, "getPlainText", { enumerable: true, get: function () { return extension_word_count_1.getPlainText; } });
exports.FullKit = {
    name: "full-kit",
    extensions: [
        ...starter_kit_1.StarterKit.extensions,
        extension_inline_code_1.InlineCodeExtension,
        extension_subscript_1.SubscriptExtension,
        extension_superscript_1.SuperscriptExtension,
        extension_highlight_1.HighlightExtension,
        extension_text_color_1.TextColorExtension,
        extension_text_stroke_1.TextStrokeExtension,
        extension_background_color_1.BackgroundColorExtension,
        extension_indent_1.IndentExtension,
        extension_font_family_1.FontFamilyExtension,
        extension_font_size_1.FontSizeExtension,
        extension_line_height_1.LineHeightExtension,
        extension_word_count_1.WordCountExtension,
    ],
};
exports.default = exports.FullKit;

},{"@smeditor/starter-kit":12,"@smeditor/extension-inline-code":40,"@smeditor/extension-subscript":41,"@smeditor/extension-superscript":42,"@smeditor/extension-highlight":43,"@smeditor/extension-text-color":44,"@smeditor/extension-text-stroke":45,"@smeditor/extension-background-color":46,"@smeditor/extension-indent":47,"@smeditor/extension-table":32,"@smeditor/extension-font-family":48,"@smeditor/extension-font-size":49,"@smeditor/extension-line-height":50,"@smeditor/extension-word-count":51}],
40:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-inline-code
 *
 * Inline `<code>` mark — short code spans inside paragraphs.
 * Distinct from `code_block`, which is a block-level node.
 *
 * Shortcut: `Mod-e` (the de-facto standard across editors).
 * Excludes other marks: when applied, marks like bold/italic/link
 * are removed because monospace code shouldn't be re-styled.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.InlineCodeExtension = void 0;
const core_1 = require("@smeditor/core");
exports.InlineCodeExtension = {
    name: "inline_code",
    marks: [
        {
            name: "code",
            inclusive: false,
            // TODO: once MarkSpec supports an `excludes` field in core,
            // declare ["bold", "italic", "underline", "strike", "link"] so
            // code spans automatically clear other styling.
            toDOM: () => ["code", 0],
            parseDOM: [{ tag: "code" }],
        },
    ],
    commands: {
        toggleCode: () => (editor) => {
            const next = (0, core_1.toggleMarkInDoc)(editor.getJSON(), editor.getSelection(), "code");
            if (!next)
                return false;
            editor.dispatch({
                doc: next,
                selection: editor.getSelection(),
                addToHistory: true,
            });
            return true;
        },
    },
    keyboardShortcuts: {
        "Mod-e": (editor) => { var _a, _b, _c; return (_c = (_b = (_a = editor.commands).toggleCode) === null || _b === void 0 ? void 0 : _b.call(_a)) !== null && _c !== void 0 ? _c : false; },
    },
};
exports.default = exports.InlineCodeExtension;

},{"@smeditor/core":1}],
41:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-subscript
 *
 * Subscript text — `<sub>`. Mutually exclusive with superscript:
 * the two extensions list each other in `excludes`, so toggling
 * subscript removes any existing superscript on the same span.
 *
 * Shortcut: Mod-, (matches Google Docs).
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.SubscriptExtension = void 0;
const core_1 = require("@smeditor/core");
exports.SubscriptExtension = {
    name: "subscript",
    marks: [
        {
            name: "subscript",
            inclusive: true,
            // TODO: declare excludes: ["superscript"] once MarkSpec supports it
            toDOM: () => ["sub", 0],
            parseDOM: [
                { tag: "sub" },
                {
                    tag: "span",
                    getAttrs: (el) => {
                        var _a;
                        const style = (_a = el.getAttribute("style")) !== null && _a !== void 0 ? _a : "";
                        return /vertical-align:\s*sub/i.test(style) ? null : false;
                    },
                },
            ],
        },
    ],
    commands: {
        toggleSubscript: () => (editor) => {
            const next = (0, core_1.toggleMarkInDoc)(editor.getJSON(), editor.getSelection(), "subscript");
            if (!next)
                return false;
            editor.dispatch({
                doc: next,
                selection: editor.getSelection(),
                addToHistory: true,
            });
            return true;
        },
    },
    keyboardShortcuts: {
        "Mod-,": (editor) => { var _a, _b, _c; return (_c = (_b = (_a = editor.commands).toggleSubscript) === null || _b === void 0 ? void 0 : _b.call(_a)) !== null && _c !== void 0 ? _c : false; },
    },
};
exports.default = exports.SubscriptExtension;

},{"@smeditor/core":1}],
42:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-superscript
 *
 * Superscript text — `<sup>`. Mutually exclusive with subscript.
 * Shortcut: Mod-. (matches Google Docs).
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.SuperscriptExtension = void 0;
const core_1 = require("@smeditor/core");
exports.SuperscriptExtension = {
    name: "superscript",
    marks: [
        {
            name: "superscript",
            inclusive: true,
            // TODO: declare excludes: ["subscript"] once MarkSpec supports it
            toDOM: () => ["sup", 0],
            parseDOM: [
                { tag: "sup" },
                {
                    tag: "span",
                    getAttrs: (el) => {
                        var _a;
                        const style = (_a = el.getAttribute("style")) !== null && _a !== void 0 ? _a : "";
                        return /vertical-align:\s*super/i.test(style) ? null : false;
                    },
                },
            ],
        },
    ],
    commands: {
        toggleSuperscript: () => (editor) => {
            const next = (0, core_1.toggleMarkInDoc)(editor.getJSON(), editor.getSelection(), "superscript");
            if (!next)
                return false;
            editor.dispatch({
                doc: next,
                selection: editor.getSelection(),
                addToHistory: true,
            });
            return true;
        },
    },
    keyboardShortcuts: {
        "Mod-.": (editor) => { var _a, _b, _c; return (_c = (_b = (_a = editor.commands).toggleSuperscript) === null || _b === void 0 ? void 0 : _b.call(_a)) !== null && _c !== void 0 ? _c : false; },
    },
};
exports.default = exports.SuperscriptExtension;

},{"@smeditor/core":1}],
43:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-highlight
 *
 * Background-color highlighter mark. Renders as `<mark>` (semantic).
 * Optional `color` attr — when set, becomes
 * `<mark style="background-color: COLOR">`. When unset, plain `<mark>`
 * inherits the browser's default yellow.
 *
 * Security: only safe-looking CSS color tokens are accepted (hex,
 * rgb/rgba, hsl/hsla, named keywords). Anything else is rejected at
 * parse time so `style` can't become an injection vector.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.HighlightExtension = void 0;
const core_1 = require("@smeditor/core");
exports.HighlightExtension = {
    name: "highlight",
    marks: [
        {
            name: "highlight",
            inclusive: true,
            attrs: { color: { default: null } },
            toDOM: (mark) => {
                var _a;
                const color = (0, core_1.sanitizeCSSColor)((_a = mark.attrs) === null || _a === void 0 ? void 0 : _a.color);
                return color
                    ? ["mark", { style: `background-color: ${color}` }, 0]
                    : ["mark", 0];
            },
            parseDOM: [
                {
                    tag: "mark",
                    getAttrs: (el) => {
                        var _a;
                        const style = (_a = el.getAttribute("style")) !== null && _a !== void 0 ? _a : "";
                        const m = /background(?:-color)?\s*:\s*([^;]+)/i.exec(style);
                        if (!m)
                            return null;
                        const color = (0, core_1.sanitizeCSSColor)(m[1]);
                        return color ? { color } : false;
                    },
                },
            ],
        },
    ],
    commands: {
        /**
         * setHighlight({ color }) — highlights the selected text. With a
         * colour it applies (and replaces) a coloured highlight on just
         * the selected range; without one it toggles a plain `<mark>`.
         */
        setHighlight: (opts = {}) => (editor) => {
            const color = opts.color ? (0, core_1.sanitizeCSSColor)(opts.color) : null;
            const selection = editor.getSelection();
            if (!selection)
                return false;
            if (color) {
                const next = (0, core_1.setMarkAcrossSelection)(editor.getJSON(), selection, {
                    type: "highlight",
                    attrs: { color },
                });
                if (!next)
                    return false;
                editor.dispatch({ doc: next, selection, addToHistory: true });
                return true;
            }
            const next = (0, core_1.toggleMarkInDoc)(editor.getJSON(), selection, "highlight");
            if (!next)
                return false;
            editor.dispatch({ doc: next, selection, addToHistory: true });
            return true;
        },
        /** Remove the highlight mark from the selection. */
        unsetHighlight: () => (editor) => {
            const selection = editor.getSelection();
            const next = (0, core_1.unsetMarkAcrossSelection)(editor.getJSON(), selection, "highlight");
            if (!next)
                return false;
            editor.dispatch({ doc: next, selection, addToHistory: true });
            return true;
        },
    },
    keyboardShortcuts: {
        "Mod-Shift-h": (editor) => { var _a, _b, _c; return (_c = (_b = (_a = editor.commands).setHighlight) === null || _b === void 0 ? void 0 : _b.call(_a)) !== null && _c !== void 0 ? _c : false; },
    },
};
exports.default = exports.HighlightExtension;

},{"@smeditor/core":1}],
44:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-text-color
 *
 * A mark that carries a CSS color. Renders as `<span style="color:…">`.
 *
 * Security: accepts only colors that look like a valid CSS color token
 * (hex, rgb/rgba, hsl/hsla, or a named keyword). Anything else is
 * rejected at parse time so `style` can't become an injection vector.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.TextColorExtension = void 0;
const core_1 = require("@smeditor/core");
exports.TextColorExtension = {
    name: "text_color",
    marks: [
        {
            name: "text_color",
            inclusive: true,
            attrs: { color: { default: null } },
            toDOM: (mark) => {
                var _a;
                const color = (0, core_1.sanitizeCSSColor)((_a = mark.attrs) === null || _a === void 0 ? void 0 : _a.color);
                return color ? ["span", { style: `color: ${color}` }, 0] : ["span", 0];
            },
            parseDOM: [
                {
                    tag: "*",
                    getAttrs: (el) => {
                        var _a;
                        const style = (_a = el.getAttribute("style")) !== null && _a !== void 0 ? _a : "";
                        // Match `color:` only at the start of a declaration so a
                        // `background-color:` declaration is never mistaken for it.
                        const m = /(?:^|;)\s*color\s*:\s*([^;]+)/i.exec(style);
                        if (!m)
                            return false;
                        const color = (0, core_1.sanitizeCSSColor)(m[1]);
                        return color ? { color } : false;
                    },
                },
            ],
        },
    ],
    commands: {
        /**
         * setTextColor({ color }) — colours exactly the selected text.
         * With a collapsed caret the whole block is coloured, so a
         * toolbar click still does something. Replaces any existing
         * colour on the range instead of stacking a second one.
         */
        setTextColor: (opts) => (editor) => {
            const color = (0, core_1.sanitizeCSSColor)(opts === null || opts === void 0 ? void 0 : opts.color);
            if (!color)
                return false;
            const selection = editor.getSelection();
            const next = (0, core_1.setMarkAcrossSelection)(editor.getJSON(), selection, {
                type: "text_color",
                attrs: { color },
            });
            if (!next)
                return false;
            editor.dispatch({ doc: next, selection, addToHistory: true });
            return true;
        },
        unsetTextColor: () => (editor) => {
            const selection = editor.getSelection();
            const next = (0, core_1.unsetMarkAcrossSelection)(editor.getJSON(), selection, "text_color");
            if (!next)
                return false;
            editor.dispatch({ doc: next, selection, addToHistory: true });
            return true;
        },
    },
};
exports.default = exports.TextColorExtension;

},{"@smeditor/core":1}],
45:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-text-stroke
 *
 * Inline text outline / stroke. Renders as a span with
 * `-webkit-text-stroke` plus `paint-order: stroke fill` so it works in
 * current Chromium/WebKit browsers while round-tripping through JSON.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.TextStrokeExtension = void 0;
const core_1 = require("@smeditor/core");
function sanitizeWidth(raw) {
    if (raw == null || raw === "")
        return "1px";
    const n = typeof raw === "number" ? raw : Number(String(raw).replace(/px$/i, ""));
    if (!Number.isFinite(n))
        return "1px";
    const clamped = Math.max(0, Math.min(n, 8));
    return `${Math.round(clamped * 100) / 100}px`;
}
function parseStroke(style) {
    var _a;
    const shorthand = /(?:^|;)\s*(?:-webkit-)?text-stroke\s*:\s*([0-9.]+px)\s+([^;]+)/i.exec(style);
    if (shorthand) {
        const color = (0, core_1.sanitizeCSSColor)(shorthand[2]);
        return color ? { width: sanitizeWidth(shorthand[1]), color } : false;
    }
    const colorMatch = /(?:^|;)\s*(?:-webkit-)?text-stroke-color\s*:\s*([^;]+)/i.exec(style);
    const widthMatch = /(?:^|;)\s*(?:-webkit-)?text-stroke-width\s*:\s*([^;]+)/i.exec(style);
    if (!colorMatch)
        return false;
    const color = (0, core_1.sanitizeCSSColor)(colorMatch[1]);
    return color
        ? { color, width: sanitizeWidth((_a = widthMatch === null || widthMatch === void 0 ? void 0 : widthMatch[1]) !== null && _a !== void 0 ? _a : "1px") }
        : false;
}
exports.TextStrokeExtension = {
    name: "text_stroke",
    marks: [
        {
            name: "text_stroke",
            inclusive: true,
            attrs: {
                color: { default: null },
                width: { default: "1px" },
            },
            toDOM: (mark) => {
                var _a, _b, _c;
                const color = (0, core_1.sanitizeCSSColor)((_a = mark.attrs) === null || _a === void 0 ? void 0 : _a.color);
                const width = sanitizeWidth(String((_c = (_b = mark.attrs) === null || _b === void 0 ? void 0 : _b.width) !== null && _c !== void 0 ? _c : "1px"));
                return color
                    ? [
                        "span",
                        {
                            style: `-webkit-text-stroke: ${width} ${color}; text-stroke: ${width} ${color}; paint-order: stroke fill`,
                        },
                        0,
                    ]
                    : ["span", 0];
            },
            parseDOM: [
                {
                    tag: "*",
                    getAttrs: (el) => {
                        var _a;
                        const parsed = parseStroke((_a = el.getAttribute("style")) !== null && _a !== void 0 ? _a : "");
                        return parsed || false;
                    },
                },
            ],
        },
    ],
    commands: {
        setTextStroke: (opts) => (editor) => {
            const color = (0, core_1.sanitizeCSSColor)(opts === null || opts === void 0 ? void 0 : opts.color);
            if (!color)
                return false;
            const width = sanitizeWidth(opts === null || opts === void 0 ? void 0 : opts.width);
            const selection = editor.getSelection();
            const next = (0, core_1.setMarkAcrossSelection)(editor.getJSON(), selection, {
                type: "text_stroke",
                attrs: { color, width },
            });
            if (!next)
                return false;
            editor.dispatch({ doc: next, selection, addToHistory: true });
            return true;
        },
        unsetTextStroke: () => (editor) => {
            const selection = editor.getSelection();
            const next = (0, core_1.unsetMarkAcrossSelection)(editor.getJSON(), selection, "text_stroke");
            if (!next)
                return false;
            editor.dispatch({ doc: next, selection, addToHistory: true });
            return true;
        },
    },
};
exports.default = exports.TextStrokeExtension;

},{"@smeditor/core":1}],
46:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-background-color
 *
 * Inline background color — `<span style="background-color: …">`.
 * Distinct from `highlight` (`<mark>`): highlight is semantic, this
 * is purely cosmetic so it doesn't render with `<mark>`'s default
 * yellow.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.BackgroundColorExtension = void 0;
const core_1 = require("@smeditor/core");
exports.BackgroundColorExtension = {
    name: "background_color",
    marks: [
        {
            name: "background_color",
            inclusive: true,
            attrs: { color: { default: null } },
            toDOM: (mark) => {
                var _a;
                const color = (0, core_1.sanitizeCSSColor)((_a = mark.attrs) === null || _a === void 0 ? void 0 : _a.color);
                return color
                    ? ["span", { style: `background-color: ${color}` }, 0]
                    : ["span", 0];
            },
            parseDOM: [
                {
                    tag: "*",
                    getAttrs: (el) => {
                        var _a;
                        if (el.tagName.toLowerCase() === "mark")
                            return false;
                        const style = (_a = el.getAttribute("style")) !== null && _a !== void 0 ? _a : "";
                        const m = /background(?:-color)?\s*:\s*([^;]+)/i.exec(style);
                        if (!m)
                            return false;
                        const color = (0, core_1.sanitizeCSSColor)(m[1]);
                        return color ? { color } : false;
                    },
                },
            ],
        },
    ],
    commands: {
        /** setBackgroundColor({ color }) — applies to the selected range. */
        setBackgroundColor: (opts) => (editor) => {
            const color = (0, core_1.sanitizeCSSColor)(opts === null || opts === void 0 ? void 0 : opts.color);
            if (!color)
                return false;
            const selection = editor.getSelection();
            const next = (0, core_1.setMarkAcrossSelection)(editor.getJSON(), selection, {
                type: "background_color",
                attrs: { color },
            });
            if (!next)
                return false;
            editor.dispatch({ doc: next, selection, addToHistory: true });
            return true;
        },
        unsetBackgroundColor: () => (editor) => {
            const selection = editor.getSelection();
            const next = (0, core_1.unsetMarkAcrossSelection)(editor.getJSON(), selection, "background_color");
            if (!next)
                return false;
            editor.dispatch({ doc: next, selection, addToHistory: true });
            return true;
        },
    },
};
exports.default = exports.BackgroundColorExtension;

},{"@smeditor/core":1}],
47:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-indent
 *
 * Block-level indentation. Sets a `level` attribute on the current
 * block(s); the theme reads that and applies a CSS margin.
 *
 * Conceptually distinct from list nesting:
 *   - List nesting uses `Tab`/`Shift+Tab` inside `bullet_list` /
 *     `ordered_list` to move items between nesting levels. That's
 *     handled in the list-item extension and isn't this one.
 *   - This extension lets you indent **any** block — a paragraph, a
 *     heading, a blockquote — without nesting it. Useful for callouts
 *     and "let me push this paragraph in a bit" cases.
 *
 * Levels are 0..8. Each level corresponds to 24 px of left padding by
 * default; themes can override `--sme-indent-step`.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.IndentExtension = void 0;
const core_1 = require("@smeditor/core");
const MAX_LEVEL = 8;
function applyDelta(editor, delta) {
    var _a, _b, _c;
    const sel = editor.getSelection();
    if (!sel)
        return false;
    const doc = (0, core_1.cloneDoc)(editor.getJSON());
    // Resolve through leafBlocksInSelection so a deep selection indents
    // the leaf blocks inside table cells, not just the top level.
    const leaves = (0, core_1.leafBlocksInSelection)(doc, sel);
    if (leaves.length === 0)
        return false;
    let changed = false;
    for (const { node: block } of leaves) {
        const current = Number((_b = (_a = block.attrs) === null || _a === void 0 ? void 0 : _a.indent) !== null && _b !== void 0 ? _b : 0) || 0;
        const next = Math.max(0, Math.min(MAX_LEVEL, current + delta));
        if (next === current)
            continue;
        if (next === 0) {
            if (block.attrs) {
                const { indent, ...rest } = block.attrs;
                if (Object.keys(rest).length === 0)
                    delete block.attrs;
                else
                    block.attrs = rest;
            }
        }
        else {
            block.attrs = { ...((_c = block.attrs) !== null && _c !== void 0 ? _c : {}), indent: next };
        }
        changed = true;
    }
    if (!changed)
        return false;
    editor.dispatch({ doc, selection: sel, addToHistory: true });
    return true;
}
exports.IndentExtension = {
    name: "indent",
    commands: {
        /** Move the current block(s) one indent level deeper. */
        indent: () => (editor) => applyDelta(editor, +1),
        /** Move the current block(s) one indent level out. */
        outdent: () => (editor) => applyDelta(editor, -1),
    },
    keyboardShortcuts: {
        Tab: (editor) => {
            var _a, _b, _c;
            // Only swallow Tab when actually changing indent. If we can't,
            // returning false lets the browser handle Tab (focus next field
            // when the editor is the last on the page, etc.).
            return (_c = (_b = (_a = editor.commands).indent) === null || _b === void 0 ? void 0 : _b.call(_a)) !== null && _c !== void 0 ? _c : false;
        },
        "Shift-Tab": (editor) => {
            var _a, _b, _c;
            return (_c = (_b = (_a = editor.commands).outdent) === null || _b === void 0 ? void 0 : _b.call(_a)) !== null && _c !== void 0 ? _c : false;
        },
    },
};
exports.default = exports.IndentExtension;

},{"@smeditor/core":1}],
48:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-font-family
 *
 * A mark carrying a CSS `font-family`. Renders as
 * `<span style="font-family: …">`.
 *
 * Security: the value is checked against a conservative pattern —
 * letters, digits, spaces, commas, hyphens and quotes only. Anything
 * with parens, semicolons or other CSS punctuation is rejected so the
 * `style` attribute can't be used as an injection vector.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.FontFamilyExtension = void 0;
const core_1 = require("@smeditor/core");
// Font stacks are letters / digits / spaces / commas / hyphens / quotes.
const FONT_FAMILY_TOKEN = /^[a-zA-Z0-9 ,"'-]+$/;
function sanitizeFamily(raw) {
    const t = raw.trim();
    if (!t || t.length > 200)
        return null;
    return FONT_FAMILY_TOKEN.test(t) ? t : null;
}
exports.FontFamilyExtension = {
    name: "font_family",
    marks: [
        {
            name: "font_family",
            inclusive: true,
            attrs: { family: { default: null } },
            toDOM: (mark) => {
                var _a, _b;
                const family = sanitizeFamily(String((_b = (_a = mark.attrs) === null || _a === void 0 ? void 0 : _a.family) !== null && _b !== void 0 ? _b : ""));
                return family
                    ? ["span", { style: `font-family: ${family}` }, 0]
                    : ["span", 0];
            },
            parseDOM: [
                {
                    tag: "*",
                    getAttrs: (el) => {
                        var _a;
                        const style = (_a = el.getAttribute("style")) !== null && _a !== void 0 ? _a : "";
                        const m = /font-family\s*:\s*([^;]+)/i.exec(style);
                        if (!m)
                            return false;
                        const family = sanitizeFamily(m[1]);
                        return family ? { family } : false;
                    },
                },
            ],
        },
    ],
    commands: {
        /** setFontFamily({ family }) — applies to the selected range. */
        setFontFamily: (opts) => (editor) => {
            var _a;
            const family = sanitizeFamily((_a = opts === null || opts === void 0 ? void 0 : opts.family) !== null && _a !== void 0 ? _a : "");
            if (!family)
                return false;
            const selection = editor.getSelection();
            const next = (0, core_1.setMarkAcrossSelection)(editor.getJSON(), selection, {
                type: "font_family",
                attrs: { family },
            });
            if (!next)
                return false;
            editor.dispatch({ doc: next, selection, addToHistory: true });
            return true;
        },
        unsetFontFamily: () => (editor) => {
            const selection = editor.getSelection();
            const next = (0, core_1.unsetMarkAcrossSelection)(editor.getJSON(), selection, "font_family");
            if (!next)
                return false;
            editor.dispatch({ doc: next, selection, addToHistory: true });
            return true;
        },
    },
};
exports.default = exports.FontFamilyExtension;

},{"@smeditor/core":1}],
49:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-font-size
 *
 * A mark carrying a CSS `font-size`. Renders as
 * `<span style="font-size: …">`.
 *
 * Accepted values: a number followed by a CSS length unit
 * (`px`, `pt`, `em`, `rem`, `%`). Bare numbers are treated as `px`.
 * Anything else is rejected.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.FontSizeExtension = void 0;
const core_1 = require("@smeditor/core");
const FONT_SIZE_TOKEN = /^(\d+(?:\.\d+)?)(px|pt|em|rem|%)?$/;
function sanitizeSize(raw) {
    var _a;
    const t = String(raw).trim();
    const m = FONT_SIZE_TOKEN.exec(t);
    if (!m)
        return null;
    const value = Number(m[1]);
    if (!Number.isFinite(value) || value <= 0 || value > 400)
        return null;
    const unit = (_a = m[2]) !== null && _a !== void 0 ? _a : "px";
    return `${m[1]}${unit}`;
}
exports.FontSizeExtension = {
    name: "font_size",
    marks: [
        {
            name: "font_size",
            inclusive: true,
            attrs: { size: { default: null } },
            toDOM: (mark) => {
                var _a, _b;
                const size = sanitizeSize(String((_b = (_a = mark.attrs) === null || _a === void 0 ? void 0 : _a.size) !== null && _b !== void 0 ? _b : ""));
                return size
                    ? ["span", { style: `font-size: ${size}` }, 0]
                    : ["span", 0];
            },
            parseDOM: [
                {
                    tag: "*",
                    getAttrs: (el) => {
                        var _a;
                        const style = (_a = el.getAttribute("style")) !== null && _a !== void 0 ? _a : "";
                        const m = /font-size\s*:\s*([^;]+)/i.exec(style);
                        if (!m)
                            return false;
                        const size = sanitizeSize(m[1]);
                        return size ? { size } : false;
                    },
                },
            ],
        },
    ],
    commands: {
        /** setFontSize({ size }) — applies to the selected range. */
        setFontSize: (opts) => (editor) => {
            var _a;
            const size = sanitizeSize(String((_a = opts === null || opts === void 0 ? void 0 : opts.size) !== null && _a !== void 0 ? _a : ""));
            if (!size)
                return false;
            const selection = editor.getSelection();
            const next = (0, core_1.setMarkAcrossSelection)(editor.getJSON(), selection, {
                type: "font_size",
                attrs: { size },
            });
            if (!next)
                return false;
            editor.dispatch({ doc: next, selection, addToHistory: true });
            return true;
        },
        unsetFontSize: () => (editor) => {
            const selection = editor.getSelection();
            const next = (0, core_1.unsetMarkAcrossSelection)(editor.getJSON(), selection, "font_size");
            if (!next)
                return false;
            editor.dispatch({ doc: next, selection, addToHistory: true });
            return true;
        },
    },
};
exports.default = exports.FontSizeExtension;

},{"@smeditor/core":1}],
50:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-line-height
 *
 * Block-level line spacing — the last piece of §5. Sets a
 * `lineHeight` attribute on the leaf block(s) under the selection;
 * the paragraph and heading extensions read it and emit
 * `style="line-height: …"`.
 *
 * Accepted values: a unitless multiplier (`1`, `1.5`, `2`) or a
 * number with a CSS unit (`px`, `em`, `rem`, `%`). Anything else is
 * rejected. `setLineHeight({ value: null })` clears it.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.LineHeightExtension = void 0;
const core_1 = require("@smeditor/core");
const LINE_HEIGHT_TOKEN = /^(\d+(?:\.\d+)?)(px|em|rem|%)?$/;
function sanitize(raw) {
    const t = String(raw).trim();
    const m = LINE_HEIGHT_TOKEN.exec(t);
    if (!m)
        return null;
    const value = Number(m[1]);
    if (!Number.isFinite(value) || value <= 0 || value > 10)
        return null;
    return m[2] ? `${m[1]}${m[2]}` : m[1];
}
exports.LineHeightExtension = {
    name: "line_height",
    commands: {
        /**
         * setLineHeight({ value }) — applies to every leaf block the
         * selection touches, reaching into table cells / blockquotes /
         * list items. Pass `{ value: null }` to clear.
         */
        setLineHeight: (opts) => (editor) => {
            const selection = editor.getSelection();
            if (!selection)
                return false;
            const raw = opts === null || opts === void 0 ? void 0 : opts.value;
            const value = raw === null || raw === undefined ? null : sanitize(String(raw));
            if (raw !== null && raw !== undefined && value === null)
                return false;
            const next = (0, core_1.setBlockAttrs)(editor.getJSON(), selection, {
                lineHeight: value,
            });
            if (!next)
                return false;
            editor.dispatch({ doc: next, selection, addToHistory: true });
            return true;
        },
        unsetLineHeight: () => (editor) => {
            const fn = editor.commands.setLineHeight;
            return typeof fn === "function" ? fn({ value: null }) : false;
        },
    },
};
exports.default = exports.LineHeightExtension;

},{"@smeditor/core":1}],
51:[function(require,module,exports){
"use strict";
/**
 * @smeditor/extension-word-count
 *
 * Doesn't add any nodes, marks, or commands — only utilities that
 * walk the current document and return character/word counts.
 *
 * Apps subscribe to `editor.on('update', …)` and call `countWords` /
 * `countCharacters` on each update, then render the totals somewhere
 * in their own UI. This keeps the extension surface tiny and lets
 * apps choose where the counts live (footer, sidebar, status bar, …).
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.WordCountExtension = void 0;
exports.getPlainText = getPlainText;
exports.countCharacters = countCharacters;
exports.countWords = countWords;
function walkText(node, sink) {
    var _a;
    if (node.type === "text") {
        sink((_a = node.text) !== null && _a !== void 0 ? _a : "");
        return;
    }
    if (!node.content)
        return;
    // Insert a space after every non-text child so sibling block boundaries
    // (list items, table cells, blockquote paragraphs, …) become whitespace
    // and don't merge into one word. Adjacent text leaves stay un-separated so
    // a word split across mark boundaries ("wor" + "ld") remains one word.
    for (const child of node.content) {
        walkText(child, sink);
        if (child.type !== "text")
            sink(" ");
    }
}
function getPlainText(doc) {
    var _a;
    // Build each top-level block's text, then join with a single space so word
    // counts don't run together — without a spurious trailing space.
    const top = (_a = doc.content) !== null && _a !== void 0 ? _a : [];
    return top
        .map((block) => {
        const parts = [];
        walkText(block, (t) => parts.push(t));
        return parts.join("");
    })
        .join(" ");
}
/** Count of Unicode characters (ignoring surrogate-pair quirks). */
function countCharacters(doc, opts) {
    const text = getPlainText(doc);
    if ((opts === null || opts === void 0 ? void 0 : opts.includeSpaces) === false)
        return text.replace(/\s+/g, "").length;
    return text.length;
}
/**
 * Count of words. A word is a maximal run of non-whitespace characters.
 * Matches what most UIs show — Google Docs, Word, Notion all use this
 * definition for their word counts.
 */
function countWords(doc) {
    const text = getPlainText(doc).trim();
    if (!text)
        return 0;
    return text.split(/\s+/).length;
}
/**
 * Build a (possibly configured) word-count extension.
 *
 * With no `limit`, the returned extension is identical to the original:
 * just a name, no `handleTextInput`, so it never blocks input and does
 * not affect StarterKit/FullKit or existing behavior. With a `limit`,
 * it adds a `handleTextInput` hook that refuses input once the limit
 * would be exceeded.
 */
function makeExtension(options) {
    var _a;
    const ext = {
        name: "word_count",
        // No nodes, marks, commands, or shortcuts. The package exists so it
        // shows up in the StarterKit/FullKit bundles and the helpers above
        // are importable from a stable module path.
        configure(o) {
            return makeExtension({ ...options, ...o });
        },
    };
    // Enforcement is strictly opt-in: only attach the hook when a limit is
    // given, so the unconfigured extension behaves EXACTLY as before.
    if (typeof options.limit === "number") {
        const limit = options.limit;
        const mode = (_a = options.mode) !== null && _a !== void 0 ? _a : "characters";
        ext.handleTextInput = (editor, text) => {
            const doc = editor.getJSON();
            if (mode === "words") {
                // Words mode (simple, conservative): only block input that would
                // start a NEW word once we're already at/over the word limit.
                // Typing inside or extending an existing word is always allowed,
                // and whitespace never starts a word, so it's allowed too.
                const startsNewWord = /\S/.test(text);
                if (!startsNewWord)
                    return false;
                return countWords(doc) >= limit;
            }
            // Characters mode (the primary use case): block when the incoming
            // text would push the total past the limit. includeSpaces defaults
            // to true, matching countCharacters' default.
            const current = countCharacters(doc);
            return current + text.length > limit;
        };
    }
    return ext;
}
exports.WordCountExtension = makeExtension({});
exports.default = exports.WordCountExtension;

},{}]
},C={};
function R(id){if(C[id])return C[id].exports;var x=M[id];if(!x)throw new Error("SMEditor module "+id+" not found");var m=C[id]={exports:{}};function r(s){var n=x[1][s];if(n===undefined)throw new Error("SMEditor dependency "+s+" not found");return R(n)}x[0](r,m,m.exports);return m.exports}R(0);
})();
